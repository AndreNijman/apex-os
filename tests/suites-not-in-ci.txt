# Suites CI does not run, and why. One path, then the reason.
#
# check-suites-run-in-ci.sh fails if a suite is in neither a workflow nor this
# file, AND fails if a suite here starts being run by CI — so this list can
# only shrink. Everything below that is not a `-live` suite is DEBT, not a
# decision: it should be wired into a workflow and deleted from here.
#
# ── legitimately cannot run on a GitHub runner ──────────────────────────────
tests/test-apex-audio-live.sh        needs real audio hardware; -live by name
tests/test-apex-firewall-live.sh     loads nftables into the live kernel; needs CAP_NET_ADMIN
tests/test-apex-lid-live.sh          needs a real SW_LID device and a logind session
#
# ── DEBT: these can run headless and simply were never wired in ─────────────
tests/test-apex-firewall-ssh.sh      asserts the ssh allowance in the shipped ruleset
tests/test-device-services-netns.sh  12 assertions; sends real packets in a private netns
tests/test-secret-at-rest.sh         the at-rest property P0-002 earned
tests/test-apex-multilib.sh          drives apex-pkg; needs care about what it installs
tests/test-labwc-session.sh          needs a nested compositor; check whether the harness now allows it
