{
  "_comment": "Live dispatch table. The orchestrator writes this at every dispatch and every landing. resume.sh reads it. agent_id is only useful while the session that spawned it lives; after a restart, dispatch a fresh agent with the card instead.",
  "updated": "2026-09-12T03:05+08:00",
  "_round": "Round 15. THREE UNITS FINISHED AND LANDED since round 14 - desktop-ai-apps, base-partials and p1-005 - and four fresh units were dispatched into the freed slots. Counts 64/25/36/2 -> 67 done / 25 partial / 33 todo / 2 blocked of 127. BASE is down to two partials, both hardware-blocked and both named as assertions rather than topics.",
  "_landed_this_round": {
    "apex-os": "roadmap/v2.2 fbf06a4f -> 4f80746e across three merges. 3bf8e558 desktop-ai-apps: both AI apps baked into the image from new stage 5a-aiapps, proven END TO END in a scratch fedora-bootc:43 container with the stanza text extracted verbatim, 11 mutation pairs. 59245497 base-partials: BASE-013 and BASE-014 closed, five product defects fixed. 4f80746e p1-005: sixteen Cloudflare capabilities, +39 tests to 2448/0, 23 mutation pairs, no live account touched.",
    "apex-shell": "roadmap/v2.2 9888537 -> cd324ec: the base-partials shell half (Settings names the mode, the PowerMenu gate, the PowerControl verb nothing asserted)."
  },
  "_status_recorded_this_round": "BASE-013, BASE-014, P1-006 -> done. BASE-009, BASE-010, P1-005, P1-007 -> partial, every one with its missing assertion named. The four partials are honest in different ways and the distinction matters: BASE-009 and BASE-010 are HARDWARE-blocked (the live Desktop->Gaming switch IS loginctl terminate-user and needs the gaming box, which Andre has put off-limits; placement and idle-unload need a GPU with a loaded model, and no reachable machine has one). P1-005 and P1-007 are STRUCTURE-blocked: R2's narrower-token half belongs to P1-011, and DNS's elevated class cannot be required because no elevated class exists in apex-secret-core - adding one is a field on OperationSpec across 28 literals in 6 files plus a break-glass grant path, which the agent deliberately refused to take unilaterally mid-integration.",
  "_deliberately_held": {
    "p1-005 / p1-008 / p1-011 / p1-014": "The Cloudflare capability suite, 13 tasks, mutually conflicting (p1-005 with p1-002, p1-008 with p1-002 and p1-005, p1-011 with p1-005 and p1-008, p1-014 with p1-011), so at most ONE can hold a slot at a time. NOT dispatched again this round, and the reason is arithmetic rather than preference: the ceiling is six and five units were one item from finishing. p1-005 is FIRST IN LINE the moment a slot frees, and the slot that frees soonest is p1-035's - its remaining work is one uncommitted file. Confirm p1-002 is not in the unlanded set before dispatching p1-005.",
    "p1-038-hardware": "Still not dispatchable unattended, and the reason has not changed: the 18 labwc matrix rows need real applications on a real session. Cannot be done headless, and cannot be done on the L16 while Andre is using it. A katana job once gaming is over, or a job for Andre with a checklist. The step-by-step list is in state/agents/p1-038.md. Do not pretend otherwise.",
    "p1-052-relay / p1-053a / p1-040": "READY, held for the ceiling. Do NOT re-dispatch the old p1-050 unit; queue.json records why it was replaced.",
    "apex remote is documented nowhere": "FINDINGS-round5 item 2, still unowned after three rounds. apex remote pair|devices|revoke|status|enable appears in no doc, and check-doc-verbs.sh structurally cannot catch it because it validates documented-verb -> real-command and not the reverse. It belongs to the next integrate or followups round, against the tip. Recorded here so it is not lost a fourth time.",
    "test-apex-task.sh": "NEW this round, and nobody owns it. 70 passed / 1 failed, and it is pre-existing: checked at 583355e before any of this round's merges and red there too. The assertion wants 'understands up to' in the refusal for a tasks.toml written by a newer apex; the message was reworded and the suite was not. A third red suite that the recorded CI baseline (which was taken at 1ab542c) does not show. One line to fix, in either direction - give it to the next followups round.",
    "the branches that are now safe to delete": "apex-os fix/pkg-multilib, fix/pkg-multilib-2, task/p1-018-mcp-auth, task/p1-050-remote-protocol (settled this round by content, not by heuristic); apex-shell fix/labwc-desktop-parity, fix/popup-first-open-and-media-keys, fix/screenshot-off-hyprland, task/p0-016-agent-settings. Left in place rather than deleted - deletion is cheap and irreversible, and nothing depends on it happening this round.",
    "integrate-4 housekeeping": "Three items were owed from integrate-4's card. ONE IS NOW DONE: the four nested shell suites have been run from the repo root on the landed tip and are green (see _verification_on_the_landed_tip). Still owed: the per-commit build check for the four p1-020os commits, and squash-merged branch cleanup by git merge-tree --write-tree comparison (the deletable list is under 'the branches that are now safe to delete').",
    "base-partials": "HELD ONE ROUND, deliberately, to keep the ceiling at six while a user-directed unit holds a slot. Four of its eight items are closed (BASE-018, BASE-002, BASE-016, BASE-005) and pushed; the four left are BASE-009, BASE-010, BASE-013, BASE-014. Two of those are exactly the ones that cannot be finished right now: BASE-009 is Gaming Mode and KATANA IS GAMING, and BASE-010 needs a live model that no machine here has loaded. Its branch also carries 836df42 'feat(greet): the session picker names the desktops, not the compositors' - the BASE-013 criterion-3 defect the recon identified, now fixed on the branch and unlanded. First in line next round.",
    "editors-launchable (user-directed)": "HOLDS THE SIXTH SLOT. Andre reported that nvim and zed do not work, then clarified: 'it says provided by apex already when i install but the existing one doesnt run'. Measured on BOTH machines before any change: /usr/bin/nvim is the real 64-bit neovim-0.11.6 rpm and runs on both; zed's binary runs on both; NEITHER machine has a Zed desktop entry; TERMINAL=alacritty IS shipped by the image so it is not a machine-local difference; xdg-terminal-exec is ABSENT on both; the L16 has ~/.config/nvim and katana has none. apex-pkg refusing with 'already provided by APEX-OS' is CORRECT - it stops an overlay shadowing the image's copy, which is the guard that was missing when 53 of katana's binaries went 32-bit - so the defect is that the provided copy cannot be LAUNCHED, not that the guard is wrong. Zed's half is fixed and landed (ec91e92). The agent on task/terminal-entries-launchable is measuring the other half headlessly: whether the launcher honours Terminal=true at all, since nvim.desktop declares it and the shell's claim that execute() 'respects Terminal=' is a comment, not evidence. It was told explicitly to report that the launcher is fine and the complaint is something else, if that is what it measures."
  },
  "agents": [
    {
      "slug": "p0-014",
      "items": [
        "P0-014",
        "P0-015"
      ],
      "repo": "both",
      "worktree": "/var/tmp/apex-work/wt-p0-014",
      "branch": "task/p0-014-remote-lock",
      "second_worktree": "/var/tmp/apex-work/wt-p0-015-shell",
      "second_branch": "task/p0-015-lock-tests",
      "agent_id": "af5342261e9b13e15",
      "queue_id": "p0-014",
      "dispatched": "2026-09-12T02:08+08:00",
      "fresh_agent_for_card": "p0-014",
      "session": "round 14 orchestrator, pid 59982",
      "title": "finish commit 4 (the enforcement) and commit 5, on a branch whose first six commits have landed",
      "at_dispatch": "Commit 4b DONE and pushed (2dd67e7), on top of commits 1-3 and the harness fix; 10 dirty paths to finish, commit and push. HANDED THE CI EVIDENCE ITS CARD SAID DID NOT EXIST: a GitHub runner's /proc/N/cgroup reads '/system.slice/hosted-compute-agent.service', a THIRD classification distinct from this laptop's session scope and from scheduled-job, and two request_origin tests fail there while nine pass - on a tip that already carries cca4442. Told explicitly NOT to weaken the gate to make CI green: a runner genuinely has no human at it, the gate saying so is the gate working, and the harness is what must present a real local origin. Then commit 5, and the long-open before/after counts.",
      "output_glob": "/tmp/claude-1000/-var-home-andre-Projects-apex/*/tasks"
    },
    {
      "slug": "p2-010",
      "items": [
        "P2-011"
      ],
      "repo": "apex-os",
      "worktree": "/var/tmp/apex-work/wt-p2-010",
      "branch": "task/p2-010-hardware-storage",
      "agent_id": "adc7ed64356ca851f",
      "queue_id": "p2-e",
      "dispatched": "2026-09-12T02:08+08:00",
      "fresh_agent_for_card": "p2-010",
      "session": "round 14 orchestrator, pid 59982",
      "title": "P2-011 agent cgroup budgets - the last item of a unit whose other three are done and recorded",
      "at_dispatch": "budget.rs and its tests written, committed and pushed (23b543ad); one dirty path to finish. CARRIES FORWARD ITS PREDECESSOR'S DYING FINDING, which the orchestrator independently hit days earlier and abandoned a run over: apex-agentd's unit tests can HANG FOREVER - three threads blocked with no timeout, egress::tests on a unix-socket read and two pty::tests on pipe reads. Any full-workspace run can stall a CI job or an agent indefinitely, and a hang is not a pass. Told to settle what it is and, if it is outside P2-011's scope, record it precisely as a FOUND rather than fix it silently.",
      "output_glob": "/tmp/claude-1000/-var-home-andre-Projects-apex/*/tasks"
    },
    {
      "slug": "p1-008",
      "items": [
        "P1-008",
        "P1-009",
        "P1-010"
      ],
      "repo": "apex-os",
      "worktree": "/var/tmp/apex-work/wt-p1-008",
      "branch": "task/p1-008-cloudflare-one",
      "agent_id": "a6a5f547e766361e4",
      "output_glob": "/tmp/claude-1000/-var-home-andre-Projects-apex/*/tasks",
      "dispatched": "2026-09-12T03:02+08:00",
      "session": "round 15 orchestrator, pid 59982",
      "queue_id": "p1-008",
      "title": "Cloudflare One, Workers AI and AI Gateway, Secrets Store",
      "card": "NONE AT DISPATCH - told to create it as an early action.",
      "at_dispatch": "Dispatched straight after p1-005 landed and pointed at it as the model: that unit took the provider to 22 of section 13.2's 32 names and THE TEN REMAINING ARE THIS UNIT'S. Handed p1-005's two defects as the shape to expect (error KIND asserted, not merely that an error happened) and warned that the missing elevated class will probably reach it too - if so, leave the criterion partial rather than adding a field across 28 literals mid-integration. No live account calls; the loopback double that 401s unauthenticated requests is already in the tree."
    },
    {
      "slug": "p1-040",
      "items": [
        "P1-040"
      ],
      "repo": "apex-shell",
      "worktree": "/var/tmp/apex-work/wt-p1-040",
      "branch": "task/p1-040-per-output-scaling",
      "agent_id": "a87fd6a859519fd98",
      "output_glob": "/tmp/claude-1000/-var-home-andre-Projects-apex/*/tasks",
      "dispatched": "2026-09-12T03:02+08:00",
      "session": "round 15 orchestrator, pid 59982",
      "queue_id": "p1-040",
      "title": "Per-output scaling: two densities, two sizes, one shell",
      "card": "NONE AT DISPATCH - told to create it as an early action.",
      "at_dispatch": "The harness already exists - run-scaling-test.sh brings up two headless outputs at different densities and is 30/0 - so the assertion has somewhere to live on day one. The cost is the refactor: one factor through two singletons with 831 call sites across 82 files. Told explicitly to judge the scope and, if the full migration is genuinely its own unit, to record the design and land whatever narrower provable improvement it can rather than half-migrating 831 call sites."
    },
    {
      "slug": "p2-b",
      "items": [
        "(see queue.json p2-b)"
      ],
      "repo": "both",
      "worktree": "/var/tmp/apex-work/wt-p2-b",
      "branch": "task/p2-b-accessibility-i18n",
      "agent_id": "ab0d75f96ea53bd12",
      "output_glob": "/tmp/claude-1000/-var-home-andre-Projects-apex/*/tasks",
      "dispatched": "2026-09-12T03:02+08:00",
      "session": "round 15 orchestrator, pid 59982",
      "queue_id": "p2-b",
      "title": "Accessibility baseline and internationalisation baseline",
      "card": "NONE AT DISPATCH - told to create it as an early action.",
      "at_dispatch": "New unit, no card. Told that a baseline is a MEASUREMENT rather than an aspiration: accessibility closes by asking the running artefact about accessible names, focus order and contrast, not by grepping for a property; i18n closes by proving strings extract and substitute, not that a framework supports it in principle. Where a criterion cannot be honestly measured headless, name it and leave it partial."
    },
    {
      "slug": "p1-052",
      "items": [
        "P1-051",
        "P1-052"
      ],
      "repo": "both",
      "worktree": "/var/tmp/apex-work/wt-p1-052",
      "branch": "task/p1-052-relay",
      "agent_id": "add3217c1c951ec31",
      "output_glob": "/tmp/claude-1000/-var-home-andre-Projects-apex/*/tasks",
      "dispatched": "2026-09-12T03:02+08:00",
      "session": "round 15 orchestrator, pid 59982",
      "queue_id": "p1-052",
      "title": "The APEX Remote remainder: relay, reconnect, mDNS, connection quality, two shell pages",
      "card": "NONE AT DISPATCH - told to create it as an early action.",
      "at_dispatch": "The queue note tells it what is left and what is NOT its: P1-051 stays partial for a reason outside this unit (the device key in Android Keystore with setUserAuthenticationRequired, a per-approval challenge and attestation - p1-053a's work, whose desktop half is p0-014's CTAP2 verifier, in flight right now), so it must not close P1-051 alone. THE SHAPING CONSTRAINT: do not deploy anything to Andre's Cloudflare account. Write the relay's server side with tests against a local double, exactly as the capability sets were proven against a loopback transport and the trust gate against a minted CA. 'Written, tested, ready to deploy' is a complete and honest outcome, and the card says what deploying would require."
    }
  ],
  "_field_contract": "resume.sh does os.path.isdir(worktree) and git rev-list origin/<branch>..HEAD, so `worktree` and `branch` must each be ONE value. An agent working two repos records the other in second_worktree/second_branch, which resume.sh ignores. Writing 'a and b' there makes the next report say 'no worktree' for a worktree that is fine.",
  "_findings": [
    "TWO DEFECTS OF ONE SHAPE in the Cloudflare provider, both surfaced only because a test asserted the error KIND rather than that an error happened: NoRecord fell through to Refused and NoResource to PermissionDenied, so 'you did not bind that' reached the caller as 'you may not'.",
    "FIVE PRODUCT DEFECTS from base-partials, two of which would have hit Andre directly: a remembered session that no longer exists made the greeter fall back to SORT ORDER, so a machine could land on a different desktop than the user chose; and the PowerMenu offered Gaming Mode on machines with no gamescope. Also labwc missing from the Settings compositor control entirely, layers.max(1) turning 'unknown' into 'one layer', and locate() ignoring Roots while deciding which binary gets EXECUTED - which had left three resolver tests asserting against the developer's real machine rather than the fixture.",
    "A MULTI-AGENT HAZARD worth designing around: a file in one agent's session scratchpad was overwritten mid-run by another unit's harness, because two harnesses shared a pristine/ directory. The affected agent switched to unique names and verified every file byte-identical afterwards, but a restore-the-wrong-file race between concurrent agents is a silent way to record a false mutation verdict.",
    "THREE PRE-EXISTING FLAKY TESTS recorded rather than touched, each failing once under parallel load and passing otherwise: apex-agentd grants::…another_boot… (its own module comment documents a set_var race), apex::provenance, and apex-agentd system_grants. None is in this round's diff.",
    "NAMING NEEDS ANDRE'S RATIFICATION and is now user-visible: 'Floating' is ratified at ROADMAP.md:1031, but 'Scrolling' (niri) and 'Tiling' (Hyprland) are not - and the greeter and Settings now show them. Asked."
  ],
  "_tip_verification": "ROADMAP/state/ci-suites-on-tip-1ab542c.txt — the full apex-os CI suite list run on roadmap/v2.2 @ 1ab542c: 27 of 30 green, 2 red (both pre-existing, see _findings), 1 skipped because its name ends -live. Plus cargo test --locked --workspace 1964 passed / 1 failed and run-clippy.sh rc=0. Recorded because integrate-4 chose its post-landing suites from memory rather than from CI's list; the list is now on disk.",
  "_orchestrator": {
    "pid": 59982,
    "note": "Interactive session, heartbeat guarded on pid. Took the write path back after the unattended orchestrator exited at 21:43:55."
  },
  "_previous_pause": {
    "when": "2026-09-07T15:32+08:00 to 2026-09-08T08:56+08:00",
    "what": "Andre paused the run to shut the laptop down. Six agents stopped with TaskStop, two hand-run snapshots (wt-p1-035 10 files +1713, wt-p1-039 5 files +1537, wt-p2-010 2 files +512/-17, wt-trust 2 files +1450/-187, apex-shell p3/recovery-ui 1 file), AUTORESUME removed, and the boot firing correctly refreshed the report without starting a session. Nothing was lost and no work had to be reconstructed.",
    "found": "The apex-os MAIN clone (/var/home/andre/Projects/apex/apex-os, not a worktree) is on a DETACHED HEAD in the middle of an interrupted rebase - .git/rebase-merge exists - with 64 untracked files. It is nobody's round-6 or round-7 work; every agent works in /var/tmp/apex-work/wt-*. It is the 'skipped mid-operation' line in the snapshot log, which means those 64 untracked files are NOT covered by refs/wip. They look like rebase leftovers rather than unique work (apexd/apex-aid/, apexd/apex/src/*.rs, all present in branch tips) but nobody has verified that. Do NOT blind-abort it: inspect, then decide. Deliberately left alone rather than tidied."
  },
  "_findings_amended": "ROADMAP/state/FINDINGS-round5.md item 1 now carries an AMENDED section. Its headline - 'apex ai status --json returns prose, not JSON' - was WRONG, and the p1-039 agent proved it by measuring the streams into separate files: stdout 803 bytes of valid JSON, stderr 50 bytes of prose, exit 0, and jq worked all along. What fails is a caller that merges them with 2>&1, which is what the suite line did. The CAUSE the finding named was right and is fixed (454ae3a): PATH presence stood in for hardware. Kept rather than rewritten, because the wrong headline is itself the lesson - a finding written from a suite's failure line inherits that line's assumptions.",
  "_completed_units": {
    "trust-enforcement": "COMPLETE AND LANDED 2026-09-08 (apex-os e67fab9 -> 583355e). Removed from queue.json units as well as from this table, and for the same reason followups-integrate-3 had to be: its items are '(new, from P1-047)' rather than roadmap ids, so resume.sh can never see it as finished and would have offered it as READY forever the moment its agent left the table."
  },
  "_verification_on_the_landed_tip": {
    "cargo": "cargo test --locked --workspace --no-fail-fast: 36 binaries, 2339 passed, 1 failed. The one failure is renewing_a_grant_that_does_not_exist_is_refused_without_asking_anybody in apex-agentd/tests/system_grants.rs - the SAME pre-existing cgroup/origin artifact the baseline (1964 passed / 1 failed) carried. No new failure. The landings added 6 test binaries and 375 tests.",
    "clippy": "tests/run-clippy.sh: PASS, clippy is clean - after 1afd807. It was NOT clean on the merge: the container resolved clippy 1.98 while files/scripts/clippy-local pins rust:1.97, and 1.98's redundant_guards flagged apex/src/firmware.rs:133. A branch can be measured clean and still redden the tip; run tests/run-clippy.sh, not just clippy-local.",
    "apex-os suites": "48 files: 40 rc=0, 4 skipped as -live, 3 rc=2 and 1 rc=1 - all four explained below.",
    "THE RESULT WORTH READING": "tests/test-privilege-requests.sh is 39 passed / 0 failed on this tip. It was 30 passed / 8 failed on the recorded baseline, and three integration rounds had written those eight off as an artifact. p0-014's harness fix (cca4442, 3c3abd3) is what changed: unshare -r gives the test a real effective uid 0 with no sudo and no prompt. FINDINGS-round5 item 3 is CLOSED.",
    "the four non-green": [
      "test-apex-env.sh and test-apex-resolve.sh reported rc=2 ('the stubs must be the only container tooling reachable') ONLY inside the sequential runner; run alone on the same tip they are 265/0 and 88/0. That is cross-suite interference in the runner, not a tip regression - do not chase it as one, and do not run these two in a loop with the others without isolating /tmp.",
      "test-apex-firewall-ssh.sh rc=2: 'refusing to guess a target; pass --target HOST'. It needs an argument.",
      "test-apex-task.sh 70 passed / 1 failed, and this one is REAL but PRE-EXISTING: checked at 583355e before any of this round's merges and it is 70/1 there too. The assertion wants 'understands up to' in the refusal for a tasks.toml from a newer apex; the message was reworded to 'is version 99, and this build of APEX reads version 1' and the suite was not updated. A third red suite nobody had recorded."
    ],
    "apex-shell": "22 static suites green, 17 node suites green, on 665a3cc.",
    "the four nested apex-shell runners": "RUN, and this closes one of the three housekeeping items integrate-4's card has owed since round 6. All four green on 665a3cc: run-compositor-facade-test.sh 52/0, run-scaling-test.sh 30/0, run-service-tier-test.sh 88/0, run-agent-center-smoke.sh rc=0 ('the Agent Center rendered 6 session(s) and 1 request(s) cleanly'). The smoke test SKIPS with rc=0 unless APEX_OS_ROOT names an apex-os checkout - it was re-run with APEX_OS_ROOT=/var/tmp/apex-work/int-os, which is the only way it actually executes. Worth running rather than inspecting: run-agent-center-smoke.sh is the file whose second error count was hand-resolved at integration (the tip's inline filter had drifted from the count_errors helper the branch added), and bash -n cannot tell whether a resolved helper call is reached. tests/lib/headless.sh owns XDG_RUNTIME_DIR, XDG_STATE_HOME, XDG_CONFIG_HOME and HOME and always starts its own headless wlroots compositor - always, not 'unless there is a session to nest in' - so none of this can put a window on Andre's desktop."
  },
  "_not_landed_and_why": {
    "task/p1-025-skills-plugins": "Merged cleanly, compiled, and then ABORTED before commit. Its own tip does not survive CI's clippy: apex/src/digest.rs's describe/is_measured/same_as and skill.rs's Origin::describe have no committed caller, and cargo clippy --all-targets -- -D warnings turns that into an error. The callers are in provenance.rs, which is P1-026 and is uncommitted in the worktree. Nothing is wrong with the work; it is half of a commit. It lands the moment P1-026 is committed. Do NOT 'fix' this by adding #[allow(dead_code)].",
    "task/p1-050-remote-protocol": "Nothing to land - SETTLED rather than assumed. resume.sh listed 09c977d as unlanded on a 2/10-added-lines heuristic; its actual content (the type_complexity alias in the Cloudflare provider tests, and the format! with nothing to interpolate in the CLI's Cloudflare tests) is already on the tip, and the tip carries MORE of that branch than the branch does. Safe to delete."
  },
  "_ci": "CI RAN FOR THE FIRST TIME IN THIS PROGRAM, and this is the record of what that cost and bought. pr-validation.yml triggered ONLY on `pull_request: branches: [main]`, and the program is forbidden from opening a PR until final integration - so sixty landed items, twelve landings and ~2000 Rust tests had been verified on exactly one laptop. `gh run list --branch roadmap/v2.2` returned nothing at all. Fixed in four commits, each one exposing the next problem: (1) push + workflow_dispatch triggers added; the first run failed in 18s. (2) The parity check cloned apex-shell's DEFAULT branch, so it compared apex-os roadmap/v2.2 against apex-shell main and reported drift on two touchpad defaults - while the two INTEGRATION branches agreed perfectly at 22 settings. It now clones the matching branch and PRINTS which one it used. THE DRIFT IT EXPOSED IS REAL AND IS KEPT: apex-shell main has drag_lock=true and click_method=clickfinger where apex-os main's generator has false and buttonAreas, so whoever does the final integration must land both sides together or opening the Input page silently changes a user's touchpad behaviour. (3) The path classifier read github.event.pull_request.* and died with exit 128 on a push; it is now event-aware and, when no range can be determined, selects EVERYTHING - this file already records four occasions where a selector picked nothing, the job skipped, and the workflow passed. (4) Six confinement assertions in mcp_sidecar_live.rs had never run anywhere but the L16 because the runner has no bubblewrap; installed rather than skipped, because they are the assertions that the sandbox IS a sandbox. Then bwrap failed on Ubuntu 24.04's kernel.apparmor_restrict_unprivileged_userns=1, so the sysctl is relaxed and PROVEN with a smoke check before any test runs. THE FINDING THAT PAYS FOR ALL OF IT: on the runner, /proc/N/cgroup reads '/system.slice/hosted-compute-agent.service', which is neither a login session nor a user service - a THIRD classification distinct from the L16's session scope and from scheduled-job. Two request_origin tests fail there while nine pass. Four rounds of orchestrators wrote that the workflow file was the only evidence of what CI presents; it is now measured, and p0-014 has been sent the exact string while it is still live."
}