# p1-045
items: P1-045, P1-046, P1-047
repo: apex-os
worktree: /var/tmp/apex-work/wt-p1-045
branch: task/p1-045-updates-trust

## NEXT
Start P1-045. Create `apexd/apexd-core/src/migrate.rs`: a `StoreSpec { id, path, current,
rollback: Rollback }` registry, a version peek that reads the version field without a full
parse, `plan()` returning UpToDate/Forward(steps)/TooNew, and `migrate()` that writes a
`.pre-v<N>` copy first and round-trip-verifies before rename (the precedent is
apexd/apex/src/task.rs:290 "refusing to write a task file that cannot be read back").
Wire two real stores: `Blueprint` (blueprint.toml, already versioned, rollback answer =
RefuseAndExplain) and `HostCaps` (~/.local/state/apex/hosts/<name>.json, unversioned,
rollback answer = ReadableByOlder — needs the `#[serde(flatten)] unknown` its doc already
claims). Declare it in `apexd-core/src/lib.rs`.

## DONE
- P1-047 pushed as 512bd91 `feat(trust): every image is signed, and no machine has ever
  checked one`. `apex status` carries a Trust block; `apex trust [--verify] [--json]` is
  the full readout. 17 rust unit tests + 25 shell assertions in tests/test-apex-trust.sh,
  wired into pr-validation static + rust jobs. Mutation pairs recorded below.

## IN PROGRESS
- nothing half-written

## FOUND
- `/etc/containers/policy.json` on the L16 is byte-identical to `/usr/etc/containers/policy.json`
  and contains one `insecureAcceptAnything` default. Every APEX image is cosign-signed and
  SBOM-attested in CI, and NOTHING on an installed machine checks any of it. The booted
  deployment's origin records this in its own words:
  `container-image-reference=ostree-unverified-registry:ghcr.io/andrenijman/apex-os:daily`.
- `bootc status` requires root even to READ (it opens the sysroot for write), so it is
  unusable from `apex status`. `rpm-ostree status --json` works unprivileged; so does
  reading `/proc/cmdline` -> `ostree=` symlink -> `<deploy>.origin`, which is world-readable.
- The booted image HAS a cosign signature in the registry
  (`ghcr.io/andrenijman/apex-os:sha256-308127d9....sig`), signed by
  `https://github.com/AndreNijman/apex-os/.github/workflows/build-image.yml@refs/heads/main`,
  source revision 57f593a. It has NO `.att` tag: the SBOM attestation landed on
  roadmap/v2.2 and has never been built, so "attestation absent" is a live, real state.
- `cosign` is not installed in the APEX image; `skopeo` and `openssl` are. So a machine can
  learn that a signature EXISTS and read the identity it claims, but cannot check its
  cryptography. That is a third state, distinct from signed and from unsigned.
- `apexd-core::host::HostCaps` (apexd-core/src/host.rs:470-476) documents "Keeps unknown keys,
  unlike the registry ... a field this build does not recognise is a newer peer" — and the
  derive has no `#[serde(flatten)]` catch-all, so unknown keys are silently dropped on the
  read-modify-write. A doc claiming forward compatibility the code does not provide.
- `apex-agent-core/src/request.rs:665` appends to `privilege-audit.jsonl` with no
  `.mode(0o600)`, unlike `apex-secret-core/src/audit.rs:194` which sets it. Umask-dependent.
- No channel/ring/stream concept exists anywhere in the tree. `Containerfile.base:472` states
  "ONE update channel" as a deliberate product invariant, so P1-046 is a reversal of a stated
  decision, not a gap-fill. `apex`, `daily`, `gaming-mesa`, `gaming-nvidia` are four aliases
  for ONE digest and must stay that way forever (build-image.yml:21-30).
- `clippy` is installed on neither the L16 nor katana. Run it as
  `podman run --rm -v <wt>:/src:z -w /src/apexd docker.io/library/rust:1.97 cargo clippy`.

## MUTATION PAIRS
- P1-047 #1: `classify_skopeo_failure` forced to return `Artifact::Absent` for every
  stderr -> `cargo test -p apex trust::` 16 passed / 1 failed
  (`an_unreachable_registry_is_never_reported_as_unsigned`); restored -> 17 passed / 0 failed.
- P1-047 #2: the `Err(e) if e == "not-ostree"` arm in `offline_report` collapsed to
  `Err(_) => Pull::NotOstree` -> tests/test-apex-trust.sh --with-binary 23 passed / 2 failed;
  restored -> 25 passed / 0 failed.

## BLOCKED ON
- nothing
