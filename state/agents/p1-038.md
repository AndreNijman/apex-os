# P1-038 — Floating/labwc validation matrix + the three "unmerged" August branches

Worktree: `/var/tmp/apex-work/wt-p1-038`, branch `task/p1-038-labwc-parity`,
based on `origin/roadmap/v2.2` @ `d2d1f33`.

## THE REFRAME — read this before touching anything

**The three branches are not unmerged. All 17 commits landed byte-for-byte in
August as squash-merges.** Proof: `git diff origin/fix/<branch> <squash>` is
empty for all three.

| branch | squash commit on roadmap/v2.2 |
|---|---|
| `fix/screenshot-off-hyprland` (2) | `4e267eb` fix: screenshots and popup input regions off Hyprland (#5) |
| `fix/labwc-desktop-parity` (10) | `f609a6c` fix(labwc): complete desktop parity (#6) |
| `fix/popup-first-open-and-media-keys` (5) | `b8a63a8` fix(popups): first-open rendering, dead toasts, and media keybinds (#7) |

`git log origin/roadmap/v2.2..origin/<branch>` lists commits only because the
branch pointers were never deleted after the squash. Do NOT rebase or port them.

## SURVIVAL AUDIT — done. All 17 survive on d2d1f33.

Method: `git log <squash>..origin/roadmap/v2.2 -- <files>`, then read the code.

- Untouched since landing, so intact by construction: `src/scripts/screenshot.sh`
  (4aea321), `src/components/LazyPopup.qml` + the four popups (4170f5c),
  `src/popups/VPNTab.qml` (f75f322, 4ba1d51), `tests/service-tier-test.qml`.
- Touched later, verified intact by reading: ArchMenu/AudioPopup/QuickControl
  masks (f3a28a6), TopBar mask + exclusion floor (2563d25), AppDock (91b7425),
  PopupDismiss labwc exception (6fd0fb3), Network.qml BlueZ + VPN order
  (c156004), IpcManager caffeine handler, ShellState `_vpnProbeGeneration`
  (9b021e0) and the `--pdeathsig` pair (f78dc62), Media group (3fec251).
- Two August CI guards deliberately superseded, both documented in place:
  - the `systemd-inhibit ... isLabwc` guard from 8cf779f moved to
    `tests/check-idle-inhibit.sh` after `6f2e55d` proved August had it backwards
    (Caffeine did nothing at all on Hyprland).
  - the `bindel` grep from f3748ee became `locked = true, repeating = true`
    after `e032598` migrated the Hyprland artifact to Lua.

## STATUS

- [x] Worktree created
- [x] Reframe proven
- [x] Per-commit survival audit (17 rows)
- [ ] P1-038 headless matrix

## NEXT

Build the labwc matrix. Model it on `tests/run-nav-geometry-test.sh` (headless
wlroots labwc, private `XDG_RUNTIME_DIR`/`HOME`/`XDG_STATE_HOME`, PATH stubs,
skips clean without a compositor). Never `qs -p`. Automatable slice: popup mask
geometry measured under labwc, LazyPopup first-open, TopBar input mask, AppDock
capacity, PopupDismiss unmapping, screenshot dispatch by compositor. The
application half of §21 (OBS/Steam/Wine/VRR/suspend) is physical and stays a
documented remainder.
