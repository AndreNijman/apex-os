# P1-038 — Floating/labwc validation matrix + the three "unmerged" August branches

Worktree: `/var/tmp/apex-work/wt-p1-038`, branch `task/p1-038-labwc-parity`,
based on `origin/roadmap/v2.2` @ `d2d1f33`. Pushed. Do not rebase; Andre
integrates.

## THE REFRAME — read this before touching anything

**The three branches are not unmerged. All 17 commits landed byte-for-byte in
August as squash-merges.** Proof: `git diff origin/fix/<branch> <squash>` is
empty for all three.

| branch | squash commit on roadmap/v2.2 |
|---|---|
| `fix/screenshot-off-hyprland` (2) | `4e267eb` fix: screenshots and popup input regions off Hyprland (#5) |
| `fix/labwc-desktop-parity` (10) | `f609a6c` fix(labwc): complete desktop parity (#6) |
| `fix/popup-first-open-and-media-keys` (5) | `b8a63a8` fix(popups): first-open rendering, dead toasts, and media keybinds (#7) |

`git log origin/roadmap/v2.2..origin/<branch>` lists commits only because the
branch pointers were never deleted after the squash. Nothing to port. The three
branch refs can be deleted.

## SURVIVAL AUDIT — done. All 17 survive on d2d1f33.

Method: `git log <squash>..origin/roadmap/v2.2 -- <files>` per commit, then read
the code where anything touched it, then run the behavioural suites.

- Untouched since landing: `src/scripts/screenshot.sh` (4aea321),
  `src/components/LazyPopup.qml` + four popups (4170f5c), `src/popups/VPNTab.qml`
  (f75f322, 4ba1d51), `tests/service-tier-test.qml`.
- Touched later, verified intact: ArchMenu/AudioPopup/QuickControl masks
  (f3a28a6), TopBar mask + exclusion floor (2563d25), AppDock (91b7425),
  PopupDismiss labwc exception (6fd0fb3), Network.qml BlueZ + VPN order
  (c156004), IpcManager caffeine handler, ShellState `_vpnProbeGeneration`
  (9b021e0) and the `--pdeathsig` pair (f78dc62), Media group (3fec251).
- Two August CI guards deliberately superseded, both documented in place:
  - 8cf779f's `systemd-inhibit ... isLabwc` guard moved to
    `tests/check-idle-inhibit.sh` after `6f2e55d` measured that August had it
    backwards — Caffeine did nothing at all on Hyprland.
  - f3748ee's `bindel` grep became `locked = true, repeating = true` after
    `e032598` migrated the Hyprland artifact to Lua.

## WHAT LANDED ON THIS BRANCH

| commit | what |
|---|---|
| `d2523e2` | `tests/check-screenshot-dispatch.sh` — 42 assertions, 4 self-test mutants. No compositor needed. |
| `cfad23f` | `tests/run-labwc-matrix-test.sh` + `tests/labwc-matrix-test.qml` — headless labwc, private HOME. |
| `2ea0790` | Two headless outputs; per-output bar and dismiss surface. |

Suite totals today: labwc matrix 60/0, screenshot dispatch 42/0, service tier
88/0, color tokens 22/0 (`EXPECT_WHITE_FG` untouched at 211),
compositor-backends 36/0, idle-inhibit 17/0, scale-tokens 5/0, keybind-lua 16/0.

## P1-038 STATUS: partial

§21's 25 rows, and where each one stands.

**Automated and green (4):**

| row | covered by |
|---|---|
| screenshots | `tests/check-screenshot-dispatch.sh` — grimblast on Hyprland, grim elsewhere, region/cancel/active, clipboard detachment |
| multi-monitor | `tests/run-labwc-matrix-test.sh`, two headless outputs: per-output bar width, per-output dismiss surface, dashboard scoping |
| lock/idle | `tests/check-idle-inhibit.sh` (17/0) — the logind lock is held on Caffeine alone |
| scaling | `tests/run-scaling-test.sh`, `tests/run-nav-geometry-test.sh` (`NAV_GEOMETRY_COMP=labwc`) |

**Read but not run (2)** — the Hyprland-only dependency that killed screenshots
is absent here, which is not the same as the row passing:

| row | what was checked |
|---|---|
| recording | `ScreenRecService` reaches the compositor only through `CompositorService.windowBoxScript`, with a plain-`slurp` fallback, since `30d1801`. `wf-recorder` is wlr-screencopy. No Hyprland-only path remains — but nothing records under labwc in CI. |
| GTK / Qt | the dock resolves `DesktopEntries` against a real wlr-foreign-toplevel, and the suite is itself a Qt client under labwc. Neither is application validation. |

**Remainder — needs real hardware and real applications (18, plus the two read
rather than run above).** None can be faked headlessly; each needs a labwc
session on the L16 or katana:

1. Firefox screen sharing — xdg-desktop-portal-wlr picker, then share a window
   and an output; confirm the shared surface is the one chosen.
2. Chromium screen sharing — same, plus tab capture.
3. OBS — window capture, output capture, and that the APEX bar is excludable.
4. Discord — Go Live on a window and on an output.
5. Flatpak portals — file chooser, screenshot portal, and openURI from a
   sandboxed app.
6. Steam — big-picture and desktop mode, plus overlay input reaching the game.
7. gamescope — nested and embedded, with the bar's exclusive zone respected.
8. Wine/XWayland — an X11 client's titlebar buttons must not sit under the bar
   mask; this is the row the `exclusiveZone >= notchHeight` assertion exists for
   and only a real XWayland client proves it.
9. VS Code — Electron/Ozone under Wayland and under XWayland.
10. JetBrains — JBR Wayland and X11 fallback.
11. LibreOffice — gtk3 and qt6 VCL plugins.
12. Blender — its own decorations and its fullscreen path.
13. fractional scaling — 1.25 / 1.5 on real outputs, mixed with 1.0.
14. rotation — 90/180/270, and the bar mask following.
15. refresh-rate switching — the bar surviving a mode change without a restart.
16. VRR — enable on labwc and confirm no bar-driven repaint storm.
17. suspend/resume — the bar, the dock and the logind inhibitor after a cycle.
18. dock/hotplug — plug and unplug an output; per-output windows created and
    destroyed, and the dashboard's output going away while it is open.

Rows 1–5 and 8–12 are the ones most likely to be broken today: nothing in the
repo has ever exercised a portal or an XWayland client under labwc.

## NEXT

Run rows 1–18 in a real labwc session on the L16 and record the result against
this list. Nothing further is automatable without those applications present.
