# p1-025 — Plugin provenance and isolation, connector trust planes

Tasks: **P1-026** (`partial`), **P1-028** (`partial`). P1-025 and P1-027 in this
unit are already `done` — check their evidence before touching their code.

Repo: apex-os. Branch `task/p1-025-plugin-trust`, from `origin/roadmap/v2.2`.
Worktree **`/var/tmp/apex-work/wt-p1-025-trust`** — NOT `wt-p1-025`, which is
still checked out to the previous unit's landed branch `task/p1-025-skills-plugins`
and was left alone rather than clobbered.

## Why this is dispatchable now and was not before

`queue.json` says `after: p1-018`, and p1-018 was complete-but-unlanded for two
rounds. It is landed now: `roadmap/v2.2` carries it and the unlanded set is
empty. That was the only block.

## Criteria, quoted from roadmap.yaml

- P1-026: marketplace origin and commit/hash tracked; executable plugin content
  runs inside a sandbox.
- P1-028: security UI distinguishes local vs remote connectors; hardened
  profiles can reduce cloud-side tools/connectors.

## Traps specific to this unit

- "Runs inside a sandbox" is the criterion that is easiest to assert vacuously.
  The proof is a plugin that tries to reach something outside the sandbox and is
  *observed* to fail — not a grep for a bwrap argument. bubblewrap is installed
  on this machine and in both CI workflows, and
  `kernel.apparmor_restrict_unprivileged_userns=0` is set in CI with a smoke
  check that proves the sandbox actually starts.
- P1-028's UI half lives in apex-shell. If you need a shell change, make it on
  `task/p1-025-plugin-trust-shell` in the apex-shell worktree and say so on the
  card; do not edit apex-shell from the apex-os worktree.

## MEASURED, not remembered — claude 2.1.260 on this machine

Probed headlessly with `ANTHROPIC_BASE_URL=http://127.0.0.1:1` so no request left
the machine, and a sentinel script per server that appends its name to a log when
it is started. Fixture HOME under the session scratchpad; nothing real touched.

1. With no flag, **all four** surfaces start a server: `~/.claude.json`
   `mcpServers`, `~/.claude.json` `projects.<dir>.mcpServers`, the working
   directory's own `.mcp.json`, and an enabled plugin's `.mcp.json`.
2. With `--strict-mcp-config --mcp-config <file>`, **only the file's servers
   start** — the plugin-defined one is dropped along with the rest. That is the
   mechanism P1-026's second criterion needs and `sidecar.rs` already named.
3. A server key containing `:` (`plugin:fakeplug:plugsrv`) is accepted verbatim
   in a `--mcp-config` file, so a curated document can keep the agent's existing
   spelling — and therefore the `mcp__<server>__<tool>` names any permission rule
   already refers to.
4. A plugin's live `.mcp.json` is the copy under `installed_plugins.json`'s
   `installPath` (`plugins/cache/<mp>/<plugin>/<version>`), not the marketplace
   checkout. `servers::enabled_plugin_configs` already reads the right one; a
   fixture that points installPath at the checkout measures a stale file.
5. Repo `.mcp.json` servers are approved per directory
   (`enabledMcpjsonServers` / `disabledMcpjsonServers` in the project entry), and
   `-p` skips the trust dialog. A curated document that listed an unapproved one
   would be APEX approving it on the user's behalf.

## NEXT

Implementing now, on `task/p1-025-plugin-trust` in `/var/tmp/apex-work/wt-p1-025-trust`.

- `apex-agent-core/src/mcpconf.rs`: `Origin`, `Definition`, `read(home, cwd)`
  (the four surfaces + the per-directory approval lists), `wrap`/`unwrap` for the
  `apex mcp run <name> -- …` argv (canonical here; `servers::confined` delegates),
  `ConnectorPolicy` (All / LocalOnly / Only(names) / None), and a pure
  `curate(defs, policy, apex) -> Curated { document, decisions }`.
- Wrapping rule: a kept stdio server is wrapped when its origin is third-party
  (plugin or repository) — the same split `Surface::is_writable_here` draws from
  the other side. The user's own `~/.claude.json` definitions are NOT silently
  wrapped; `apex mcp confine` is the user's verb for those.
- An unplaceable transport is passed through verbatim under `All` and dropped
  under any reducing policy: it cannot be shown to be on the plane that was kept.
- `AgentPolicy.connectors` as dimension 7, `#[serde(default)]`, `--connectors`.
- Daemon: `install_mcp_config` in `apex-agentd/src/session.rs`, same shape as
  `install_hook_settings`; file in the scratch, pushed onto `spec.ro`.
- Proof: `tests/test-apex-mcp-launch.sh` runs the real wrapped argv and observes
  a plugin server failing to read a sentinel in the real HOME and failing to
  reach a listener that is definitely there; the same script unwrapped succeeds.

Baselines on the fork point `cafd3635`: cargo `--workspace` **2821 passed / 0
failed**; `tests/test-apex-skill.sh --with-binary` **73 passed / 0 failed**.
