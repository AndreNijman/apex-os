# p1-025
items: P1-025, P1-026, P1-027, P1-028
repo: apex-os
worktree: /var/tmp/apex-work/wt-p1-025
branch: task/p1-025-skills-plugins

## NEXT
round 10. LANDING BLOCKER IS GONE: merged origin/roadmap/v2.2 (83e3ef6, one
trivial main.rs mod-list conflict), P1-026 committed (c636b9a), **tests/run-clippy.sh
says PASS clippy is clean**, 520 unit tests pass, both pushed. Remaining: (1) real
`apex provenance show` + `apex skill list` run against this machine for the card's
counts — read-only, do NOT run `provenance record` against the real home; (2) write
tests/test-apex-skill.sh modelled on tests/test-apex-trust.sh, with HOME *and*
XDG_STATE_HOME pointed at a fixture; (3) tests/check-doc-verbs.sh for the 6 new
verbs (needs a freshly built apex — use target/debug/apex, the installed one is
older); (4) mutation proofs incl. the 3 the advisor named (revision_of dirty:None
-> Some(false); read_registry EACCES arm -> None; revision_of dir-refused arm);
(5) full suite.

## DONE
- worktree /var/tmp/apex-work/wt-p1-025 created from origin/roadmap/v2.2 @ e67fab9,
  branch task/p1-025-skills-plugins pushed to origin (upstream set).
- round 2: re-measured the four items from scratch (predecessor's findings were
  lost). Exists-vs-adding below is from real reads, cited by file:line.

## FOUND — the card's own correction
- **`apexd/apex/src/plugin.rs` DOES NOT EXIST**, at e67fab9 or at the tip 583355e.
  The previous NEXT named it; it was a guess. The plugin CLI is bash:
  `files/system/libexec/apex-plugin` (480 lines), with `tests/test-apex-plugin.sh`
  (704 lines) and `files/system/libexec/apex-plugin-verdict.js` (194 lines).
- **`apex-plugin` is about apex-shell QML plugins, NOT Claude Code plugins.**
  Its subject is `~/.config/apex-shell/plugins/<id>/{plugin.json,*.qml}`
  (apex-plugin:86-87). Four verbs only: list, info, enable, disable
  (apex-plugin:470-477). No marketplace, no origin, no commit, no hash anywhere
  in it.
- **`verify.rs` is tip-only.** 2198 lines at 583355e, absent at e67fab9. This
  branch must compile at e67fab9, so its `Verdict` cannot be imported — see the
  parallel-enum note under P1-026.

## FOUND — exists vs adding, per item

### P1-025 — skill inventory/sync/audit
EXISTS:
- `apexd/apex-agent-core/src/profile.rs` knows the skills directory as a *mount
  and export* concern, not an inventory: `Role::Skills` (profile.rs:168-169),
  and the `CLAUDE` profile's entry `path: "skills", kind: Dir, base: Root,
  class: Reusable, mount: ReadOnly` (profile.rs:345-353). `plan_export`
  (profile.rs:907) walks it for bundling.
- Live ground truth: `~/.claude/skills/` holds **30 skill directories**
  (measured by `ls`, read-only).
ADDING (nothing today lists, hashes, types or audits a skill):
- no origin, no hash, no type metadata on any skill;
- no executable-vs-reference distinction anywhere;
- no inventory, sync or audit verb.

### P1-026 — plugin/marketplace provenance and isolation
EXISTS:
- **Real marketplace state, and it is Claude Code's, not apex-shell's.**
  `~/.claude/plugins/known_marketplaces.json` — 3 marketplaces, each
  `{source:{source:"github",repo:"<owner>/<repo>"}, installLocation, lastUpdated}`.
  `~/.claude/plugins/installed_plugins.json` — `version: 2`, `plugins` keyed
  `<name>@<marketplace>`, each an array of `{scope, installPath, version,
  installedAt, lastUpdated}`.
  **DECISION: P1-026's "marketplace" is the Claude Code plugin registry.**
  Evidence: apex-shell QML plugins have no marketplace concept at all, and the
  MCP layer already reads this registry (below). The run's own rule — never
  install from a real marketplace — is about this registry too.
- `apexd/apex/src/mcp/servers.rs:379 enabled_plugin_configs(home)` already
  reads `~/.claude/settings.json`'s `enabledPlugins` and
  `~/.claude/plugins/installed_plugins.json`, and resolves each `installPath`
  to a `.mcp.json`. `Surface::Plugin { plugin, file }` (servers.rs:54) is the
  resulting provenance-ish label.
- **The only real sandbox in the repo**: `apex mcp run <name> -- <cmd>`,
  `mcp/sidecar.rs` → `apex_agent_core::sandbox::build_argv` (bubblewrap).
  `McpPolicy` (sidecar.rs:97-113) is default-deny on network/project/broker,
  private per-server HOME. `mcp/confine.rs` rewrites a definition to wrap it.
  `servers::confined_server` (servers.rs:645) detects an already-wrapped one.
ADDING:
- no version/commit/hash is ever re-checked against the bytes on disk. The
  registry records `version` (and for 2 of the plugins the recorded version is
  the literal string `"unknown"`), never a content hash.
- **Parallel enum, declared**: this branch needs verify.rs's 4-value shape
  (verify.rs:136 `Verdict`, :336 `arm`, :363 `decide` at the tip) but cannot
  import it at e67fab9. A local enum is defined instead; the orchestrator
  should unify the two when both are on one tip.
- **HONESTY — criterion 2 is partial by architecture, not by effort.**
  "Executable plugin content runs inside a sandbox" cannot be delivered whole:
  * apex-shell QML plugins run **in-process in the shell**, and the shipped
    script already says so — "neither one confines hostile code. Install
    plugins you would run as yourself." (apex-plugin:327-329, repeated :463-464).
    No sandbox is possible without an apex-shell change, which this unit cannot
    grant itself.
  * a Claude Code plugin's MCP server IS confinable, and only iff it has been
    wrapped by `apex mcp confine` — which `confined_server` can measure.
  * plugin hooks and skill scripts: nothing in apexd runs them, so nothing here
    confines them. Reported as unconfined rather than left blank.
  So what ships is a per-component confinement *status*, with the word
  "sandbox" spent only where bubblewrap actually runs.

### P1-027 — memory provider interface
CONSTRAINT (given, not re-derived): Andre's memory is an Obsidian vault on a
Synology NAS, NFS-mounted on an Orange Pi, exposed as an MCP server. It is
authoritative and stays authoritative. No migration, no APEX-owned store, and
**this branch never writes to it or its MCP server.**
EXISTS:
- `servers::discover` (servers.rs:313) already finds it and classifies it:
  `Surface` (whose file defines it), `Transport::Endpoint{kind,url}` vs
  `Stdio{command,args}`, and `Credential` (servers.rs:113-147) — `Brokered`,
  `InConfig`, `FromEnvironment`, `None`, with `agent_readable()`.
- `mcp/sidecar.rs`'s own header uses an MCP *memory* server as its worked
  example (sidecar.rs:1-17), so the memory case is already the module's
  motivating one.
ADDING:
- no "provider" notion, no health probe, no project→memory mapping surface.
- read-only status over the definition `discover` already returns, plus a
  health probe with four verdicts (answered / refused / unreachable /
  could-not-probe). Zero writes.

### P1-028 — separate local and cloud-side connector trust planes
EXISTS:
- **The local/remote split is already in the data**: `Transport::Stdio` (a
  program this machine runs) vs `Transport::Endpoint{kind,url}` (an address the
  agent posts to) — servers.rs:81-88. `mcp.rs:340 describe_transport` prints
  both, and `mcp.rs:191 list` / `servers.rs:546 as_json` are the security
  surface. Neither one *names* a plane.
- `apex mcp list` already counts agent-readable credentials (mcp.rs:227-229).
- **The hardened-profile hook is `PolicyPreset`**, `apexd/apex-agent-core/src/policy.rs:788`
  — five named presets (`default`, `agent-bypass`, `unrestricted`,
  `system-access`, `unsafe-everything`, policy.rs:818-836) over six dimensions
  (`AgentPolicy`, policy.rs:535). Note: every preset only ever *widens* from
  `Default`; there is no preset tighter than default, and none of the six
  dimensions is about connectors.
- NOT a profile: `apexd/apex/src/mode.rs` is power/performance modes. The only
  `hardened` hits in the repo are `apex-secretd/src/broker.rs:264
  hardening_flags()`, unrelated (systemd exec flags).
ADDING:
- criterion 1 is small: name the plane in the report and the JSON.
- **HONESTY — criterion 2**: no dimension of `AgentPolicy` gates connectors, so
  nothing in the launch path can currently *enforce* a reduced cloud-side
  connector set. What can be delivered honestly is the measurement: which
  connectors a named profile would remove, reported as such, with the missing
  enforcement hook named. If it is shipped as an enforcing flag it would be a
  label.

## LANDED ON THE BRANCH (real runs, real counts)

### 31e5860 — P1-025
- `apexd/apex/src/digest.rs` (new): `Digest::{Sha256,Unmeasured}`, `walk`,
  `digest`/`digest_with`. Hashes by driving `sha256sum` (trust.rs/verify.rs
  pattern) so "could not measure" is a type-carried state.
  `Digest::same_as -> Option<bool>`: an unmeasurable pair cannot be read as a
  mismatch. Manifest = per-file sha256 + execute bit + relative path, so a
  `chmod +x` changes the digest. A tree with ANY unreadable part gets NO digest.
  6 unit tests.
- `apexd/apex/src/skill.rs` (new): `apex skill list|audit [--json]`.
  Per skill: `Origin::{User,Project,Plugin{plugin,marketplace}}`,
  `Manifest::{Present,Absent,Unreadable}`, `Kind::{Executable{scripts},
  Reference,Undetermined}`, digest, file count, symlinks, denied list.
  8 unit tests.
- **Real run on this machine**: 34 skills, 4 executable (code-reviewer 3
  scripts, resume-guard 6, stop-slop 1, turnstile-spin 4), 30 reference.
  `apex agent profile doctor` also says 34 — agreement asserted, not assumed.
- **DEFECT FOUND (the fifteenth of that shape), not fixed here**:
  `apexd/apex-agent-core/src/profile.rs:1789` is
  `if let Ok(it) = std::fs::read_dir(&skills_dir)` and iterates `it.flatten()`,
  and checks the manifest with `.exists()` (:1795). All three discard a
  refusal, so **a skills directory that exists and cannot be read reports
  "0 skills"** — in the one place it does most damage, because "0 skills" is
  what somebody would rely on to say nothing unexpected is installed. Left for
  the orchestrator to route; `skill.rs` refuses to say it (unreadable root ⇒
  INCOMPLETE, prints "no skills were READ", exits non-zero).

### 5917833 — P1-027 + P1-028
- `apexd/apex/src/connector.rs` (new), 10 unit tests.
- P1-028 c1: `Plane::{Local,Cloud,Unknown}` off `Transport`. `plane` line added
  to `apex mcp list`, `plane` field added to `servers::as_json`, new
  `apex mcp planes [--json]`. **Real run**: 1 local (`memory`), 2 cloud
  (`claude-memory`, `plugin:github:github`).
- P1-028 c2 — **real, not a label, and narrower than the criterion's wording**:
  the five presets cannot reduce anything (all widen `default`; no
  `AgentPolicy` dimension names a tool or connector). But
  `AgentPolicy::effective_network` (policy.rs:564-569) maps
  `SandboxPolicy::Strict -> NetworkPolicy::Offline` unconditionally, so
  `--sandbox strict` removes every cloud connector while leaving the local
  program runnable. Table built from `PolicyPreset::ALL` + `SandboxPolicy::ALL`,
  not hand-written, so an upstream retightening shows up here. Reported as
  all-or-nothing: **there is no per-server switch anywhere in APEX**, and
  `--strict-mcp-config` is an unbuilt remainder named in sidecar.rs. Two tests
  assert those sentences stay in the output.
- P1-027: `apex mcp memory [--json] [--probe]`. A view, never a store —
  `apexOwnedStore: false`, `migrationRequired: false` in the JSON so the
  constraint is checkable. Provider identity is **declared** in
  `~/.config/apex/memory.toml`, never guessed; with no declaration a
  name-shaped guess is labelled `GUESSED` everywhere.
  `Health::{Answered,Refused,Unreachable,NotProbed}` — `NotProbed` serialises
  `healthy: null`. Probing is opt-in, read-only (`initialize` only), and
  **never grants a capability in order to report on one**.
- **Real run**: this machine has two candidates and no declaration, so the
  refuse-to-guess arm is exercised live — nothing marked authoritative, the
  ambiguity reported. **The live claude-memory MCP server was NOT probed**
  (`--probe` never run against it); the probe path is proved against fixtures.

## IN PROGRESS
- P1-026 provenance.rs.

## BLOCKED ON
- nothing
