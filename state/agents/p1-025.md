# p1-025 — Plugin provenance and isolation, connector trust planes

Tasks: **P1-026** (`partial`), **P1-028** (`partial`). P1-025 and P1-027 in this
unit are already `done` — check their evidence before touching their code.

Repo: apex-os. Branch `task/p1-025-plugin-trust`, from `origin/roadmap/v2.2`.
Worktree **`/var/tmp/apex-work/wt-p1-025-trust`** — NOT `wt-p1-025`, which is
still checked out to the previous unit's landed branch `task/p1-025-skills-plugins`
and was left alone rather than clobbered.

## Why this is dispatchable now and was not before

`queue.json` says `after: p1-018`, and p1-018 was complete-but-unlanded for two
rounds. It is landed now: `roadmap/v2.2` carries it and the unlanded set is
empty. That was the only block.

## Criteria, quoted from roadmap.yaml

- P1-026: marketplace origin and commit/hash tracked; executable plugin content
  runs inside a sandbox.
- P1-028: security UI distinguishes local vs remote connectors; hardened
  profiles can reduce cloud-side tools/connectors.

## Traps specific to this unit

- "Runs inside a sandbox" is the criterion that is easiest to assert vacuously.
  The proof is a plugin that tries to reach something outside the sandbox and is
  *observed* to fail — not a grep for a bwrap argument. bubblewrap is installed
  on this machine and in both CI workflows, and
  `kernel.apparmor_restrict_unprivileged_userns=0` is set in CI with a smoke
  check that proves the sandbox actually starts.
- P1-028's UI half lives in apex-shell. If you need a shell change, make it on
  `task/p1-025-plugin-trust-shell` in the apex-shell worktree and say so on the
  card; do not edit apex-shell from the apex-os worktree.

## NEXT

Plan settled (advisor-reviewed). One mechanism closes both remainders: the
`--strict-mcp-config --mcp-config <file the runtime wrote>` path that
`sidecar.rs` and `connector.rs` both already name as "the remainder, not built".

1. Read `claude --help` on this machine for the real flag spellings; do NOT
   design around remembered flags. Settle whether strict mode also drops
   plugin-provided servers.
2. Find the launch argv site and copy P0-011's `--settings` precedent (daemon
   writes a file, binds it read-only back through the home mask).
3. Writer is a PURE function `(servers, policy) -> Value` in agent-core:
   every Stdio server gets `apex mcp run <name> -- <orig>`, every Endpoint is
   kept or dropped by the connector policy, `Other` is dropped and said so.
   Never rewrite a plugin's own `.mcp.json` — the provenance digest landed last
   round would flag every plugin MODIFIED.
4. Connector policy as a `#[serde(default)]` policy input (AgentPolicy crosses
   the wire). `--sandbox strict` must still remove the whole plane: existing
   floor in `policy_invariants.rs`.
5. Only then fix the honesty text: `connector.rs` `perConnectorSwitch:false`,
   the "Nothing in APEX can disable one cloud connector" paragraph,
   `docs/agent-runtime.md:1709`, `sidecar.rs`'s remainder paragraph, and
   provenance's `Confinement` lines (third state: confined at launch by the
   curated config, though the raw definition is unconfined).
6. The sandbox proof: a fake plugin whose stdio server tries to read a sentinel
   in the real `$HOME` and open a socket; launched through the curated config it
   must FAIL, unwrapped it must SUCCEED (the inverse). bwrap unable to start =>
   could-not-run, non-zero, never a pass.

Leave partial honestly: QML plugins run in-process in apex-shell and are
architecturally unconfinable.
