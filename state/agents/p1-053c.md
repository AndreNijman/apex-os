# p1-053c — Android voice, screenshot, file and clipboard handoff; production quality

items: P1-059, P1-060
repo: apex-os (android/ lives in-tree; there is no apex-android repository)
branch: task/p1-053c-android-handoff  (fresh, off roadmap/v2.2 @ 69a2f51e)
worktree: /var/tmp/apex-work/wt-p1-053c

## Where this started

Predecessor p1-053b landed as merge `69a2f51e`: 383 `:core` + 27 `:app` JVM
tests, the terminal, Agent Center, worktrees, approvals, alerts, and
`android/core/src/test/resources/requests.json` + `android_requests_wire.rs`,
which settle "which verbs exist" with a compiler instead of an argument.

## The wire vocabulary, read off `protocol.rs` rather than remembered

28 verbs: hello, run, list, info, attach, resize, inject, input, signal, event,
telemetry, tool_check, worktrees, logs, remove, prune, declare_origin,
privilege_request, requests, decide, request_executed, grants, revoke,
system_grants, revoke_system_grant, renew_system_grant, secret_use,
elevation_challenge.

**There is no file-transfer verb and no clipboard verb.** `apex send
--clipboard` (`apexd/apex/src/dispatch.rs:548`) is real, and it is ssh +
`wl-paste`/`wl-copy` between two Linux hosts in the §20 host registry. An
Android phone is not an ssh destination running `wl-copy`, so that mechanism is
unreachable from here — it is a different feature wearing a similar name.

## FINDING: `inject` is reachable from a paired phone and is not origin-gated

`Request::Inject { id, source }` reads `source` — **an absolute path on the
host** — with the daemon's own access, outside every sandbox. That is the point
of the verb for a human at the keyboard: `~/Pictures/Screenshots` is masked by
the tmpfs over `$HOME` and the daemon can see it.

`apexd/apex-agentd/src/inject.rs:48` refuses exactly one caller: one that
resolves to a managed session. It never consults the origin.
`apex-remoted`'s `control()` (`serve.rs:473`) is a **denylist of one verb**
(`attach`). `apex-remoted` is not a managed session.

So a paired device can send `{"cmd":"inject","id":N,"source":"/home/<user>/.ssh/id_ed25519"}`
and the daemon will read that file, copy it into the session's inbox where the
agent can read it, and type the path into the agent's terminal. The module's own
threat model names this exact attack for a *session* ("`apex agent send 4
~/.ssh/id_ed25519` ... would be a complete escape") and gates it there; the
remote caller was not considered, because P1-035 landed before there was a
phone.

Nothing in `android/` builds an `inject` request today, so this is a hole in the
daemon rather than a defect in the app — but P1-059's fourth criterion is about
not reaching past what was explicitly granted, and this is that criterion
inverted.

## Criterion 2 needs a transport that does not exist, and the arithmetic says so

"Photo/screenshot/file upload lands in an explicit permitted project/temp path
and inserts a safe path into the agent session" is exactly what
`apex_agent_core::inject` does — `safe_name`, `destination`, `payload`,
`keystrokes`, the INBOX under the session scratch, nothing submitted. What is
missing is getting the bytes from the phone to the machine.

`apex-remote-core`'s `MAX_PAYLOAD` is 65514 bytes (`wire.rs:67`). A control
frame carries one line of agentd JSON; base64 costs 4/3; so a single-frame
handoff verb tops out near 48 KB of file. A screenshot is 100 KB to 2 MB. A
verb that carries a log tail and refuses the thing the criterion is named after
is the same mistake as building a request nothing can parse.

Doing it properly means chunked transfer — begin/chunk/end, staging state and a
size cap in the daemon, a timeout, cleanup of an abandoned upload, and the
origin gate above applied to the destination. That is a protocol unit, not an
Android round, and it is recorded as one.

## Plan for this round

1. Origin-gate `inject`, mirroring `decide`'s predicate; pin it upstream.
2. P1-059 what is reachable: push-to-talk and clipboard-send through
   `Input { id, data }` with an explicit target; the photo-picker rule and a
   manifest guard for criterion 4.
3. P1-060 what closes headless: in-app help with a parity checker against
   apex-shell's `AgentHelpContent.qml`; consent-based crash reporting with a
   redactor; a signed release APK/AAB pipeline.
4. Say plainly what no round on this hardware can verify.

## NEXT

Emulator attempt 3 is running:
`/var/tmp/apex-work/scratch/p1-053c/emu-boot3.sh`, log `emu-boot3.log`
(swiftshader + `-feature -Vulkan,-GLDirectMem`, host Vulkan layers disabled).
Attempts 1 and 2 both SIGSEGV'd in `qemu-system-x86_64-headless` about 40
seconds in, after `adb get-state` already answered `device`. If attempt 3 dies
too, stop trying, write the three commands and their exact failures into the
P1-060 evidence as a could-not-run, and spend the rest of the round on the
chunked file transport for P1-059 C2 (`protocol.rs` version handling,
`requests.json` + `android_requests_wire.rs` to pin both sides, destination
gated with `decide`'s predicate).

## DONE

- `c23217b9` **pushed**: `AccessibilityStaticsTest` (four rules, each mutated
  red alone and by name, sources restored byte-identical) plus the defect it
  found — three `Modifier.width(110.dp)` label columns that clip at any font
  scale above 1.0, now `labelColumnWidth()` in `ui/theme/Theme.kt`. The
  predecessor's untracked file is no longer dangling. Counts: 434 `:core`,
  37 `:app`, green; `check-help-prose.sh` still TOTAL 0.
- Round 1 landed on `roadmap/v2.2` as merge `551e9e1a`, seven commits:
  `02e133c0` origin-gate on `inject`; `b6c4ef33` multi-line COPY test fix;
  `1cafad93` push-to-talk + clipboard handoff; `b05f0f46` + `5dd69d6a`
  shellcheck discovery; `5f3a815d` review where the person is; `c61517b0`
  in-app guide + desktop parity; `8576b14e` signed release pipeline;
  `a79b89ae` crash reports with no SDK.
- P1-059 recorded `partial`: C1, C4 met; C3 half (send done, receive has no
  verb); C2 not met, refused with arithmetic.
- P1-060 recorded `partial`: C3, C4, C5 met; C1 partial (no device);
  C2 not met.

## IN PROGRESS

- Emulator spike, see NEXT and BLOCKED ON.
- **A HEADLESS EMULATOR IS RUNNING AND MUST BE KILLED.**
  `emulator` 37.1.11 + `system-images;android-36;aosp_atd;x86_64` installed
  (INSTALL_EXIT=0, SDK now 3.3 GB). `emu-boot.sh` created AVD `apex-p1053c`
  and launched it with `-no-window` and `DISPLAY`/`WAYLAND_DISPLAY` unset.
  **Kill it with**
  `ANDROID_SDK_ROOT=/var/tmp/android-sdk /var/tmp/android-sdk/platform-tools/adb -s emulator-5554 emu kill`
  or `kill $(cat /var/tmp/apex-work/scratch/p1-053c/emu.pid)`. A headless 2 GB
  VM left running burns this laptop's battery with nobody watching, so if you
  are a stranger reading this card, kill it first and ask afterwards.

## FOUND

- **P1-060's "no emulator" was "not obtained", not "impossible".** There IS an
  Android SDK on this machine at `/var/tmp/android-sdk` (cmdline-tools,
  platform-tools, build-tools 35+36, platforms/android-36, installed by
  `setup.sh` on 2026-09-12 — that is how round 1 built without a
  `local.properties`). `/dev/kvm` exists and is `crw-rw-rw-`, `/var` has 754 GB
  free, 20 GB RAM available, and `dl.google.com` answers. So `sdkmanager
  --install emulator "system-images;android-36;aosp_atd;x86_64"` is reachable
  and round 1's flat "there is no emulator" was a claim about what was
  installed, read as a claim about what was possible.
- (round 1's other findings are in the roadmap evidence for P1-059/P1-060.)

## BLOCKED ON

- Nothing.
