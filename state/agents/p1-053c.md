# p1-053c — Android voice, screenshot, file and clipboard handoff; production quality

items: P1-059, P1-060
repo: apex-os (android/ lives in-tree; there is no apex-android repository)
branch: task/p1-053c-clipboard  (off roadmap/v2.2 @ 13d53c01)
worktree: /var/tmp/apex-work/wt-p1-053c

## PROTOCOL_VERSION IS NOT BUMPED — it stays **8**. Round 6 confirms round 5.

**For `p2-d`, which is holding a `PROTOCOL_VERSION` bump for its Route-B
capsule auth:** round 6 did not touch `protocol.rs` AT ALL. Checked, not
assumed — `git diff --stat 491a3fef..b341e433 --
apexd/apex-agent-core/src/protocol.rs` is **empty**, and
`protocol.rs:112` on the branch tip still reads `pub const PROTOCOL_VERSION:
u32 = 8;`. p2-d is free to take 8 → 9 on its own, and this unit has no
further work in that file. The paragraph below is round 5's reasoning, kept.

Said in as many words because `p2-d` was told to hold `protocol.rs` for this
unit and needs to know what to rebase onto. This round adds
`Request::Clipboard` (no fields) and `Response::Clipboard { text }` to
`apexd/apex-agent-core/src/protocol.rs` and changes nothing else in that file.
A daemon older than the verb answers `unparseable request`; the failure loses a
feature and never a restriction, which is the criterion `Request::Event`
records for not bumping. So p2-d's capsule-credential change is free to bump 8
to 9 on its own.

## NEXT

**P1-059 IS FINISHED — ask the orchestrator to land `task/p1-053c-clipboard`
@ `b341e433` and set P1-059 `partial` → `done`** (all four criteria; C3 was
the only one open and both its halves are now in). Then the only work left on
this unit is **P1-060 C1 and C2**, which need a device or a working emulator
and have neither — see the four-crash SIGSEGV measurement in FOUND (rounds
1-3). If a device appears, start with C2's headless-testable parts
(backgrounding, battery saver, poor-network — `LowBandwidthTest` is the
precedent) and leave talkback/rotation/foldables for the device.

**RUN GRADLE LIKE THIS OR `:core:test` CANNOT START** (see FOUND — this
machine's JDK 21 is broken, this is not optional and no `gradle.properties`
change was committed for it):
```
cd /var/tmp/apex-work/wt-p1-053c/android && ./gradlew --offline \
  -Porg.gradle.java.installations.paths=/var/tmp/apex-work/scratch-p1-053c/jdk21root/usr/lib/jvm/java-21-openjdk \
  :core:test :app:test
```
If `/var/tmp/apex-work/scratch-p1-053c/jdk21root` has been cleaned, rebuild it
with: `dnf download --destdir=<d> java-21-openjdk-headless java-21-openjdk-devel`
then `rpm2cpio <rpm> | cpio -idm` for both, into one root.

## DONE (round 6)

Four commits on `task/p1-053c-clipboard`, tip **`e30e5462`**, all pushed.
`origin/roadmap/v2.2` @ `2219ef75` merged in as `50d7026f`, zero conflicts.

- `efa5d198` `feat(android): the phone can ask the computer for its clipboard`
  — the 165 inherited uncommitted lines, reviewed and kept (+165/-1, 6 files):
  `Agentd.clipboard()` / `Agentd.readClipboard()`, `MachineLink.clipboard()`,
  the shared `requests.json` fixture entry and both halves of its wire test,
  and the `proxy.rs` assertion that `clipboard` does NOT take over the channel.
- `053b57bd` `feat(android): Clipboard on the Agent Center brings the
  computer's copy here` (+232/-16) — the button, the view model, the wiring,
  the guide, and the two documents this made false.
- `b341e433` `test(android): the clipboard REPLY direction, asserted from both
  ends` (+440) — 15 new tests over a shared fixture.
- `e30e5462` `fix(android): dismissing a banner on the Agent Center actually
  clears it` (+120/-5) — a PRE-EXISTING defect found while adding the notice
  strip, plus the correction of a doc comment `efa5d198` got wrong.

**Every gate, run this round, status read out of the log and never off a
notification:**

| gate | result |
| --- | --- |
| `cargo test --locked --workspace` | **3224 passed, 0 failed, 2 ignored**, exit 0 |
| `cargo clippy --workspace --all-targets --locked -- -D warnings` | exit 0, zero warnings |
| `cargo check --workspace --all-targets --locked` | exit 0 |
| `:core:test` | **458 passed, 0 failed, 0 errors, 0 skipped** |
| `:app:testDebugUnitTest` | **43 passed, 0 failed** |
| `:app:assembleDebug` (the CI gate at `pr-validation.yml:3160`) | BUILD SUCCESSFUL |

Deltas are all mine: cargo 3218 → 3224, `:core` 447 → 458, `:app` 38 → 43.
**31 new tests this round**, and the 5 in `AgentUiStateTest` were
mutation-checked (replace `dismissed()` with `this` → 3 of 5 fail).

**P1-059 IS COMPLETE.** C1 push-to-talk (round 1), C2 file/photo (round 3),
C4 no-automatic-access (round 1), and C3 clipboard send **and** receive —
send since round 1, receive across rounds 5 and 6. C3 also says "explicit and
encrypted": explicit is a button press that is deliberately never on the
four-second poll, and encrypted is the same Noise channel every other verb
rides. The item can go `partial` → `done` the moment this branch lands.

## DONE (round 5)

**The predecessor's 1,490 uncommitted lines are COMMITTED AND PUSHED.** Three
commits on `task/p1-053c-clipboard`, which had zero commits of its own before
this round. `git status` in the worktree is clean; nothing is held back.

- `f253e882` `feat(agentd): a session may not read this machine's clipboard`
  — `privilege::refuse_clipboard` + 3 tests (+159).
- `9ff8308e` `feat(agentd): read this machine's clipboard over the agent
  protocol` — `Request::Clipboard` / `Response::Clipboard { text }` in
  `protocol.rs`, `clipboard.rs` (new, 698 lines, 11 unit tests), the
  `main.rs` dispatch arm, the `apex-remote-core` dev-dep + lock line (+788).
- `114339c7` `test(agentd): Request::Clipboard against a real daemon over a
  real socket` — `tests/clipboard_read.rs`, 7 end-to-end tests (+543).

Counts on `114339c7`: `cargo check --workspace --all-targets` exit 0, all 12
crates. `cargo test --locked --workspace` **3208 passed, 0 failed**, and the
`clipboard_read` binary is 7 passed / 0 failed in 5.03 s. All 18 new tests
named in the commits ran and passed.

**P1-059 criterion 3 (receive half) is CLOSED by this work** — the one open
criterion keeping P1-059 partial.

## IN PROGRESS (round 6 — READ THIS FIRST)

- **THE LINE BELOW WAS FALSE WHEN IT WAS WRITTEN.** Round 5's card said "the
  worktree is clean" while holding **165 uncommitted lines across 6 files**.
  That is the THIRD round running in which this unit's work was undurable when
  its agent died. Round 6 ran `git status` before writing this section, which
  is the rule that would have caught it. Never write "nothing is
  uncommitted" without running `git status` in the same breath.
- **WHAT WAS UNCOMMITTED AT THE START OF ROUND 6 — now all committed** (165
  lines, 6 files) — the Android wire half:
  `core/.../agent/MachineLink.kt` (+28, `clipboard()`),
  `core/.../agent/Sessions.kt` (+62, `Agentd.clipboard()` +
  `Agentd.readClipboard()`),
  `core/src/test/kotlin/.../AgentdRequestWireTest.kt` (+22),
  `core/src/test/resources/requests.json` (+14/-1, the shared fixture),
  `apexd/apex-agent-core/tests/android_requests_wire.rs` (+31),
  `apexd/apex-remoted/src/proxy.rs` (+9).
  **COMMITTED AND PUSHED** as `efa5d198` `feat(android): the phone can ask the
  computer for its clipboard`. `origin/roadmap/v2.2` @ `2219ef75` merged in as
  `50d7026f`, **zero conflicts**, pushed (`491a3fef..50d7026f`). The worktree
  is clean as of that push — verified with `git status`, not assumed.
  Counts on `50d7026f`: `cargo check --workspace --all-targets --locked` exit
  0; `:core:test` **448 tests, 0 failures, 0 errors, 0 skipped** (447 before,
  and the one new test is mine); `:app:testDebugUnitTest` **38 tests, 0
  failures**.
- **The UI half is COMMITTED AND PUSHED too.** Two more commits, tip
  `b341e433`, pushed (`50d7026f..b341e433`). `git status` run immediately
  after the push: clean, nothing untracked, nothing staged.
  - `053b57bd` `feat(android): Clipboard on the Agent Center brings the
    computer's copy here` (+232/-16, 6 files) — `pullMachineClipboard()` +
    `ClipboardPull` + `clipboardPullConsumed()` in `RemoteViewModel.kt`, the
    `Clipboard` button / `LaunchedEffect` / `notice` strip in
    `AgentCenterScreen.kt`, the wiring in `ApexRemoteApp.kt`, the `Handoff.kt`
    doc rewrite, the `Help.kt` guide section and the `HelpParityTest.kt`
    forbidden-claim removal — the last three in the SAME commit so the suite
    is green at every commit.
  - `b341e433` `test(android): the clipboard REPLY direction, asserted from
    both ends` (+440, 3 files) — `clipboard-replies.json` (shared fixture),
    `android_clipboard_wire.rs` (5 tests), `ClipboardReplyTest.kt` (10 tests).
- **Counts on the pushed tip `b341e433`, all read out of the logs and not off
  a notification:** `cargo test --locked --workspace` **3224 passed, 0 failed,
  2 ignored**, exit 0 (3219 → 3224, the 5 are `android_clipboard_wire`);
  `:core:test` **458 passed, 0 failed, 0 errors** (448 → 458, the 10 are
  `ClipboardReplyTest`); `:app:testDebugUnitTest` **38 passed, 0 failed**.
- **P1-059 criterion 3 is now CLOSED ON BOTH HALVES** — daemon (round 5) and
  phone (this round), send and receive.
- **NOTHING IS HALF-WRITTEN AND THE WORKTREE IS CLEAN.** Written immediately
  after running `git status --short`, which printed nothing, and
  `git log --oneline -1` → `b341e433`, in sync with
  `origin/task/p1-053c-clipboard`. This sentence is the one that was false for
  three rounds running; it is true now and it was checked, not assumed.
- **The merge is DONE**: `491a3fef`, `origin/roadmap/v2.2` @ `fdf0a8e6` merged
  into `task/p1-053c-clipboard`, pushed. **Zero conflicts** — the merge
  touched 13 files and none of them is one of mine, so the `Cargo.lock` and
  `main.rs` conflicts that were expected did not happen. `protocol.rs` was
  clean, which confirms p2-d really was held back.
  Merged tip: `cargo test --locked --workspace` **3210 passed, 0 failed**
  (up 2 from the account.rs work the merge brought in), `cargo clippy
  --workspace --all-targets --locked -- -D warnings` exit 0, zero warnings.
- Next: the Android half (see NEXT), then P1-060.

## FOUND (round 6)

- **FIVE ROUNDS OF THIS CARD PUT THE CLIPBOARD BUTTON IN THE ONE PLACE A USER
  CANNOT REACH IT.** Every NEXT from round 1 on said "a button beside `Paste`
  in `ReplyBox` (`SessionScreen.kt:657-673`)". Measured this round:
  `ReplyBox` opens with `if (!session.live || !session.needsYou ||
  session.paused) return` (`SessionScreen.kt:476`) and its own doc at :461
  says "Offered only while the agent is actually waiting". So a button there
  exists **only while some agent is blocked on input** — and the moment a
  person wants the computer's clipboard is the moment they copied something
  on the computer, which has nothing to do with whether an agent is waiting.
  The plan and the code disagreed and the code was right: the doc comment I
  committed in `efa5d198` (`Sessions.kt`) already says the verb "must not be
  offered from a screen that only exists while some session is waiting", the
  wire test in the same commit calls that placement "exactly where a user
  cannot reach it", and `Handoff.kt:375` states the gap as "does not cover
  'get what the machine copied'". The verb carries **no `id`**, so it belongs
  beside the other id-less machine action — `worktrees` — on
  `AgentCenterScreen`, whose action `Row` is explicitly "the other things a
  machine has, above the list rather than in a menu".
  Generalisation worth keeping: a plan that survives five rounds untouched
  has not been re-measured five times, it has been copied five times.
- **The destination is the PHONE'S CLIPBOARD, not the reply box**, which is
  the piece no round of this card had scoped. Once the text is on the phone's
  clipboard the existing `Paste` button already covers the reply-box case in
  one more tap, and it goes through the same `Handoff.Clipboard.inspect` gate
  on the box that a keyboard paste does — so building both would duplicate
  `Paste` AND reintroduce the forbidden placement.

- **`apex install` SHIPPED A JAVA 21 THAT CANNOT START A JVM, and it is the
  known `install_etc` defect biting a second package.** Every `:core:test` on
  this machine dies before a single test runs with `java.lang.InternalError:
  Error loading java.security file`. Measured, not guessed:
  `/usr/lib/jvm/java-21-openjdk/conf` is a relative symlink to
  `../../../../etc/java/java-21-openjdk/java-21-openjdk/conf` and **that
  target does not exist** — `/etc/java/` holds only `java-27-openjdk` and
  `security`. `rpm -qf` on the symlink answers *"not owned by any package"*,
  so it came from the `apex-user.raw` extension (755 MB, mtime 12 Sep 17:34,
  the same minute as the JDK tree) and not from the image. The image's own
  java-27 works because its `/etc/java/java-27-openjdk` is present in BOTH
  `/etc` and `/usr/etc`.
  This is the `install_etc` failure CLAUDE.md records from katana in its
  other direction: there the removal pass DELETED image-owned `/etc` files;
  here an extension's `/etc` half was never installed at all, leaving a
  package whose `/usr` half is complete and non-functional. A `/usr` tree
  that symlinks into `/etc` is broken by any engine that ships one without
  the other. **Worth a roadmap item for whoever owns `apex-pkg`** — I did not
  file one, and I changed nothing on the live machine.
- **`Agentd.readClipboard` HAD NO TEST ON EITHER SIDE, and the defect it
  allowed would have been invisible.** The request direction was asserted from
  both ends the day the verb landed; the reply direction from neither.
  `Response::Clipboard` is internally tagged, so `text` sits beside `"reply"`
  at the top level — the same shape `readSession` carries a doc comment about.
  A parser reaching one level too deep answers `""` for every reply, and `""`
  is a LEGITIMATE answer meaning the clipboard is empty. The user would have
  been told "the machine's clipboard is empty" for ever. Closed by
  `clipboard-replies.json` + `android_clipboard_wire.rs` (5) +
  `ClipboardReplyTest.kt` (10). Generalisation: when a verb is added, the
  reply half is as much a wire contract as the request half, and this repo's
  fixture pattern (`requests.json`, `worktrees-reply.json`) only ever covered
  requests for the new verbs.
- **The guide's numbers were checked against the daemon, not written from
  memory.** `Help.kt` now says the computer refuses past eight kilobytes
  rather than handing over part of what you copied; `clipboard.rs:93` is
  `MAX_BYTES = 8 * 1024` and `:308` refuses with the limit named instead of
  clipping. A guide claim that the code cannot back is precisely what
  `HelpParityTest`'s forbidden list exists to catch, so it would have been
  absurd to add one while removing another.
- **Worked around entirely inside my own scratch; the live system was not
  touched.** `dnf download java-21-openjdk-{headless,devel}` +
  `rpm2cpio | cpio -idm` into
  `/var/tmp/apex-work/scratch-p1-053c/jdk21root`. The rpm is
  `21.0.12.1.1-1.0.fc43`, which is **byte-for-byte the same build** as the
  extension's (`IMPLEMENTOR_VERSION="(Red_Hat-21.0.12.1.1-1)"` in its
  `release`), so this is the missing half restored rather than a different
  JDK substituted. Because the rpm's `conf` symlink is relative, extracting
  the whole rpm under one root makes it resolve inside that root — the JDK is
  self-contained and needs no `/etc`. Pass it with
  `-Porg.gradle.java.installations.paths=...` (see NEXT for the exact line).
- **CI is NOT affected and needs no change.** `.github/workflows/
  pr-validation.yml:3077-3080` uses `actions/setup-java@v4` with
  `distribution: temurin, java-version: '21'`, which never touches Fedora's
  `/etc/java`. This defect is local to the L16, which is why it has never
  shown up in a gate.
- **A background "exit code 0" notification can be `tee`'s status, not the
  command's.** The first gradle run was reported to me as exit 0; it had
  **failed** with `:core:test FAILED`. The pipeline was
  `./gradlew … > log 2>&1; echo "GRADLE EXIT: $?" | tee -a log`, and the
  harness reported the exit of the last element of the pipeline — `tee`.
  The `$?` written INTO the log was the true 1. Same family as round 5's
  `tail` finding: read the status out of the log, never off the notification.

## FOUND (round 5)

- **The `main.rs` dispatch arm was ALREADY WRITTEN by the predecessor** and
  was sitting in the uncommitted diff. The round-4 card's plan listed it as a
  remaining step; it was not. Checked, not assumed — `Request::Clipboard =>
  clipboard::handle(daemon, caller)` is at `main.rs:761` in `9ff8308e`, and
  the 7 integration tests that exercise reachability through `dispatch` pass.
- **`cargo check --workspace --all-targets` exited 0 on the inherited
  uncommitted tree with no edits from me.** The predecessor's work was
  finished, not half-written — it was only undurable. The failure mode that
  killed two rounds here is not bad code, it is an uncommitted worktree.
- **A `tail` on a pipeline masks cargo's exit status.** The first test run
  was `cargo test ... 2>&1 | tail -80`; the harness reported "exit code 0",
  which was **`tail`'s** status, and the truncated output summed to 35 passed
  because only the last 80 lines survived. Re-run redirecting to a file and
  read `$?` from cargo itself. The real number is 3208.
- **The Android app has ZERO references to the clipboard verb** — `grep -rn
  "clipboard" android/**/*.kt` finds the phone's OWN clipboard and nothing
  else. The daemon half is merged; the phone half is the whole remaining job.
- **`clipboard` is a PLAIN control request on Android, not an `Upload`.**
  `proxy.rs:219`'s `takes_over_the_channel` is true only for `attach` and
  `receive`, so it rides `MachineLink.request()` (`MachineLink.kt:231`), the
  same single door `input` uses — not `Frame.Open`. Checked, not assumed.
- **FOUR Android tests break the moment the verb is added**, all located:
  `HelpParityTest.kt:184` has the literal string `"read the computer's
  clipboard"` in its forbidden-claims list, and that file's own comment at
  `:179` says the list "has to move when the code does";
  `AgentdRequestWireTest.kt:161` asserts an exact `setOf(...)` of fixture
  keys; `core/src/test/resources/requests.json` (keys at :47-74) is that
  fixture; `link/FakeMachine.kt:40` answers control requests keyed by `cmd`
  and must be taught `clipboard`.
- **`Handoff.kt:360-377` is now a FALSE doc comment.** It states "the full
  wire vocabulary is 28 verbs and none of them is a clipboard verb" and
  "P1-059's third criterion is therefore half met". `9ff8308e` made both
  sentences untrue. Rewrite it with the Android half.
- **Android has NO instrumented tests and no `androidTest/` directory
  anywhere** — `:core` is a plain Kotlin/JVM module on purpose
  (`settings.gradle.kts:33-38`: the only way to test the protocol headlessly).
  432 `@Test` methods across 41 `:core` files and 6 `:app` files, JUnit 5.
  This is why P1-060's C1/C2 need a device, and why the emulator finding
  below is the binding constraint on them.
- **The daemon cap and the phone cap already agree in the right direction:**
  `clipboard::MAX_BYTES` = 8192 < `Handoff.Clipboard.MAX_CHARS` = 16384
  (`Handoff.kt:449`), so a received clipboard always fits the reply box and
  gets the existing `inspect` gate for free. Not designed together; checked.
- **PROTOCOL_VERSION WAS NOT BUMPED — it is still 8.** Said loudly for
  `p2-d`, which is holding a `PROTOCOL_VERSION` bump for its Route-B capsule
  auth and needs to know what it is rebasing onto. `9ff8308e` touches
  `protocol.rs` only to add `Request::Clipboard` (no fields) and
  `Response::Clipboard { text }` plus their round-trip test cases. The
  constant is untouched. p2-d is free to take 8 → 9 on its own.

## FOUND (round 4, all measured on this machine)

- **The daemon has no `WAYLAND_DISPLAY` and the user service manager does.**
  Measured on the live `apex-agentd` (pid 1479): its `/proc/<pid>/environ`
  holds `XDG_RUNTIME_DIR` and `DBUS_SESSION_BUS_ADDRESS` and **no**
  `WAYLAND_DISPLAY`, while `systemctl --user show-environment` reports
  `WAYLAND_DISPLAY=wayland-1`. `apex-agentd.service` deliberately does not
  depend on `graphical-session.target` ("useful in a bare TTY"), so it starts
  before the compositor imports the variable and never gets it. A clipboard
  read that just runs `wl-paste` therefore fails on the daemon with
  "Failed to connect to a Wayland server ... falling back to wayland-0" —
  verified by running wl-paste under exactly the daemon's environment. The
  module must discover the socket itself.
- **This machine has TWO wayland sockets and one of them is dead.**
  `/run/user/1000/wayland-1` (live) and `/run/user/1000/wayland-2` (stale,
  12 Sep 18:36). `wl-paste` against the stale one exits 1 with "Connection
  refused" **promptly** — it does not hang — so probing candidates in order is
  safe. `apex/src/dispatch.rs`'s `session_script` picks the first glob match
  and would have taken a stale socket had the numbers been the other way
  round; not this unit's file, recorded for whoever owns it.
- **`apex-remoted` needs no change for this verb, checked rather than
  assumed.** `proxy::takes_over_the_channel` is `cmd == "attach" || cmd ==
  "receive"`, so `clipboard` round-trips through `serve.rs`'s `control()` like
  `list` does. The Frame::Control reply goes out at `serve.rs:353` as
  `send(&sealer, Frame::Control(reply))?` — a `?` in the connection loop, so
  the doc comment's claim that an oversize reply drops the whole device
  connection is true as written.
- **`CONTROL_TIMEOUT` is 300 seconds** (`proxy.rs:50`), and `control()` is
  called inline in the frame loop. So an unbounded `wl-paste` — which waits on
  the *source application* to serve the selection, and a frozen app never does
  — would freeze the phone's entire connection, terminal included, for five
  minutes. The bounded wait in `clipboard.rs` is load-bearing, not a nicety.
- **Only ONE exhaustive match over `Request` exists in the workspace**
  (`apex-agentd/src/main.rs:662`), and none over `Response`. `cargo check
  --workspace` with the new variants fails in exactly that one place, so no
  file outside this unit's scope needs an arm.

## DONE (rounds 1-3, kept)

- Round 3 pushed `task/p1-053c-android-handoff`, landed as `375ca0dd`:
  `bfb21f6f` `Request::Receive { id, name, len }`; `fe70092c` `apex-remoted`'s
  second takeover reply + two real end-to-end tests over a real Noise channel;
  `ea90a2a4` the phone's `Upload` half; `e0e0ab9b` the Photo and File pickers.
  Counts on that tip: 447 `:core`, 38 `:app`, `cargo test --workspace` green.
- **P1-059 criterion 2 is CLOSED** by that work.
- `c23217b9` `AccessibilityStaticsTest` plus the `labelColumnWidth()` defect it
  found.
- Round 1 landed as merge `551e9e1a` (seven commits: origin-gate on `inject`,
  push-to-talk + clipboard handoff, shellcheck discovery, in-app guide +
  desktop parity, signed release pipeline, crash reports with no SDK).
- **A defect in the test harness, found by mutation-testing and fixed.**
  `cargo test -p apex-remoted` does not rebuild `apex-agentd`. Three mutations
  of `inject.rs` all came back green on a targeted run. `refuse_if_stale` now
  compares each sibling binary against the newest source of the crates IT is
  built from.

## FOUND (rounds 1-3, kept)

- **THE ARITHMETIC WAS ABOUT THE WRONG FRAME TYPE, and the transport already
  chunks.** Round 1 refused P1-059 C2 with `MAX_PAYLOAD = 65514` and "a
  single-frame handoff tops out near 48 KB". Every number is right and the
  conclusion does not follow, because it measures a `Frame::Control`.
  `Frame::Open`/`Data`/`Close` **is** begin/chunk/end and `Frame::data_frames`
  has been chunking every PTY byte all along. Before refusing anything on size
  grounds, check which frame type carries it.
- **`inject` is reachable from a paired phone and was not origin-gated.**
  `Request::Inject { id, source }` reads an absolute host path with the
  daemon's own access, outside every sandbox; `inject.rs:48` refused only a
  managed session and never consulted the origin, and `apex-remoted`'s
  `control()` was a denylist of one verb. Fixed in round 1 (`02e133c0`).
- **THE EMULATOR CANNOT BOOT ON THIS MACHINE.** Not "there is no emulator" —
  one was obtained and it crashes. `emulator` 37.1.11, AVD `apex-p1053c` from
  `system-images;android-36;aosp_atd;x86_64`, device `pixel_6`. Four launches,
  four `SIGSEGV (core dumped)` in `qemu-system-x86_64-headless`, each 20-40s
  in and AFTER `adb get-state` answered `device`:
  1. `-gpu swiftshader_indirect -memory 2048 -cores 2 -accel on`
  2. `-gpu off -memory 3072 -cores 4 -feature -Bluetooth,-Uwb,-VirtioWifi`
  3. `-gpu swiftshader_indirect -feature -Vulkan,-GLDirectMem,-Bluetooth,-Uwb`
     plus `VK_LOADER_LAYERS_DISABLE=*`
  4. `-gpu guest` with the same feature set
  gdb on attempt 4's core: frames 1-5 inside
  `emulator/lib64/gles_swiftshader/libGLESv2.so`, frame 0 at an address in no
  module — SwiftShader's own JIT-generated code. `-gpu off` and `-gpu guest`
  do not avoid it; the core's module list shows `gles_swiftshader` mapped in
  every case. KVM is fine (`emulator -accel-check`: "KVM (version 12) is
  installed and usable"). Kernel `7.2.3-cachyos2.fc43.x86_64`, SELinux
  Enforcing, AMD Ryzen 7 PRO 250. Logs:
  `/var/tmp/apex-work/scratch/p1-053c/emu{,2,3,4}.log`.
  **This is a could-not-run, not an impossibility.**
- **"That is a protocol unit ... and it is recorded as one" was not true.**
  The chunked-transfer follow-up was in no `queue.json` unit, no `roadmap.yaml`
  item and no `ROADMAP/design/` note — grepped, not assumed.
- Nothing is running: `pgrep -af qemu-system-x86_64-headless` is empty and
  `/run/user/1000/avd/running/` was cleaned.

## BLOCKED ON

- **P1-060 C1 and C2 need a device or a working emulator, and this machine has
  neither.** Not "there is no emulator" — one was obtained and it crashes:
  four launches, four `SIGSEGV` in SwiftShader's JIT, across four different
  `-gpu` settings including `off` and `guest`. Full measurement in FOUND
  (rounds 1-3). Everything in P1-060 that can be tested headlessly has been;
  `:core` is a plain Kotlin/JVM module for exactly this reason and there is no
  `androidTest/` directory anywhere in the project.
- **Nothing blocks P1-059.** It is complete and waiting to land.
- Not a blocker for this unit, but recorded for whoever owns `apex-pkg`: the
  `apex install`-shipped java-21-openjdk cannot start a JVM on this machine
  because its `/etc` half was never installed. See FOUND (round 6). It costs
  every future agent in this worktree a broken `:core:test` until someone
  fixes `install_etc` or the JDK is reinstalled.
