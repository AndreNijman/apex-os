# p2-c
items: P2-005, P2-006, P2-007
repo: apex-os
worktree: /var/tmp/apex-work/wt-p2-005
branch: task/p2-c-2

## NEXT
Add the `hotspot_ifaces` set (`type ifname`) plus the two `iifname
@hotspot_ifaces` accept rules (udp 53,67 / tcp 53) to
`files/system/nftables/apex.nft` in the file's own voice, then extend
`tests/test-device-services-netns.sh` with real-packet DHCP-DISCOVER and DNS
cases and a second veth that proves the exception does not reach another link.

## DONE
- Branch `task/p2-c-2` cut off `cd4a799e` in `/var/tmp/apex-work/wt-p2-005`.
- The worktree's one dirty file, untracked `.p2c-pull.log`, was a `podman pull`
  log from a killed earlier p2-c attempt. It finished: image
  `ghcr.io/andrenijman/apex-os:base-d12d34502f65c4e8704e82e1285ce103a415e4ab`
  is local, 14.8 GB, and `d12d3450` carries 36232f5's package additions. Moved
  to `/var/tmp/apex-work/scratch/p2-c/pull-base-image.log` as the record; it is
  not repo content.

## IN PROGRESS
Nothing committed yet this round.

## FOUND
1. **Round 1's first hotspot blocker is already gone and the tool still
   reports it.** `files/system/nftables/apex.nft` on `cd4a799e` has **no
   forward chain** — P1-044 removed it (header, lines 25-35: every base chain
   at a hook is evaluated, so a drop here killed rootful container networking).
   `files/system/libexec/apex-devices:529` still states "the firewall drops
   forwarded traffic" as a fact about the shipped policy. To be measured in the
   netns before the line comes out, not deleted on the comment's word.
2. **Card item 1 cannot be done where round 1 said it should be.**
   `files/system/libexec/apex-desktop-menu` is a right-click *handler* — it
   calls `apex shell ipc context-menu open` and restarts the shell if that
   fails. It holds no entries. The entries are in apex-shell's QML (another
   repo); `files/desktop/labwc/menu.xml` is the emergency menu by its own
   header and says APEX Shell owns the launcher and settings.
3. Round 1's note "do not edit `files/system/nftables/apex.nft` — P1-044 owns
   it" is superseded for this round by the dispatch, which names the firewall
   policy as the work. P1-044 is in the ready queue; the diff is kept additive
   so a later P1-044 agent is not surprised.

## BLOCKED ON
Nothing yet.
