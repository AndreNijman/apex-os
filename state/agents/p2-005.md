# p2-005 — printing/scanning/shares, networking, Bluetooth/dock

Items: **P2-005**, **P2-006**, **P2-007**.
Repo: apex-os. Branch `task/p2-005-device-maturity`, worktree
`/var/tmp/apex-work/wt-p2-005`, forked from `origin/roadmap/v2.2` @ `9a24d2b`.

## NEXT

Run `dnf5 repoquery --available` over the package names below to prove they
resolve, then sweep `kernel/` for the config symbols the three acceptance lists
depend on.

## What the rpmdb sweep on the L16 already found (2026-09-07, live image)

Present: cups, cups-filters, sane-backends, sane-airscan, ipp-usb, hplip,
system-config-printer, cups-pk-helper, gvfs, gvfs-smb, gvfs-mtp, nfs-utils,
libmtp, libgphoto2, bluez, bluez-obexd, libldac, avahi, nss-mdns, colord,
udisks2, thunar-volman, NetworkManager, fwupd, usb_modeswitch.

**Absent, and each one is an acceptance criterion:**

| Missing | What does not work |
|---|---|
| `gvfs-fuse` | every GIO mount (smb://, mtp://, dav://) is invisible to non-GTK apps — no `/run/user/UID/gvfs` at all |
| `gvfs-gphoto2` | cameras (PTP) never mount. "cameras" is named in the criterion |
| `gvfs-nfs` | NFS in the file manager |
| `cifs-utils`, `samba-client` | no `mount.cifs`, so no kernel SMB mount and no `smbclient` |
| `bolt` | nothing authorises a Thunderbolt device. A TB dock sits at `authorized=0` |
| `wireguard-tools` | no `wg`, no `wg-quick`, no way to import a WireGuard conf |
| `NetworkManager-openvpn` and friends | no VPN plugin of any kind |
| `dnsmasq` | NM shared-mode hotspot has no DHCP/DNS server |
| NM connectivity config | no captive-portal detection: NM never reaches the `portal` state |

## Firewall interaction, not yet proven

`files/system/nftables/apex.nft` has `forward` at `policy drop`, and its only
DHCP rule matches replies (`udp sport 67 udp dport 68`). A hotspot's clients
send a broadcast DISCOVER to `dport 67`, which is a new flow, so
established/related cannot rescue it. Both halves need proving in a netns.

**Do not edit `apex.nft` or `files/system/firewall/services`** — P1-044's agent
owns them and has left no card to coordinate against.
