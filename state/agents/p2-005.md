# p2-c
items: P2-005, P2-006, P2-007
repo: apex-os
worktree: /var/tmp/apex-work/wt-p2-005
branch: task/p2-c-2  (merged origin/roadmap/v2.2 -> 266dcc57, pushed @ 919b3b20)

second_worktree: /var/tmp/apex-work/wt-p2-c-shell
second_branch:   task/p2-c-2-shell  (apex-shell, cut from origin/roadmap/v2.2 @ a0b3deb, pushed)

## NEXT
**THE UNIT IS CLOSED on the agent side.** The container sweep is exhausted: the
method found FOUND 11 and 12 this round, and the remaining suspects were checked
and cleared (see the 2026-09-18 section of
`ROADMAP/evidence/P2-005-007-device-maturity.md`). Everything still open needs a
BOOTED machine, which needs `apex update` + a reboot — the boot path, off-limits
to agents.

The step-by-step checklist for Andre is written, at the end of that evidence
file: update and clean reboot, the six lines only a booted machine can answer,
the hotspot safety line, unmasking avahi, the end-to-end hotspot test with a
phone, and fixing the two 802.1X profiles that validate nothing.

If a future round wants work here anyway, the only honest candidates are:
(a) re-run the container sweep after the next image build, since a NEW reader
    can arrive with any commit; and
(b) the apex-shell side (`task/p2-c-2-shell`, clean, pushed) if the firewall
    page grows more rows.
Do NOT manufacture a round to avoid closing this.

## DONE (round of 2026-09-18, round 27)
- **`0403786f` — five returns stood above a reading that needed no daemon.**
  The system CA trust store is a `[ -r ]` on one file and sat below every
  "could not look" return in `do_network`/`report_enterprise_wifi` — including
  "no saved profile uses it", which describes nearly every machine. So the line
  that says a correct password still fails to associate printed almost nowhere,
  and no assertion had ever named it. `report_nm_links`,
  `report_8021x_profiles`, `report_ca_trust` split out. 76 -> 90, eleven
  mutants. See FOUND 11.
- **`919b3b20` — "systemd could not be asked" arrived as "it is not running".**
  `unit_active` had two answers for a question with three. Six readers stated a
  negative about a daemon nothing had asked about, and two of those are claims:
  "this machine serves nothing" and the hotspot's "nothing known is in the way".
  This file's own 2026-09-13 evidence carried a FOOTNOTE warning readers not to
  believe that hotspot line — the caveat belonged in the tool. 90 -> 108,
  twelve mutants. See FOUND 12.
- **Two harness defects caught before the assertions were trusted**: a
  `case_says "readable"` satisfied by "present and unreadable" (green 91/91
  under a mutant that never read the file), and a mutant that was different but
  unobservable passing silently. Both written up in the evidence file.
- **Gates**: shellcheck clean on 0.11 AND the runner's 0.9.0; coverage 163
  scripts / 0 known-failing; suites-in-ci 75 suites, 71 run, 4 exempt (no fifth
  added); no conflict markers. `roadmap/v2.2` @ `266dcc57` merged clean.
- **Evidence**: `ROADMAP/evidence/P2-005-007-device-maturity.md` gained a dated
  section with both findings, the before/after table read out of the image, the
  three cleared suspects, and THE CHECKLIST.

## IN PROGRESS (round of 2026-09-14, round 26)
Nothing half-written. The `--json` thread is CLOSED on both sides.

## DONE (round of 2026-09-14, round 26)
- **`ded1ab1` (apex-shell) — the page reads a shape now.** Sweep step is
  `["apex","firewall","status","--json"]`; `_stepArgv` grew a `status` entry the
  sweep NEVER queues, unshifted by `_resolve` only when the JSON read failed
  validation. The fallback has to be a second PROCESS, not a second parse:
  measured on this laptop, `apex firewall status --json` -> clap's
  `unrecognized subcommand 'firewall'` on stderr, rc=2, stdout EMPTY. The miss
  is HELD, not assigned, or the page would flash "could not be read" every
  sweep on exactly the machines where the fallback is the working path. Shared
  links became a real row (the page was showing that sentence by accident, in
  the always-allowed row). `check-firewall-ui.sh` 25 -> 35 checks, 20/20
  mutants behave. Its self-test caught two of its own defects on the first run:
  a check satisfied by the `visible:` binding rather than the `label:`, and a
  pre-existing mutant that stopped applying because this commit moved one space.
- **`c6c4f15` (apex-shell) — five suites in the repo ran nowhere.** Counted
  every `node tests/*.js` and `tests/check-*.sh` in ci.yml against the tree:
  `firewall-test.js`, `check-firewall-ui.sh`, `check-no-conflict-markers.sh`,
  `agent-state-test.js`, `check-colour-page.sh` are in no step. The
  conflict-marker one was WRITTEN because roadmap/v2.2 shipped with unresolved
  markers and then wired to nothing. All five verified green before wiring, so
  this adds no red.
- **`a585e4e` (apex-shell) — the fixture that could not see the defect.**
  `parseStatus` treated a blank line as nothing, so c7a28f2c's shared-links
  paragraph stayed "under" the always-allowed heading and its last line won:
  the page reported "DHCP and DNS are open on those links only" as the traffic
  the firewall never drops. One line fixes it; three cases go red without it
  (mutation-proved, applied-check done). `parseStatusJson` added, VALIDATING
  the shape rather than spreading keys — a renamed key is `ok:false`, not an
  empty string, so drift costs the page its content instead of being silent.
  Required keys, not an exact set, so an additive change on the apex-os side
  cannot blank the page. Fixtures are FILES now:
  `tests/fixtures/firewall/gen-firewall-fixtures.sh` captures six policy states
  as prose AND `--json` plus the catalogue, from the shipped helper under an
  nft shim with CONF_DIR redirected (redirect asserted, hard failure).
  `--check` re-captures and diffs — proved both ways. 33 -> 76 checks.
- **`12201d9e` — the CLI half of `--json`.** `3ab3b6ce` landed the helper's
  `status --json`; `apex` is clap and `firewall_argv` rebuilds the helper argv
  from typed fields, so `apex firewall status --json` still died with
  "unexpected argument" before any process was spawned. `FirewallCmd::Status`
  now carries `json: bool`. Four cases assert the argv THROUGH `Cli::parse_from`
  on a real command line (not by building the enum by hand), so a flag clap
  would reject cannot pass the test either: status alone stays prose, `--json`
  reaches the helper, every verb reaches it under its own name, and a service
  name is one word however it is spelt (`../../etc/issue`, `a b`).
  `firewall_argv`'s doc comment claimed it was kept pure so its argv "can be
  asserted without running it" and nothing asserted it. 631 -> 635 bin tests,
  clippy --all-targets clean.
- **`85776285` — merged `roadmap/v2.2` (2219ef75).** Five branches landed
  alongside; clean ort merge, no conflicts.

## DONE  (round of 2026-09-13)
- Merged `roadmap/v2.2` (`e799422b`) into `task/p2-c-2` and pushed. Five commits
  on top of the predecessor's `c7a28f2c`.
- **`c8297aa3` — the hotspot rules, measured.**
  `tests/test-device-services-netns.sh` 12 -> 29 assertions, all packets. A
  second veth (`apexhost2`, 10.92.0.0/24) exists only to be the link the
  exception is NOT scoped to; a third namespace exists so "does the policy route
  a client's traffic" can be asked with a packet delivered elsewhere. The
  hotspot cases call the shipped `apex-firewall` rather than `nft add element`,
  because the dispatcher calls the tool. Mutation-proved TWELVE ways, files
  restored byte-identical by sha256.
- **`9fd2c4b7` — the shellcheck gate's discovery predicate.** The exec bit was a
  predicate, and a script installed with `COPY --chmod=0755` needs no exec bit in
  git. Ten shipped shell scripts had a shebang and were linted by nobody.
  Discovery is the shebang alone now: 150 -> 160 scripts, 0 newly failing.
- **`9284fdf7` — two readers that stated instead of reading.** Both found by
  running `apex devices` inside the built image. See FOUND 3 and 4.
  `tests/test-apex-devices.sh` 55 -> 66, mutation-proved seven ways.
- **`9bd99232` — the NM dispatcher's security property.** It shipped tested by
  nobody. Ten cases driving the real script with an `APEX_HOTSPOT_FW` shim;
  mutation-proved seven ways. `tests/test-apex-firewall.sh` 32 -> 42.
- **`39d5e1e1` — an hs-list verdict that could not fail.** `*apexhost*` is
  satisfied by `apexhost2`; `grep -qw` now, proved by a twelfth mutation.
- **Evidence written**: `ROADMAP/evidence/P2-005-007-device-maturity.md` gained
  a dated section with the before/after table for every reader the image moved.
- **`3ab3b6ce` (landed into roadmap/v2.2 this round) — `status --json`.**
  `tests/test-apex-firewall.sh` 42 -> 86, mutation-proved seven ways.

## THE THREE THAT ARE ANDRE'S, NOT AN AGENT'S — unchanged
1. **A booted reading on the L16.** `links`, `connectivity`, `paired devices`,
   `hotplug (udev)`, `sharing a printer` and the hotspot line all need
   `apex update` + reboot. That is the boot path.
2. **`apex firewall hotspot` against a real shared connection** with a phone
   joined to it. Needs the image to land first.
3. **The L16's masked avahi.** Until it is unmasked that machine can discover
   neither a printer nor a scanner.

## FOUND
1. **The image question the dispatch asked is answered: yes, there is a usable
   one, and the tier chain is the opposite way round.** `Containerfile.base`
   opens `FROM ${CORE}`, so `base-d12d3450` already contains everything the
   `core-` image has. Every package P2-007 named is installed.
2. **`36232f5` is not an ancestor of `roadmap/v2.2`, and the work IS landed.**
   Integrate-4 cherry-picked, so the content is on the tip under other hashes.
3. **`apex devices network` reported a working machine as two blockers.**
   It stated the firewall's policy as fact. The reader now reads the mechanism.
4. **`connectivity` printed nmcli's error as the state of the network.**
   The rc was thrown away.
5. **A test that had never tested what it was named after.** "A hotspot with no
   dnsmasq is blocked" passed on a machine with `/usr/bin/dnsmasq`.
6. **Three of the four new connectivity assertions were weak**, caught by their
   own mutation.
7. **nft's set printing, measured.** Elements come back quoted and wrap one per
   line as soon as there is more than one.
8. **The `gphoto2` CLI is genuinely absent** while `libgphoto2` and
   `gvfs-gphoto2` are installed — two true statements, not a gap.
9. **The desktop-menu entry cannot be done in this repo**; it belongs in
   apex-shell's QML.
11. **A `[ -r ]` on one file was gated on five daemons.** The system CA trust
   store sat below every "could not look" return in the network reader, so it
   printed on almost no machine and nothing had ever asserted it.
12. **`unit_active` had no third answer.** `systemctl is-active` needs the bus
   and exits 1 with an error when there is none, while `is-enabled` answers off
   the unit files — so six readers reported a confident negative about a daemon
   nothing had asked about, and the hotspot's "nothing known is in the way" was
   a safety claim reached by not looking.
13. **`unit_state` is NOT affected and was left alone**, checked rather than
   assumed: `is-enabled` reads the unit files and answered `enabled` for every
   unit in a container with no bus.
10. **The shell's `--json` switch cannot be a straight swap.** Measured by
   reading both sides: on an image whose `apex` predates the flag, clap rejects
   `--json` with usage on STDERR and nothing on stdout, exit 2 — so there is no
   prose left to fall back on in the same output. The page exists partly to
   behave on exactly that machine (its `absent`/`could not be read` cases are
   captured from katana), so the switch needs a fallback step, not a swap.

## BLOCKED ON
Nothing.
