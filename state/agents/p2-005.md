# p2-005 — printing/scanning/shares, networking, Bluetooth/dock

Items: **P2-005**, **P2-006**, **P2-007**.
Repo: apex-os. Branch `task/p2-005-device-maturity`, worktree
`/var/tmp/apex-work/wt-p2-005`, forked from `origin/roadmap/v2.2` @ `9a24d2b`.

## NEXT

1. **stop_slop pass** over `files/system/libexec/apex-devices`'s user-facing
   strings and its `--help`, then over this card. It is a gate on all three
   items and has not been run.
2. **`tests/run-clippy.sh`** (from `chore/run-clippy`, not in this branch) after
   the Rust change. Started at `fce0a99`; if it is red, fix and re-commit.
3. Write `ROADMAP/evidence/P2-005-007-device-maturity.md` from the two live runs
   (L16 output and katana output are in this card under `## Live evidence`).
4. Optional, if there is time: an `apex devices` entry in the desktop menu
   (`files/system/libexec/apex-desktop-menu`) so the diagnosis is reachable
   without a terminal.

Not to be done here: the firewall itself (P1-044 owns it), and any settings UI
(that is an apex-shell worktree on its own branch).

## Landed

- `36232f5` — the package gaps in `Containerfile.core`/`.base` plus
  `tests/test-device-image.sh`, 41 passed, mutation-proved seven ways
  (40/1 x5, 39/2, 40/1; restored 41/0). Adds gvfs-fuse/gphoto2/nfs,
  cifs-utils, samba-client, bolt, wireguard-tools, the three NM VPN plugins,
  dnsmasq, pipewire-codec-aptx, bluez-obexd, simple-scan; enables avahi with a
  FATAL guard; disables hplip's hpaio SANE backend; ships
  `files/system/NetworkManager/21-apex-connectivity.conf` for captive portals.
- `c823974` + `f9ab0e7` — `tests/test-device-services-netns.sh`, 12 passed.
  Discovery (mDNS, LLMNR both halves) reaches the machine through the shipped
  policy; ipp/samba/nfs are silent; putting the catalogue's port into
  `allowed_tcp` reopens all three. Ports come from
  `files/system/firewall/services`, checked against what CUPS/smbd/nfsd bind.
  Mutation-proved four ways (11/1, 9/3, 9/3, 11/1; restored 12/0).

- `5a7d0c6` — `files/system/libexec/apex-devices` (564 lines) plus
  `tests/test-apex-devices.sh`, 34 passed. Six areas: print, scan, share,
  network, bluetooth, dock. Three-answer readers (`dir_state`, `read_attr`)
  that tell ENOENT from EACCES; `firewall_verdict` naming the policy where a
  shared service fails, and only when `apex-firewall.service` is active; every
  external call under `timeout`. Mutation-proved three ways (31/3, 32/2, 33/1;
  restored 34/0). Not yet wired into the `apex` CLI or the Containerfile.
- `fce0a99` — the CLI verb plus the three acceptance gaps, 53 passed.
  `Cmd::Devices` + `DeviceArea` (clap ValueEnum) + `devices_argv` in
  `apexd/apex/src/main.rs`, `DEVICES_ENGINE`/`ops::devices` in `ops.rs`, and a
  `Containerfile.base` block at line ~1138 that runs the helper against a world
  where every root it was given does not exist. New areas: `media` (MMC hosts,
  udisks2, the seat polkit wants, mounted media), enterprise Wi-Fi inside
  `network` (NM wifi plugin, the conf.d backend, 802.1X profiles with no CA
  certificate, the trust store), and hotplug inside `dock` (udevd, Type-C
  partners). Five helper defects fixed — see the commit body. cargo test
  -p apex: 315 passed. Mutation-proved seven ways (49/4, 52/1 x5, 51/2;
  restored 53/0).

## Live evidence — two machines, read-only, no prompts (2026-09-07)

`bash files/system/libexec/apex-devices all` on the L16 and, over ssh, on
katana. Both machines run the shipped image; katana was idle (load 0.04, Steam
client open, no game). Nothing was configured, paired or authorised.

**L16** — avahi masked, so `scan` short-circuits with the unmask command
instead of hanging; `print` reports MASKED. cups.socket enabled, no queues,
sharing off. gvfs-fuse absent, so GIO mounts exist only inside GTK; gvfsd-smb,
-dav, -mtp present, -nfs and -gphoto2 absent; no mount.cifs; mount.nfs present.
4 links, connectivity full, no VPN plugin dir, no `wg`. dnsmasq present and
owned by no package — it arrived with an `apex install waydroid` extension.
hci0 with one paired device (ProtoArc T1 Plus); codecs aac faststream g722 lc3
ldac opus sbc, no aptX. Thunderbolt `domain0`/`domain1` at `security=user`,
`iommu=1`, two host routers authorised, boltd absent. USB-C: 2 ports, one with
a partner attached. No MMC host. udisks2 running. Two saved 802.1X profiles
(WIRELESS-2.4, WIRELESS-5), both EAP PEAP, both with no CA certificate and
`system-ca-certs` off.

**katana** — avahi active, so `scanimage -L` returns and the whole run
finishes in **6.8 s**, finding `airscan:w1:CANON INC. TR4600 series` at
192.168.1.121 over WSD plus the v4l webcam. No Thunderbolt controller at all,
which is why the USB-C reader had to stop being behind that early return. hci0
with nothing paired. dnsmasq genuinely absent, so the hotspot reader counts one
blocker rather than L16's zero.

The pair is the point: the same program on two machines running the same image
produces the masked-avahi refusal on one and a real device list on the other,
and neither says "no scanner".

## FOUND — for P1-044, not fixed here

Measured in a network namespace with the shipped `apex.nft` loaded, three
namespaces wired client — router — server, `ip_forward=1` on the router. Each
result is paired with a positive control (mDNS on 5353 arriving) so that
"silent" cannot mean "the harness sent nothing".

1. **A Wi-Fi hotspot cannot work under this policy, in three independent ways.**
   NetworkManager's shared mode runs dnsmasq and NATs; all three legs are shut.
   - `forward` is `policy drop`, so no client packet is routed. Measured:
     forwarding succeeds before the ruleset loads and stops after it.
   - The only DHCP rule is `udp sport 67 udp dport 68`, the **reply**
     direction. A hotspot client broadcasts a DISCOVER to `dport 67`, a new
     flow, so `established,related` cannot rescue it. Measured: silent.
   - `udp dport 53` is shut, so dnsmasq resolves nothing for clients.
     Measured: silent.
   A fix wants interface-scoped rules on the shared link, which the port-only
   exception catalogue cannot express. This is adjacent to the card's item 4.

2. `nft add element inet apex allowed_tcp { 631 }` reopens 631 immediately, so
   the exception mechanism itself is sound. Verified for 445 and 2049 too.

3. **Two lines in `apex devices network` go stale the day item 1 is fixed.**
   The hotspot reader states, as facts about the shipped policy, that `forward`
   is policy drop and that the DHCP rule matches replies rather than a client's
   request. Both are true today and both are what P1-044 would change. Ping
   this branch when the policy moves and those two `blockers+=(…)` lines in
   `files/system/libexec/apex-devices` come out.

4. Not a defect, recorded so nobody chases it: katana's
   `apex-firewall.service` was `failed` at 04:38 AWST on 2026-09-07, with
   `nft: Could not open file "/run/apex-fw/nftables/apex.nft": Permission
   denied` and `Loaded: not-found`. That is P1-044's own live experiment, not
   the shipped unit. It did make `firewall_state`'s `failed` arm worth writing:
   `is-active` says `failed`, and calling that "the firewall is not running"
   would tell a user their machine is filtered by something else's default.

## Machine survey, L16 booted on the shipped image (2026-09-07)

Present: cups, cups-filters, sane-backends, sane-airscan, ipp-usb, hplip,
system-config-printer, cups-pk-helper, gvfs, gvfs-smb, gvfs-mtp, nfs-utils,
libmtp, libgphoto2, bluez, bluez-obexd, libldac, avahi, nss-mdns, colord,
udisks2, thunar-volman, NetworkManager, fwupd, usb_modeswitch, wsdd.

**Absent, and each one is an acceptance criterion.** All confirmed available
by `dnf5 repoquery --available`; `pipewire-codec-aptx` is RPM Fusion free,
which this image already enables, everything else is Fedora.

| Missing | What does not work |
|---|---|
| `gvfs-fuse` | every GIO mount (smb://, mtp://, dav://) is invisible to non-GTK apps — there is no `/run/user/UID/gvfs` at all |
| `gvfs-gphoto2` | cameras never mount. `gvfsd-gphoto2` is absent from the image |
| `gvfs-nfs` | NFS in the file manager |
| `cifs-utils`, `samba-client` | no `mount.cifs`, so no kernel SMB mount and no `smbclient` |
| `bolt` | nothing authorises a Thunderbolt device |
| `wireguard-tools` | no `wg`, no `wg-quick`, no way to import a WireGuard conf |
| every NM VPN plugin | openvpn, openconnect, vpnc, l2tp all absent |
| `dnsmasq` | NM shared-mode hotspot has no DHCP or DNS server |
| NM connectivity config | no captive-portal detection: NM never reaches `portal` |
| `pipewire-codec-aptx` | `/usr/lib64/spa-0.2/bluez5/` has aac, ldac, lc3, opus, g722, faststream, sbc — no aptX |
| any scanning application | only the `scanimage` CLI ships |

## Defects found live, not in any task description

1. **`scanimage -L` never returns on the shipped image.** It hangs after
   initialising hplip's `hpaio` backend (`SANE_DEBUG_DLL=4` shows it as the
   last one loaded; the process sits in `rt_sigsuspend` with no children and
   no sockets). Commenting `hpaio` out of `/etc/sane.d/dll.d/hpaio` makes it
   return in 7 s **and find a real scanner** — `airscan:w1:CANON INC. TR4600
   series` at 192.168.1.121 over WSD. So the image can scan and hplip stops it.
   Any scan UI that enumerates devices hangs at startup.

2. **`avahi-daemon.service` and `.socket` are masked** on the L16
   (`/etc/systemd/system/avahi-daemon.{service,socket} -> /dev/null`, 6 Sep
   08:41, owned by no package). `Containerfile.core:1040` builds the whole
   driverless-print story on the claim that "avahi is already installed AND
   enabled in this image (verified)", and no build step asserts it. A mask in
   `/etc` survives every image update and nothing reports it.

3. Thunderbolt on the L16: `domain0`/`domain1` are `security=user` with
   `iommu_dma_protection=1`. `security=user` means an attached device stays at
   `authorized=0` until something authorises it, and `bolt` is not installed,
   so a USB-C dock behind a TB controller is dead with no message.

4. `Containerfile.core:1003` says "there is no `gvfs-archive` subpackage".
   `dnf5 repoquery` finds `gvfs-archive-1.58.4-4.fc43`. The comment is stale.

## Notes for whoever picks this up

- **Do not edit `files/system/nftables/apex.nft` or
  `files/system/firewall/services`** — P1-044 owns both.
- `git checkout -- <path>` after a `git mv` restores from the **index**, which
  holds the old content under the new name. It silently reverted a rewritten
  file during a mutation run here. Mutation runs use `cp` backups instead.
