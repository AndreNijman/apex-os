# p1-062 — destructive chaos/torture reliability test infrastructure

Task: **P1-062** (`todo`). Repo: apex-os. Branch `task/p1-062-chaos`, from
`origin/roadmap/v2.2`. Worktree `/var/tmp/apex-work/wt-p1-062`.

`depends_on: P0-001`, which is `partial` and stays partial because it needs a
fresh install on hardware that is off-limits. That does not block building the
harness — it blocks *qualifying a machine with* it. Build the harness.

## Criteria, quoted from roadmap.yaml

1. Automated tests simulate power loss during update, full disk, network loss,
   shell/apexd crashes, expired cloud token, killed agent during checkpoint,
   monitor hot-unplug, suspend during work, and failed rollback.
2. Boot/update/install/rollback loops run continuously in VMs.
3. Failures produce reproducible diagnostics and do not silently corrupt the
   installation.

## What this program has already learned that belongs in this harness

Every one of these is a real incident from this repository's history, and each
is a chaos case that would have caught it:

- **A staged ostree deployment is discarded by a crash before clean shutdown.**
  "Updated but nothing changed" is that, and it is exactly criterion 1's power
  loss during update. The machine must come back on the old image and *say so*.
- **`apex install` builds a sysext that merges live**, and a hotfix overlay
  reverts on reboot while `apex-user.raw` persists. A reboot loop must assert
  which of those two survived.
- **A test that cannot run must say so.** Two assertions that could never run
  or never fail kept `roadmap/v2.2` from producing an image for five days. A
  chaos harness that reports "no corruption" when it never managed to inject
  the fault is the same defect with higher stakes — every case needs a verdict
  with a could-not-inject arm, not a boolean.
- **The runner is a different environment** from this laptop: OpenSSL 3.0.13 vs
  3.5.7, no `/etc/localtime`, no CAP_NET_ADMIN over a real netns. If a case
  needs a kernel interface, get it with `unshare --user --map-root-user --net`
  or say the case could not run.

## Constraints that shape the design

- **Headless, always.** No window on Andre's desktop. VMs run with no display;
  qemu with `-nographic`/`-display none`, or libvirt with no graphics device.
- **This laptop is the only machine.** katana is off-limits. GitHub CI is the
  second environment, and a "continuous" loop there is a scheduled workflow,
  not an infinite one — design for a bounded run with a resumable cursor.
- **Never touch the live machine's boot path.** No `bootctl install`, no
  `rpm-ostree` against the host, no `apex update` outside a VM. The image build
  already asserts that nothing in its payload does; your harness must be held
  to the same rule and should assert it about itself.
- There is a rollback staged on this laptop right now waiting for Andre to
  reboot (`ROADMAP/evidence/P0-001-rollback-staged.md`). Do not disturb it.

## NEXT

Round 2 agent, branch `task/p1-062-chaos-2`, worktree
`/var/tmp/apex-work/wt-p1-062b`. Round 1's worktree `/var/tmp/apex-work/wt-p1-062`
is superseded — its work was recovered onto the new branch. Do not message it.

**Pushed, three commits, four cases, all four inject a real fault and are
observed to. `4 survived, 0 failed, 0 could not inject`.**

- `ae0678db` — criterion 3: `tests/chaos/lib.sh`, `tests/chaos/run-chaos`, the
  three-way verdict, bundles, seeds, `--replay`. Cases
  `power-loss-during-update` (tears `last-update.json`) and `failed-rollback`
  (chmod 0000 on the deploy dir). Caught a `channel.rs` defect: `read_record`
  returned `Option`, so a torn record printed "nothing recorded yet" for a
  machine that had just updated.
- `10366f66` — the FOURTH verdict arm, `case_prove_exposed`: a fault can be
  real, proven, and irrelevant. `apex trust --verify` cannot be a network-loss
  subject because `verify.rs` short-circuits the registry under any fixture
  root. Also: `$APEX_BIN` is now a generated wrapper that refuses to run
  without a fixture root (exit 2), replacing a rule that was only a comment.
  Case `network-loss` drives `apex doctor` in three real network namespaces and
  caught the metrics probe reporting ENETUNREACH and ECONNREFUSED with the same
  sentence.
- `1db98e3a` — case `full-disk`: a real ENOSPC on a tmpfs in a mount namespace.
  The atomic write held; `qualify::save` left a 0644 `qualification.json.tmp`
  on the full filesystem, fixed with `OpenOptions::mode(0o600)` + removal on
  failure.

Every case mutation-tested with a NAMED red, restored with plain `cp`.

Still outstanding:
1. `tests/test-apex-chaos.sh` — the harness's self-test. `lib.sh` and
   `run-chaos` both already claim it exists. Two halves: a structural check
   that nothing under `tests/chaos/` names `bootctl|rpm-ostree|ostree admin|
   efibootmgr` or runs `apex update` outside a comment (with the inverse
   mutant, as `check-ai-parity --self-test` does), and synthetic cases in a
   temp `--cases-dir` that prove each verdict arm.
2. CI: `pr-validation.yml`. The userns/netns cases need
   `kernel.apparmor_restrict_unprivileged_userns=0` — reuse the existing
   bubblewrap sysctl step's job rather than adding a second one.
3. Criterion 2's loop. `files/scripts/boot-v2/run-scenarios`,
   `tests/test-boot-v2.sh` and `.github/workflows/boot-v2.yml` already exist
   and `run-chaos` borrowed its STAGED pattern from them — read those before
   writing any qemu. Bounded run with a resumable cursor, `CASE_STAGED=1`
   locally (kvm is present here, absent on CI).
4. `power-loss-during-update.sh`'s header forward-references a `chaos-loop`
   that does not exist yet. Either criterion 2 ships under that name or the
   comment gets reworded.
