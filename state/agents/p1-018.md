# P1-018 / P1-019 — MCP auth helper, per-MCP sidecar sandboxing

Branch: `task/p1-018-mcp-auth` in apex-os, worktree `/var/tmp/apex-work/wt-p1-018`,
forked from `origin/roadmap/v2.2` @ `9a24d2b`, plus a cherry-pick of
`chore/run-clippy` (`526e16c`).

Env every command in this task runs with:

```sh
export CARGO_TARGET_DIR=/var/tmp/apex-build-cache/p1-018
export XDG_STATE_HOME=/var/tmp/apex-p1-018-state XDG_CONFIG_HOME=/var/tmp/apex-p1-018-config
```

## What is already true before this task

P0-003 landed `apex mcp bridge` and `apex secret migrate`. The bridge satisfies
P1-018's first criterion on its own. What is missing is the verb a person uses
once for a server that has no plaintext to migrate, and anything at all for
P1-019.

## Findings the roadmap did not have

- `GITHUB_PERSONAL_ACCESS_TOKEN` in `~/.claude/settings.json` is 23 bytes of
  `[A-Z_]` shaped `REPLACE_WITH_ACTUAL_PAT`. A placeholder, never a token, so
  `plugin:github` has never authenticated once.
- That plugin's endpoint is `api.githubcopilot.com`, and `migrate.rs`'s
  `provider_for` pins any `GITHUB*` name to `github.com`. A migration would
  store the credential for a host the broker's pin can never carry it to.
- The plugin's server is defined in
  `~/.claude/plugins/cache/claude-plugins-official/github/unknown/.mcp.json`
  with `"Authorization": "Bearer ${GITHUB_PERSONAL_ACCESS_TOKEN}"`. Migration
  does not read that file, and removing the `env` entry it interpolates breaks
  the server silently.
- `claude-memory`'s bearer token is still in cleartext in `~/.claude.json` on
  this machine: migrate has never been run here.
- **`broker = true` bound the wrong socket.** `SandboxSpec::control_socket` is
  the *agent runtime's* socket (`$XDG_RUNTIME_DIR/apex-agentd/control.sock`) —
  `apex-agentd/src/session.rs:213` is the only other place that sets it, and
  `sandbox.rs`'s step 6 comment says so. `sidecar.rs` set it to
  `apex_secret_core::paths::socket()` instead, so a server with `broker = true`
  reached apex-secretd *directly* — a socket a confined agent session never
  gets — while `apex secret use` and `apex mcp bridge`, which both connect to
  **agentd**, still failed inside it. See the `feat(mcp): "may reach the broker"…`
  commit.

## Landed so far

- `9ef7…` `apex mcp list` + `apex mcp connect` + two migration defects.
- `bf083e7` a refused credential is exit 22 with the body attached (measured).
- `34016f8` P1-019's core: `apex mcp run` / `confine` / `policy`, `sidecar.rs`,
  `confine.rs`, and 4 live bwrap tests.
- `apex mcp list` run read-only against Andre's real config; it names the
  placeholder and reports 2 agent-readable MCP credentials.

Baseline on the untouched tip: **1512 passed, 0 failed**.
After `34016f8` plus the two uncommitted files: **1548 passed, 0 failed**.

## NEXT

1. Commit the two files already in the tree (`mcp.rs`, `mcp/servers.rs`) — a
   `sandbox` line in `apex mcp list`, `describe_transport` unwrapping the
   `apex mcp run` wrapper, and `shape_of` dropping the `todo`/`abc123` markers.
   They need tests: none of the three is pinned. Also `as_json` has no sandbox
   field, so `--json` and the text listing disagree.
2. Fix the `broker = true` socket (see the finding above): bind
   `apex_agent_core::paths::control_socket()`, resolve it through
   `sandbox::real_target` the way `session.rs:369` does, and *stop* binding
   secretd's. Live test: `apex mcp run` a confined `apex mcp bridge`.
3. Decide the `"*"` project grant. Leaning: implement it, but only behind an
   explicit flag on `apex secret grant`, only for an operation whose spec is
   `ResourceKind::None` with no params (which `mcp.request` is and `git.push`
   is not), enforced in `service.grant` daemon-side rather than in the CLI, and
   `"*"` must NOT match `project: None` — `grants_fail_closed_without_a_project`
   stays true.
4. `tests/run-clippy.sh` before declaring done. Clippy is on neither machine.
