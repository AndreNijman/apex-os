# P1-018 / P1-019 — MCP auth helper, per-MCP sidecar sandboxing

Branch: `task/p1-018-mcp-auth` in apex-os, worktree `/var/tmp/apex-work/wt-p1-018`,
forked from `origin/roadmap/v2.2` @ `9a24d2b`, plus a cherry-pick of
`chore/run-clippy` (`526e16c`).

Env every command in this task runs with:

```sh
export CARGO_TARGET_DIR=/var/tmp/apex-build-cache/p1-018
export XDG_STATE_HOME=/var/tmp/apex-p1-018-state XDG_CONFIG_HOME=/var/tmp/apex-p1-018-config
```

## What is already true before this task

P0-003 landed `apex mcp bridge` and `apex secret migrate`. The bridge satisfies
P1-018's first criterion on its own. What is missing is the verb a person uses
once for a server that has no plaintext to migrate, and anything at all for
P1-019.

## Findings the roadmap did not have

- `GITHUB_PERSONAL_ACCESS_TOKEN` in `~/.claude/settings.json` is 23 bytes of
  `[A-Z_]` shaped `REPLACE_WITH_ACTUAL_PAT`. A placeholder, never a token, so
  `plugin:github` has never authenticated once.
- That plugin's endpoint is `api.githubcopilot.com`, and `migrate.rs`'s
  `provider_for` pins any `GITHUB*` name to `github.com`. A migration would
  store the credential for a host the broker's pin can never carry it to.
- The plugin's server is defined in
  `~/.claude/plugins/cache/claude-plugins-official/github/unknown/.mcp.json`
  with `"Authorization": "Bearer ${GITHUB_PERSONAL_ACCESS_TOKEN}"`. Migration
  does not read that file, and removing the `env` entry it interpolates breaks
  the server silently.
- `claude-memory`'s bearer token is still in cleartext in `~/.claude.json` on
  this machine: migrate has never been run here.
- **`broker = true` bound the wrong socket.** `SandboxSpec::control_socket` is
  the *agent runtime's* socket (`$XDG_RUNTIME_DIR/apex-agentd/control.sock`) —
  `apex-agentd/src/session.rs:213` is the only other place that sets it, and
  `sandbox.rs`'s step 6 comment says so. `sidecar.rs` set it to
  `apex_secret_core::paths::socket()` instead, so a server with `broker = true`
  reached apex-secretd *directly* — a socket a confined agent session never
  gets — while `apex secret use` and `apex mcp bridge`, which both connect to
  **agentd**, still failed inside it. See the `feat(mcp): "may reach the broker"…`
  commit.

## Landed so far

- `9ef7…` `apex mcp list` + `apex mcp connect` + two migration defects.
- `bf083e7` a refused credential is exit 22 with the body attached (measured).
- `34016f8` P1-019's core: `apex mcp run` / `confine` / `policy`, `sidecar.rs`,
  `confine.rs`, and 4 live bwrap tests.
- `apex mcp list` run read-only against Andre's real config; it names the
  placeholder and reports 2 agent-readable MCP credentials.

Baseline on the untouched tip: **1512 passed, 0 failed**.
After `34016f8` plus the two uncommitted files: **1548 passed, 0 failed**.

## Landed since (pushed, `c38075f`)

- `28e4c86` the listing: `describe_transport` unwraps the `apex mcp run`
  wrapper, a `sandbox` line beside `credential`, `--json` gains `confined`, and
  `shape_of` matches `todo`/`insert`/`abc123` as delimited **words** instead of
  deleting them (deleting fixed the false positive and gave up `TODO`,
  `abc123` and `INSERT YOUR TOKEN HERE`, all of which are placeholders).
  Six mutations, red/green: 346/1 → 347/0, six times.
- `c38075f` the broker socket fix + `control_socket_in` + two live tests that
  run the shipped binary and complete a real MCP `initialize` handshake inside
  bwrap. Three mutations, red/green: M7 352/3, M8 353/2, M9 352/3 → 355/0.

Workspace: **1555 passed, 0 failed** at `c38075f`.

- `8cb4e77` the live-test helper folded into a `Sidecar` struct: eight
  arguments, four `&Path` in a row, and `clippy::too_many_arguments` would have
  failed the container run on it.
- `853475a` **the `"*"` project grant, decided and implemented.** `--everywhere`
  on `apex secret grant`/`revoke`, gated daemon-side on
  `OperationSpec::names_nothing()` (no resource, no parameters — `mcp.request`
  is the only shipped operation that qualifies; `git.push` is refused with a
  sentence saying why). `"*"` is checked *after* the named project and never
  matches `project: None`, so `grants_fail_closed_without_a_project` is
  untouched; on the `Use` path a claimed project must already be absolute, so
  `"*"` can only be a key somebody wrote, never a claim a caller makes. Five
  mutations, red/green: M10 1559/2, M11 1560/1, M12 1560/1, M13 1561/1 (after a
  synthetic spec was added to pin it — it survived at first, because no shipped
  operation is `ResourceKind::None` *with* params), M14 1560/1 → 1562/0.

Workspace: **1562 passed, 0 failed** at `853475a`.

- `1314abc` four `clippy::cloned_ref_to_slice_refs` in `connect.rs`'s tests.
- `16fc8ae` `tests/run-clippy.sh` could not run on katana at all — see below.

Clippy: **clean, exit 0**, `tests/run-clippy.sh` at `16fc8ae` with `target-clippy`
deleted first, so the whole workspace was checked and not only the changed crate.
Container on katana (Steam client running there and on the L16; katana's load
average was 0.08 on 20 cores, no game, so the build went there rather than onto
Andre's desktop).

## A defect in `tests/run-clippy.sh`, found running it — fixed in `16fc8ae`

The script's `podman run` had no `--network=host`, and on katana the container
gets no DNS: `rustup component add clippy` could not reach `static.rust-lang.org`
— `docker.io/library/rust:1` does **not** ship clippy, it downloads it. The image
pull works either way, because that goes through the host, so the only place it
showed was the `||` branch, whose `>/dev/null 2>&1` had already thrown away
`Temporary failure in name resolution` and replaced it with "could not add the
clippy component". Reads like a broken image; is a broken network. Both halves
fixed on this branch. If `chore/run-clippy` fixes it independently, drop this
commit at integration.

## NEXT

1. Named remainder, not started: `sidecar.rs`'s module doc says a session can
   rewrite `~/.claude.json` to drop the wrapper, and closing that means
   launching the agent with `--strict-mcp-config --mcp-config <a file the
   daemon wrote>`. That is a change to how sessions start, not to this module.
2. Nothing else. P1-018 and P1-019 both meet their acceptance criteria.
