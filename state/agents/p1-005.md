# p1-005
items: P1-005, P1-006, P1-007
repo: apex-os
worktree: /var/tmp/apex-work/wt-p1-005
branch: task/p1-005-cloudflare-caps
based on: origin/roadmap/v2.2 @ fbf06a4f

## NEXT
Confirm the documented Cloudflare REST paths for R2 / D1 / KV / Queues /
Hyperdrive / DNS from public docs (no account, no auth), then extend
`apexd/apex-secret-core/src/providers/cloudflare/binding.rs` with the resource
kinds §13.1 does not yet bind (d1, kv, queues, hyperdrive) before touching SPEC.

## DONE
- Worktree created and branch pushed: `task/p1-005-cloudflare-caps` @ fbf06a4f.

## IN PROGRESS
- Orientation only. Nothing written yet.

## FOUND
- **The three units share one file and one test table.** P1-005/006/007 are 16
  of §13.2's 32 names, all in `apexd/apex-secretd/src/providers/cloudflare/`.
  `tests.rs::the_declaration_is_well_formed_and_every_name_is_one_section_thirteen_two_lists`
  asserts hard counts (`from_13_2 == 6`, `SPEC.operations.len() == 7`,
  `SECTION_13_2.len() - from_13_2 == 26`) — those three numbers must move with
  every operation added, so they are a built-in arithmetic check on the unit.
- `binding.rs` already has `buckets` + `Binding::bucket()` + `BindingError::NoBucket`,
  written ahead of P1-005 and `#[allow(dead_code)]` until an operation reaches
  it. So R2's semantic half exists; only the operations and the calls are new.
- No `elevated` class exists in `apex-secret-core`: `Effect` is `{Read, Write}`
  only. P1-007's "registrar/nameserver/DNSSEC-root changes require elevated
  class" has nowhere to land yet — design decision pending.
- House standard for verdicts is `apexd/apex/src/verify.rs::Verdict` —
  `Verified / Absent / Failed / CouldNotRun`, four values because "the registry
  answered and holds nothing" and "the registry did not answer" are different
  measurements.

## BLOCKED ON
- nothing
