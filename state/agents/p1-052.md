# p1-052 — APEX Remote remainder: relay, reconnect, mDNS, quality, shell pages

Unit: `p1-052-relay`. Tasks: **P1-051** (partial — the two apex-shell pages
only), **P1-052** (relay + reconnect + mDNS + connection quality).

Repos: **both**.
- apex-os — branch `task/p1-052-relay`, forked from `origin/roadmap/v2.2`
  @ `4f80746e`. Worktree `/var/tmp/apex-work/wt-p1-052`.
- apex-shell — branch `task/p1-052-relay`, forked from `origin/roadmap/v2.2`
  @ `cd324ec`. Worktree `/var/tmp/apex-work/wt-p1-052-shell`.

Both branches pushed with `-u` before any work. **No rebase.**

## What this unit is, and is not

Successor to p1-050 (landed as apex-os `1ab542c`, P1-050 done). Five things
were left:

1. No Cloudflare Worker / Durable Object relay — `apex_remote_core::rendezvous`
   derives the id, nothing dials a server; the server side does not exist.
2. No reconnect across network changes.
3. No mDNS discovery (`_apex-remote._tcp`).
4. Connection quality is measured nowhere and rendered nowhere.
5. apex-shell owes a QR pairing page and a paired-device page.

**Not this unit, and P1-051 does not close from here:** `--origin-policy remote`
stays refused until the device key lives in Android Keystore with
`setUserAuthenticationRequired`, a per-approval challenge and attestation. That
is p1-053a; `p0-014`'s CTAP2 verifier is its desktop half.

## The deployment constraint

**Nothing is deployed to Andre's Cloudflare account.** No `wrangler deploy`, no
real Worker, no real Durable Object, no live account auth. The relay's server
side is written as code and proven against a **local double**, the pattern
already in this tree (the P1-005 Cloudflare capability sets run entirely against
a loopback transport that 401s unauthenticated requests; the trust gate was
proven against a minted CA, not production sigstore).

A correct outcome for this unit is "the relay is written, tested against a
double, and ready to deploy". What deploying would actually require is recorded
under **Deploying, when Andre wants it** below.

## The design, settled before the first commit

**The relay carries a byte stream, and nothing above it changes.** The LAN
transport is `[u32 BE length][Noise ciphertext]` over `TcpStream`
(`apex-remoted/src/net.rs`). A relay leg is a WebSocket whose binary payloads
carry that same byte stream, so `read_message`/`write_message`, the Noise
channel and every `Frame` ride over it unaltered. "The relay copies bytes it
cannot read" is then a structural property, not a promise.

**The relay leg terminates on the daemon's own loopback, not in `serve.rs`.**
Checked rather than assumed: `grep -n 'loopback\|127\.0\.0\.1\|is_loopback'`
over `apex-remoted/src/{serve,main}.rs` is empty, so nothing refuses a loopback
peer. The relay thread dials the relay outbound, and when a guest attaches it
connects to this daemon's own listener and splices. `serve::connection` keeps
its `TcpStream` and its whole session path untouched — which matters with
p0-014 in flight on the same tree — and the alternative (making `connection`,
`session`, `frames` and `Sealer` generic over a stream trait) is a six-signature
refactor bought for nothing.

Path labelling is race-free by ordering, not by luck: the splice registers its
own loopback source port in `State` **before** it writes the hello byte, and
`connection` reads the hello byte first, so the lookup cannot run early.

**No TLS crate enters the workspace.** `apex-secretd/src/providers/cloudflare/
api.rs` already settled this question for the tree — it shells out to
`curl -q -K -` and says so: "`api.cloudflare.com` is TLS, and nothing in this
workspace speaks TLS." The WebSocket client is therefore generic over
`Read + Write`; plain TCP is what the double and every test use.

**Relay defaults to off, and that is already true.** `apex-remoted --relay
<url>` is optional, `State.relay` and `PairingOffer.relay` are
`Option<String>`, and `files/system/units/apex-remoted.service` passes no
`--relay`. The image therefore ships pointing at no relay and LAN keeps
working. Pinned with a test rather than left as an observation.

**Protocol decisions taken before the double was written**, so the double and
the Worker cannot drift:

- **Roles are explicit.** The desktop dials `?role=host`, a device
  `?role=guest`. A relay that paired "the first two arrivals" would join two
  phones to each other and let them discover that only when Noise failed.
- **The host re-arms.** The moment a guest attaches, the desktop opens a fresh
  waiting connection, so one desktop serves more than one session at a time.
- **An impostor host is a denial of service, not an impersonation.** Anyone who
  has seen the QR knows the rendezvous id and can dial as host for it. They do
  not have the static private key, so `Noise_IK` fails for them. Written into
  the threat model as a decision rather than left to be discovered.

## NEXT

**Everything dispatched to this unit is done.** P1-052 is complete; P1-051's
two apex-shell criteria are complete and it still does **not** close from here
— see the note at the top, `--origin-policy remote` is p1-053a's.

What a successor would pick up, none of it dispatched here:

- The relay is written and tested against a double, **never deployed**. See
  **Deploying, when Andre wants it** below; the one blocker is that
  `apex-remoted` refuses a `wss://` relay by name and `*.workers.dev` is
  HTTPS-only. That is a dependency decision for Andre, not a bug.
- `tests/run-remote-pairing-page-test.sh` and `tests/qr-test.js` are wired into
  `ci.yml`, but CI has never run on this branch — no PR was opened, per the
  dispatch.

### What the OS side actually offers, checked rather than assumed

- `apex remote pair --text` prints **only** the `apex-remote:` payload on
  stdout; the human sentences go to stderr. Without `--text` it prints
  `qr_block`'s prose as well, so the page wants `--text`.
- `apex remote devices --json` → the `Device` array: `id`, `name`,
  `public_key`, `paired_ms`, `last_seen_ms`, `revoked_ms`,
  `requires_user_verification`, `last_path`. `state()` is `revoked` or
  `paired`, derived from `revoked_ms` alone.
- `apex remote revoke <id>`, `apex remote status --json`. There is **no**
  `pair --json`.

### Criteria, quoted from roadmap.yaml rather than paraphrased

1. "Desktop can display a QR code / short pairing flow from APEX Shell."
3. "Lost devices can be revoked from APEX Settings."
5. "Paired-device list shows name, last seen, permissions, and revocation
   state." — not named in the dispatch, but it is what a paired-device page
   *is*, and the fields are all in `Device`. `requires_user_verification` is
   the "permissions" one, and it is the device's own claim: the CLI says so in
   a sentence rather than a column, "a tick in a table would read as a fact
   this machine had verified". The page must not turn it into a tick.

## Log

- 2026-09-12 — card created; both worktrees made off the current tips
  (`4f80746e` / `cd324ec`) and both branches pushed. The `claude-memory`
  MCP server is 502 for this whole session, so `memory_boot` did not run
  and no journal entry can be appended from here.
- 2026-09-12 `c0445ea0` — `apex_remote_core::relay`: endpoint parsing, the
  two roles, and a hand-rolled RFC 6455 client over any `Read + Write`. 23
  tests. `sha1` becomes a direct dependency of the crate and **not** a new
  crate in the tree — snow already had 0.10.7 in the lock. Four mutations,
  all red: `accept_for` on the crate's URL-safe alphabet 21/2; a constant
  frame mask 22/1; a masked server frame unmasked rather than refused 22/1;
  the accept value never compared 22/1.
- 2026-09-12 `5316dc3d` — the daemon dials the relay and splices it onto
  its own listener; the host re-arms on pairing; `State::path_of` replaces
  the literal `"lan"` every session used to be recorded as; `wss://` is
  refused by name rather than dialled in the clear.
  `apex-remoted/tests/relay.rs` drives pairing **and** a session through a
  loopback relay double, and asserts the recorded payloads carry none of
  the session. apex-remote-core 78, apex-remoted 31 + 10 + 6. Four
  mutations, all red: `serve.rs` records `"lan"` for everything again 5/1;
  the splice never arms its source port 5/1; the supervisor serves one
  device and never re-arms 3/3; the double records none of what it copies
  5/1.

- 2026-09-12 `704743fb` — the desktop measures its own connections
  (`Frame::Ping` every `--ping-interval-ms`, 15 s shipped, clamped at 50 ms),
  `rendezvous::Quality` turns a round trip into the word a person is shown,
  `rendezvous::Connection` carries device + path + since + rtt as **live**
  state that is never written to the device store, and
  `apex remote status [--json]` renders it. Two new suites: the measurement
  with a negative control, and a network cut mid-session. apex-remote-core
  82, apex-remoted 31 + 10 + 8, apex 529.

  **A mutation SURVIVED and was fixed.** "any token is timed, not only one
  this end sent" passed every test, because the control only showed that a
  device answering *nothing* got no measurement — nothing ever sent a
  *forged* token. A device that could name its own token could report any
  quality it liked, including a good one for a connection that is unusable.
  The suite now sends `Pong { token: 0xdeadbeef }` and the same mutant is
  7/1. The other three: the `Pong` arm does nothing again 7/1; an unmeasured
  connection reports the worst band 8/1; the band is read off the path
  instead of measured 8/1.

  **A requirement on the Worker, found by the double.** A relay that parks a
  waiting host and never notices its socket has died answers the desktop's
  next dial with 409 for ever, and the machine is unreachable until the
  daemon restarts. The first version of the double did exactly that and the
  reconnect suite caught it. The Durable Object must evict too.

- 2026-09-12 `e028a546` — `relay/`: the Worker and the Durable Object, plus
  `ROADMAP/design/P1-052-relay.md`. Hibernation rather than `server.accept()`,
  no `ctx.storage` call anywhere, occupancy derived from `getWebSockets()`
  every time (the eviction requirement), refusals before the upgrade, and a
  pinned compatibility date. Rules in `room.js` under `node --test` (9, no
  dependencies, no build step); `index.js` **has never been executed** and the
  note and README say so. A Rust test reads `room.js` and pins the three
  notices and two role words against the enum. Four mutations, all red: a
  second host displaces the first 7/2; a missing role defaults to host 8/1;
  the id alphabet accepts a path separator 7/2; the notice drifts from the
  enum — caught by **both** node 8/1 and rust 25/1.

- 2026-09-12 `4cc8cb0c` — mDNS. `apex-remoted` announces
  `_apex-remote._tcp` by holding an `avahi-publish-service` child for as long
  as it runs — **not** a static `/etc/avahi/services/` file, which would
  advertise the machine whether or not the service was running.
  `PR_SET_PDEATHSIG` so a `SIGKILL`ed daemon does not orphan a publisher
  advertising a service that is gone. TXT carries the protocol version and
  **not** the rendezvous id, which is stable across networks. The record names
  the port the daemon is on, not the catalogue default. `--no-announce` opts
  out. `Containerfile.core` now names `avahi-tools` with a `command -v` beside
  it. Tests run against a stub reached only via `APEX_REMOTE_AVAHI_PUBLISH`,
  so no packet leaves the machine. Three mutations, all red: the catalogue
  default instead of this port unit 3/1 + e2e 2/1; the rendezvous id in the
  TXT record 3/1; the child not tied to the parent's death 2/1.

  **A child process, not zbus.** `apex-remoted`'s whole dependency list is
  `apex-remote-core, apex-agent-core, anyhow, serde, serde_json, libc` — no
  zbus and no tokio — and adding both to build the same `EntryGroup` the tool
  builds is the trade `net::local_addresses` already refused when it called
  `getifaddrs`, and `apex-secretd` refused when it shelled out to `curl`.
  Checked first: avahi is installed **and enabled** in the image
  (`Containerfile.core` fails the build if `systemctl is-enabled` disagrees),
  and `avahi-publish-service` is present on the L16 that runs it.

## Deploying, when Andre wants it

Full version with commands and cost: `ROADMAP/design/P1-052-relay.md` §5.
The short form:

**One blocker first.** `apex-remoted` refuses a `wss://` relay by name, because
nothing in the apexd workspace speaks TLS and `*.workers.dev` is HTTPS-only.
Deploying the Worker today gives a relay that works and a desktop that will not
dial it. Three ways out, and the choice is Andre's because it is a dependency
decision: add `rustls` + `webpki-roots` (~15 crates in a workspace that has
deliberately had none); tunnel through an `openssl s_client` child (zero
crates, same shape as the `curl` child `apex-secretd` already uses); or a
custom domain with "Always Use HTTPS" off so `ws://` on port 80 reaches it
(zero code, and it gives up TLS's cover of the rendezvous id from an on-path
observer — a different party from the relay operator the disclosure already
names).

**Then:** `cd relay && npm install --save-dev wrangler && npx wrangler login &&
npx wrangler deploy`, then `--relay <the printed URL>` on
`apex-remoted.service` per machine.

**Cost:** Durable Objects are on the free plan when SQLite-backed, which the
migration declares; this Worker stores nothing so no row is ever read or
written. Free plan is 100,000 requests/day and 13,000 GB-s/day; paid is $5/mo
with 1 M requests and 400,000 GB-s included. Hibernation means an idle waiting
connection is not billed for duration at all, and incoming WebSocket messages
bill 20:1. One laptop and one phone sit inside the free plan.

**The step I would take before deploying:** `npx wrangler dev --local` with
`cargo test -p apex-remoted --test relay` pointed at it instead of its double.
That is what turns `index.js` from reviewed code into run code.

## Measurement notes

`cargo test --locked --workspace` hung on this machine for twenty minutes in
`apex-agentd`'s `pty::tests` and `egress::tests`, and it was **not** a login
session problem — `tests/in-login-session.sh` fixes failures, not hangs.
Checked rather than guessed: `cargo test -p apex-agentd --bin apex-agentd
pty::` on its own is **15 passed in 0.21s**. The hang was contention with
another agent running `cargo test -p apex-agentd` in `/var/tmp/apex-work/
wt-base-os` at the same time; two leaked `apex_agentd-*` test binaries from
the killed run were still holding PTYs afterwards and were killed **by pid**
(never by name — `pkill apex-agentd` on this machine would take out the live
daemon).

Anyone re-measuring this branch should check `pgrep -af apex_agentd-` first.

- 2026-09-12 `373e833c` (apex-os) — three clippy lints in the relay's own
  test code. clippy was not installed when the relay landed; it is now
  (0.1.98 / rustc 1.98.0) and is clean on the integration tip, so all three
  were mine. `while let` for the double's copy loop, two `&vec![0u8; N]`
  that are `&[0u8; N]`. The 65536 case beside them is left as `vec!`
  deliberately — clippy does not flag it, and a 64 KiB array does not go on
  a test thread's stack to satisfy a lint nobody raised.
  `cargo clippy --locked --workspace --all-targets -- -D warnings` exits 0.
  apex-remote-core 83, apex-remoted 35 + 3 + 10 + 8.

- 2026-09-12 `673efe0` (apex-shell) — the QR encoder, ISO/IEC 18004 byte
  mode, versions 1–40, all four levels. `tests/qr-test.js`, 81 assertions,
  compares every module of 36 segno 1.6.6 symbols. Wired into CI and into
  `structure-check`'s `REQUIRED`.

  **The fixtures caught a real defect before CI ever saw it.**
  `alignmentPositions` used `Math.ceil` where the specification truncates,
  so every version from 7 up put its alignment patterns in the wrong
  columns — 21 of 36 cases, including both pairing offers. Versions 2–6
  have two alignment centres and never use the step, which is why it looked
  right on small symbols. Annex E's table is now transcribed into the test.

  **The oracle is patched, and that is the part worth reading.** segno
  1.6.6 deviates from §7.4.10: `write_padding_bits` quotes the clause in a
  comment and the code drops the condition in it, so it appends 8 zero bits
  when the stream already ends on a codeword boundary — a spurious `0x00`
  where the `0xec`/`0x11` pad codewords belong. In byte mode that is the
  only case, never an edge one: 4 mode bits plus an 8- or 16-bit count means
  the 4-bit terminator always lands exactly on a boundary. Invisible to a
  scanner, which stops at the terminator; not invisible to a byte-for-byte
  comparison. Without the correction this encoder would have to reproduce a
  bug to pass. Three things keep that honest, all checked rather than
  claimed: the generator builds every case twice and **derives**
  `_iso_corrected` by diffing; the test pins that list at exactly the four
  padded cases, so a spreading correction *or* an upstream fix reds it; and
  §7.4.10 is asserted directly off the codewords, so those assertions hold
  with every fixture deleted. The other 32 fill their symbol to capacity and
  provably cannot be reached — asserted, not stated.

  Five mutations, all red: alignment rounds up 21/60; the first pad
  codeword is `0x00` as segno emits 8/73; `FORMAT_BITS` has L and M the
  usual way round 31/50; version 32's special case dropped 2/79; the
  fixtures regenerated with stock segno 6/75. The fourth is why the Annex E
  table earns its place — no fixture is at version 32, so nothing else
  catches it.

- 2026-09-12 `cc0115e` (apex-shell) — **the two pages, P1-051 criteria 1, 3
  and 5.** `remotepairing.js` (pure logic) + `RemotePairingService.qml` (the
  four `apex remote` commands) + `QrCode.qml` (Canvas) + `RemotePairPage` +
  `RemoteDevicesPage`, registered in all three places the tree requires.

  **Pairing is not a read, and that shaped the design.** `apex remote pair`
  mints a ONE-TIME token and arms the daemon for the next device presenting
  it. Every other settings page of this shape sweeps on a timer; this one
  cannot. A code is minted exactly three times — the page first coming on
  screen with no live offer, the one on screen expiring, and the person
  asking — and never by a timer, because each new one invalidates the code
  somebody is at that moment holding their phone up to. Asserted against the
  stub's call log rather than argued: zero before the page is shown, one
  after, still one after leaving and returning, two after asking.

  The same fact is why everything goes through the `apex` CLI and not
  `control.sock`: `headless_begin` stubs `apex`, and a socket client would
  walk past the stub and mint a real token on whatever machine ran the suite.

  **The page draws nothing when it has no offer** — not a placeholder, not a
  greyed square, not a stale code. The `pair-fail` phase's central assertion
  is a negative one, because that regression looks perfectly fine in a
  screenshot.

  **A real defect, found by the suite and not by reading.** The page asked
  for a code from `onOnScreenChanged` while the `ServiceRef` beside it raises
  `refCount` from a binding on the same property. QML orders neither, and the
  handler ran first: `requestCode` saw an inactive service, returned, and the
  page showed no code at all. Now asked for on both paths; `ensureCode` is
  idempotent while an offer is live.

  Also: the QR is `Theme.fixedDark` on `Theme.fixedLight` (a wallpaper-derived
  palette can land on two mid-tones a camera cannot separate) with the
  four-module quiet zone painted rather than left to the background; the
  expiry is decoded out of the payload rather than scraped from `apex remote
  pair`'s stderr prose, which doubles as the check that a truncated read is
  refused before a QR is drawn from it; and `requires_user_verification` is
  one sentence naming the devices, never a per-row tick, with the node suite
  pinning the exported surface so nothing a row could draw can be added
  without failing there.

  remote-pairing-test 71. Page suite 3 phases, 20 + 8 + 22 = 50 assertions
  in the engine, **exact green counts rather than floors** — which caught two
  of my own counts being wrong on the first green run. Two `ci.yml` steps and
  eight `structure-check` paths.

- 2026-09-12 — **mutation protocol correction applied.** The coordinator's
  note that `cp -p` preserves mtime (so cargo skips the rebuild and the next
  verdict is taken against the mutant's binary) does not affect anything I
  took: every restore here was a plain `cp`, and the shell side has no
  compile step at all. Verified rather than assumed — each restored file was
  sha256'd against its backup and `git diff` was empty.

  The inherited Rust battery was **re-taken in part** rather than trusted.
  The highest-value pair — "any token is timed, not only one this end sent",
  the one `704743fb` records as having SURVIVED and been fixed — reds at
  exactly 7/1 again, with a `Compiling apex-remoted` line proving a real
  rebuild on both the mutation and the restore, and the restored file
  byte-identical. The rest of the predecessor's pairs are inherited, not
  re-measured.

  **Six mutations on the pages, all red; control green; all four files
  sha256-identical to their backups afterwards.**

      ensureCode mints even when a live code is on screen   pair-ok  18/20, 2 failed
      a placeholder QR is drawn when there is no offer      pair-fail 6/8,  2 failed
      the QR is given a truncated payload                   pair-ok  16/20, 4 failed
      a revoked device still offers a revoke button         devices  21/22, 1 failed
      looking at the device list mints a pairing code       devices  21/22, 1 failed
      connected and revoked share a weight (hue only)       node 1 failed + devices 21/22

  The truncation mutant is the one worth keeping: it was caught by the
  constant derived from the payload's byte length rather than from the
  encoder — "a 275-byte offer at level m is a 65-module symbol (got 45)",
  "version 12 (got 7)". An assertion that only compared the page's matrix to
  `QR.encode(payload)` would have agreed with itself and passed, because both
  sides would have been the truncated payload. The last mutant is caught
  twice, by the node suite and by the engine.

  Every mutant also dropped the phase's assertion COUNT, so the exact-count
  rule caught the vanishing as well as the failing.
