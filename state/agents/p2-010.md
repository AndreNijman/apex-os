# p2-010 — hardware, storage, firmware and agent resource budgets

Items: **P2-010** hardware qualification database, **P2-011** agent cgroup
resource budgets, **P2-014** APEX Storage Manager and disk-health UX,
**P2-015** firmware and hardware lifecycle.

Repo: apex-os. Worktree `/var/tmp/apex-work/wt-p2-010`,
branch `task/p2-010-hardware-storage`, forked from `origin/roadmap/v2.2` @ `4b7f84d`.

## NEXT

**Round 11. `budget.rs` and its tests are WRITTEN, committed and pushed** —
`1086478` (feat, 1213 lines across `budget.rs`, `main.rs`, `session.rs`) and
`84aea0a` (test, 520 lines in `tests/budget.rs`). HEAD `84aea0a`, clean,
ahead=0. The previous NEXT said "write budget.rs"; that was one step stale and
this is the correction.

**What is actually left, and it is only proof:**

1. A mutation battery for the budget logic — there is none yet, and every
   other item in this unit earned its verdict from one.
2. `cargo test --locked --workspace --no-fail-fast` for a real count, from a
   login-session cgroup (this worktree's owner is in
   `/user.slice/user-1000.slice/session-4.scope`, where the card has already
   shown the `system_grants.rs` failure does not reproduce).
3. `tests/run-clippy.sh <pushed sha>` — **not** `clippy-local`, which pins
   rust 1.97 and missed a `clippy::redundant_guards` clippy 1.98 caught.
4. The per-item verdict for all four items in this card.

The live half of `tests/budget.rs` is opt-in behind `APEX_BUDGET_LIVE=1` and is
deliberately not in `pr-validation.yml`. Nothing needs wiring into that file:
the `Tests` step is a bare `cargo test --locked` inside
`tests/in-login-session.sh`, so the six CI-safe budget tests ride in with the
workspace, and CI never sets `APEX_BUDGET_LIVE`. **They have not actually run
in CI yet** — `pr-validation.yml` on this branch triggers only on
`pull_request` to `main` and this branch has never been in one. See
`## Verdict, per item` for what `gh run list` says about the orchestrator's new
push trigger. Do not edit that workflow file from this worktree; the
orchestrator is editing it.

Design, all of it measured (see `## Measured, sixth pass`) and all of it now
built:

- Config surface is `Config.extra["budget"]`, which works because `extra` is
  `#[serde(flatten)]` — a top-level `budget` key in `agent.json` lands there
  and `apex-agent-core` is not touched at all.
- Default is **no budget → no wrapper → argv byte-identical to today**, so the
  four existing integration tests are unchanged.
- Delegation boundary is `…/user@<uid>.service/app.slice/cgroup.controllers`,
  derived from the daemon's `/proc/self/cgroup`. **Never `subtree_control`.**
- Polarity, and it is this unit's recurring sentence: a controller that was
  read and is **absent** (`io`) is a fact → drop it from argv, keep a note,
  never a zero. A controllers file that could **not be read** is an unknown →
  refuse the session.
- Scope name `apex-agent-<daemon pid>-<session id>`, validated at
  construction; must not begin `session-` (FOUND sixth) and must not contain
  `/`. The pid is in it because two daemons with different `XDG_STATE_HOME`s
  both mint session id 1 and would collide on one user manager.
- Disposable session + configured budget → refuse, naming why. Wrapping the
  disposable ENGINE would budget the container client, not the agent.

**Done, in `84aea0a`:** `apexd/apex-agentd/tests/budget.rs` (harness copied
from `system_grants.rs`, and it does set `XDG_CONFIG_HOME`; the live half
symlinks `systemd/private` from the real runtime directory).

**The scratchpad fwupd captures did NOT survive** — the card warned they
would not, and by 2026-09-08 that directory held only `report-r8.txt`. Nothing
was lost: the predecessor had already reproduced every measured shape as
inline miniatures, in `apexd-core/src/firmware.rs`'s `REAL` const (7 rows) and
`tests/test-apex-firmware.sh`'s `devices_on_battery()` (12 rows). Between them
they carry the absent `Name` key, the absent `Version` key, the hex version,
two rows sharing a name, four Secure Boot plugins, a `uefi_capsule` row that
must not be filtered with them, and `require-ac-power` on a row that
consequently has no `updatable` flag. **Do not go looking for the 37 KB files;
they are gone and they are not needed.** If a future round wants the real
document back: `fwupdmgr get-devices --json` answers as `andre` with no session
bus and no prompt.

Superseded plan, kept only because its detail is still the specification of
what got built:

1. Shell suites for the erase path. Two places, and they are different jobs:
   - **`--with-binary` (CI-safe) is where seven of the refusals actually get
     exercised**, via `.fixture/euid`. Against the fixture machine: p5 ->
     `Mounted`, `nvme0n1` -> `CarriesTheRunningSystem`, p1 -> `IsTheEsp`,
     `sdz` -> `NotAKnownBlockDevice`, wrong token -> `Unconfirmed`, euid 1000
     -> `NeedsRoot`, no euid file -> `CouldNotVerify`, unreadable mountinfo ->
     `CouldNotVerify`, a `holders/dm-0` entry -> `HasHolders`, a written
     `loop*/loop/backing_file` -> `LoopDeviceInUse` / mismatch / pass-through,
     `chmod 100` on a disk dir -> the enumeration refusal. Each asserts
     **exit 3** and the arm's own sentence. Plus the fixture-gate positive:
     permit + "would erase" + exit 0.
     **Name the fixture's permittable loop device something the host does not
     have** (`loop7`) — the fixture's `loop0` is the live Steam extension's
     name, and a mutant that drops the fixture gate would then aim `wipefs` at
     the real one.
   - `--destructive` as a THIRD arg branch, never folded into
     `--with-binary`, and **not** in `pr-validation.yml`. It must dispatch
     **before** the binary half's `export APEX_STORAGE_ROOT`, or the
     unprivileged `NeedsRoot` step (which does not go through `sudo -n`'s
     env_reset) judges the fixture machine and reports `CouldNotVerify`.
     Order: sparse file under `$TMP` -> `dev=$(sudo -n losetup --find --show
     "$f")` -> **assert `realpath "$(cat /sys/block/${dev#/dev/}/loop/
     backing_file)"` == `realpath "$f"` or exit 1 before anything else**
     (`backing_file` is a file CONTAINING a path, not a symlink — `realpath`
     on the sysfs node compares a sysfs path to a tmp path and can never
     match, so the test would silently never run) -> extend the binary half's
     **existing** `trap ... EXIT` rather than adding a second one, which
     replaces it -> mkfs -> mount -> expect `Mounted` and the signature still
     there (`sudo wipefs -n`) -> umount -> unprivileged -> `NeedsRoot` ->
     wrong token -> `Unconfirmed` -> attached loop device with no
     `--expect-backing-file` -> `LoopDeviceInUse` -> wrong backing file ->
     `BackingFileMismatch` -> sudo + exact token + correct backing file ->
     success, `wipefs -n` empty, backup file present.
2. Battery rows M9+ (one per refusal, plus the two enumeration mutants),
   then commit+push P2-014, THEN P2-015, THEN P2-011.

Measured while wiring, and both are now closed: `apex` reads no
`XDG_STATE_HOME` at startup (so `sudo`'s env_reset costs nothing), and exit
code 3 is `EXIT_NOT_FOUND` in `ops.rs` for `apex update`'s firmware pass —
a different command's convention, not a collision.

**Pushing from this worktree needs gh's credential helper.** A bare
`git push` dies with `could not read Username for 'https://github.com'` — the
remote is HTTPS and there is no credential helper configured. This works and
persists nothing:

```
git -c credential."https://github.com".helper='!gh auth git-credential' \
    push origin task/p2-010-hardware-storage
```

`gh auth status` shows a valid `gho_` token with `repo` scope. Do NOT run
`gh auth setup-git` — it would write into Andre's global git config.

Build environment for this worktree: `apexd/target` in-worktree (3.6 GB, warm,
private to this worktree, under `/var/tmp` — deliberately not moved to
`/var/tmp/apex-build-cache/`, because moving a cargo target dir invalidates
every fingerprint and costs a full rebuild). `XDG_STATE_HOME`/`XDG_CONFIG_HOME`
= `/var/tmp/p2010-xdg/{state,config}`.

## Verdict, per item

| item | verdict | proof |
|---|---|---|
| **P2-010** hardware qualification database | **done, proven** | `ee30c3b`. cargo 1801 → 1833; shell 64/0; 7-row mutation battery, all red, re-run digit-for-digit at `1dd0b13` |
| **P2-011** agent cgroup resource budgets | **done, proven** | `1086478` `84aea0a` `635b683` `23b543ad`. agentd 196/0 → 200/0; live half 8/0 in 5.18 s; 14-row battery, 14 red — 2 were green first and both were fixed |
| **P2-014** APEX Storage Manager and disk-health UX | **done, proven** | `21005dc` `eeb6c1f` `46d4ac7` `973455f` `d376193` `9c31464` `0d752fc`. core 60/0, structural 40/0, binary 141/0, destructive 59/0 on loop devices; 16-row battery, all red |
| **P2-015** firmware and hardware lifecycle | **done, proven, with hardware nobody here has** | `7cd113c`. firmware Rust 33; structural 24/0; `--with-binary` 67/0; `fwupd` pinned in `Containerfile.core` |

All four are implemented, tested, mutation-proven and pushed. What is **not**
proven, precisely, and none of it is a code gap:

- **P2-015, the write path.** Every readout is proven against this machine's
  real `fwupdmgr get-devices --json` reduced to inline fixtures. What cannot be
  proven here is a firmware update actually being **applied**: this L16 has no
  pending update, so `Writable::Blocked` on a real waiting update, the
  `require-ac-power` refusal firing for real, and a post-update version change
  are all fixture-only. That needs a machine with firmware waiting — hardware
  nobody in this program has on demand. The shapes are covered; the transition
  is not.
- **P2-014, the LUKS holder rule.** Every `holders/` directory on this machine
  is empty — there is no LUKS anywhere on the L16 — so `Refusal::HasHolders` is
  exercised only in the fixture. Needs an encrypted volume, which is hardware
  state nobody here has.
- **P2-014, non-NVMe SMART.** `classify_exit` and `parse_smart` are proven
  against this machine's NVMe and against fixtures; a real SATA/USB disk
  returning a different `smartctl` document has never been seen here. Same
  class: hardware nobody here has.
- **P2-011, `io` budgets.** Not a gap and not fixable in code: `io` is in the
  root cgroup's `cgroup.controllers` and never in its `subtree_control`, so no
  `io.max` exists anywhere under `user.slice` and no machine in this program
  can enforce one. That is the whole reason the feature reports it as a
  `Reading` carrying the reason instead of writing a zero, and the live test
  asserts `io.max` really is absent rather than taking the module's word.
- **P2-011, the live half is L16-only.** It needs a user bus, so it is opt-in
  behind `APEX_BUDGET_LIVE=1` and stays out of `pr-validation.yml`.
  Consequence, stated plainly: **CI does not protect FOUND (fifth)** — see the
  B12 row.
- **Nothing here has ever run in CI.** `pr-validation.yml` on this branch
  triggers only on `pull_request` to `main`, and this branch has never been in
  such a PR. Checked rather than assumed, on this branch *and* on the
  integration tip `ec91e92`: both have only `on: pull_request: branches:
  [main]`. `gh run list` shows the orchestrator's new `push` trigger firing on
  `roadmap/v2.2` today — two failures at 13:57 and 13:58 UTC in the `Select
  tests` job (a path filter with no PR base on a push event) and then
  **success** at 14:00 UTC (`02bac47`). So CI on a task branch is arriving but
  is not here yet; `.github/workflows/pr-validation.yml` was deliberately NOT
  touched from this worktree, because the orchestrator is editing that same
  file.

## Order of work

1. P2-010 qualification store (smallest, exercises the STORES contract).
2. P2-014 storage reader (`apexd-core/src/storage.rs`) + `apex storage`.
3. P2-014 destructive half, refuse-by-default, tested on loop devices only.
4. P2-015 firmware readout + pin fwupd in the image.
5. P2-011 cgroup budgets in `apex-agentd`.

## Measured before writing anything

- `sudo -n true` works on the L16 — non-interactive sudo is available.
- **`smartctl -j /dev/nvme0n1` as an unprivileged user exits 2** and returns a
  document with only `json_format_version`, `local_time` and `smartctl`, where
  `smartctl.exit_status` is 2 and `smartctl.messages` holds
  `"Smartctl open device: /dev/nvme0n1 failed: Permission denied"`. A reader
  that keys on the absence of `nvme_smart_health_information_log` reports
  "no SMART data" for a disk that has plenty.
- **`sudo smartctl -j /dev/nvme0n1` exits 0 and also returns no health log.**
  smartctl with no action option does nothing; `-a` (or `-x`/`-H`/`-A`) is
  what asks. Exit 0 plus an empty document is the same trap wearing root.
- `fwupdmgr get-devices --json` answers as `andre` with no session bus at all
  (it uses the system bus). No polkit prompt.
- **`io` is not in the root cgroup's `subtree_control`** (`cpu memory pids`
  are), so no `io.max` file exists anywhere under `user.slice`. I/O budgets
  are not enforceable for a user-service agent on this machine as shipped.
- `apex-agentd.service` has no `Delegate=`, so systemd owns its cgroup and
  sub-cgroups are unsupported until a drop-in adds one.
- **Nothing in any Containerfile installs `fwupd`** and
  `rpm -q --whatrequires fwupd` says no package requires it — it rides in from
  `quay.io/fedora/fedora-bootc:43`. `apex update`'s firmware pass in `ops.rs`
  depends on a package the image does not own.
- `smartmontools`, `nvme-cli`, `udisks2`, `gparted`, `parted`, `gdisk` are
  installed explicitly by `Containerfile.core:1017`.

## Done

**P2-010 — done.** `ee30c3b` on `task/p2-010-hardware-storage`, pushed.

- `apexd/apexd-core/src/qualify.rs` — consent (unset/granted/declined),
  three-valued verdicts, a fixed 12-check catalogue, the machine identity with
  no free-form field, import/merge.
- `apexd/apex/src/qualify.rs` — `apex qualify status|consent|record|probe|
  export|import`, `$APEX_QUALIFY_ROOT` fixture prefix.
- Registered in `migrate::STORES` as `qualification` and in
  `schema.rs::paths()`. `apex schema status` shows it.
- `tests/test-apex-qualify.sh`, both halves wired into `pr-validation.yml`.

Counts: cargo baseline 1801 -> **1833** (exit 0, measured from a file, not a
pipe). Shell suite **64 passed, 0 failed**.

Mutation proofs, all restored green afterwards:

| mutation | shell | apexd-core |
|---|---|---|
| green baseline | 64/0 | 22/0 |
| consent gate reads "not declined" instead of "granted" | 59/5 | 21/1 |
| SecureBoot reads byte 0 instead of byte 4 | 62/2 | — |
| a refused SecureBoot read becomes a Fail | 63/1 | — |
| withdrawing consent keeps the records | 62/2 | 21/1 |
| an unreadable database becomes an empty one | 64/0 | apex 9/1 |
| the machine identity gains a free-form map | 63/1 | 21/1 |

Note on the fifth: the shell suite does not seal the database file, so only the
Rust test catches it. Left as is — the Rust test is the right place for a
0000 file, and the suite already seals the EFI variable.

**Verified green at `1dd0b13`, all five previously-unpushed commits now on
origin.** The battery re-run reproduces the recorded table digit for digit;
`cargo test --locked --workspace` is 1025 passed / 1 failed with only the one
pre-existing apex-agentd failure; `tests/run-clippy.sh 1dd0b13` is clean.
Details under `## Measured, third pass`.

**P2-014 read half — done.** `21005dc` + `eeb6c1f` on
`task/p2-010-hardware-storage`.

- `apexd/apexd-core/src/storage.rs` — pure. `Reading<T>` (a value or the reason
  there is none, deliberately not `Option` so `unwrap_or_default` is
  unreachable), `classify_exit` for smartctl's bitmask, `parse_smart`,
  `wear_health`, `space`/`space_health`, `discards`, `trim_health`,
  `encryption_health`.
- `apexd/apex/src/storage.rs` — `apex storage status|warnings [--json]`,
  `$APEX_STORAGE_ROOT` fixture prefix, `doctor_lines` into `apex doctor`.
- `files/system/libexec/apex-storage-notice` + a user `.service`/`.timer`,
  enabled by default, wired into `Containerfile.base`.
- `tests/test-apex-storage.sh`, both halves in `pr-validation.yml`.

**Shape note for a successor: this is `Roots` + `$APEX_STORAGE_ROOT` (the
`apex trust` shape), NOT the `detect_from(sys_root, proc_root)` the old NEXT
asked for.** Deliberate: storage needs three roots (sysfs, proc, /run/udev)
plus two subprocess answers (smartctl, systemctl), and a fixture prefix covers
all five where a two-argument signature covers two. Do not "fix" it.

Sources actually used, and why they differ from the card's list: `lsblk -J`
and `findmnt -J` are not spawned — `/sys/block` and `/proc/self/mountinfo` are
the same data with no subprocess and no JSON schema to track. `nvme smart-log`
is not spawned either: `smartctl -j -a` already returns
`nvme_smart_health_information_log`, and `nvme list` prints the serial to any
user. `blkid` is not spawned — see the measurement below.

**P2-014 destructive half, pure guard — done.** `46d4ac7`, pushed.

- `apexd/apexd-core/src/storage.rs` gains a second section below an explicit
  polarity-inversion comment: `BlockDevice`, `PartitionEntry`, `Machine`,
  `Refusal` (8 arms, each with a `say()`), `Permit` (private field, `guard` is
  the only constructor), `would_destroy`, `kernel_name_of`, `is_esp`,
  `ESP_TYPE_GUID`, `ESP_TYPE_MBR`, `SIGNATURE_BACKUP_DIR`.
- Counts: core `37/0` -> **`54/0`** (10 in `apex --bins` + 44 in `apexd-core`,
  battery shape `-p apexd-core -p apex --lib --bins storage`). Structural
  `23/0` and binary `75/0` unchanged — no CLI or shell work in this commit.
  `cargo build --all-targets` clean, no warnings.

**The predecessor's uncommitted draft did not compile** — it defined
`would_destroy` and then called it `same_disk` in `guard`. It was real work,
not a spliced mutant, so the card's `git checkout` clause did not apply.

**P2-014 destructive half, the loop-device rule — done.** `973455f`, pushed.

- `BlockDevice` gains `loop_backing: Reading<Option<String>>`; `Refusal` gains
  `LoopDeviceInUse` and `BackingFileMismatch`; `guard` gains an
  `expect_backing: Option<&str>` parameter. `Known(None)` comes only from
  `NotFound` on the `loop/` directory, never from `Path::exists()`.
- Counts: core `54/0` -> **`60/0`** (10 `apex --bins` + 50 `apexd-core`).
- The finding it fixes is under **FOUND: the guard permitted erasing the live
  Steam extension** below.

**P2-014 destructive half, CLI + suites — done.** `d376193`, `9c31464`,
`0d752fc`, all pushed.

- `apexd/apex/src/storage.rs` — `machine()` (a new enumerator returning
  `Result`), `holders()`, `loop_backing()`, `partition_entry()`, `canonical()`,
  `effective_uid()` (with the `.fixture/euid` override), `erase()`,
  `EXIT_REFUSED = 3`, `WIPEFS` by absolute path, `--backup=<dir>` explicit.
- `tests/test-apex-storage.sh` — three modes. Structural **23/0 -> 40/0**,
  `--with-binary` **75/0 -> 141/0**, `--destructive` **59/0** (four
  consecutive clean runs, no leaked loop device). `--destructive` is NOT in
  `pr-validation.yml`; the other two already were and needed no change.
- `cargo test --locked --workspace --no-fail-fast` **1893 passed, 0 failed**,
  29 blocks. `tests/run-clippy.sh` **PASS**.

**P2-015 firmware readout — done.** `7cd113c`, pushed. The predecessor had
this most of the way built and uncommitted when the run was paused; it
compiled and its own tests passed. Two defects were found by reading it before
trusting it, and both are fixed in the same commit with the tests that catch
them — see **FOUND (fourth)** for the second, which is the bigger one.

- `apexd/apexd-core/src/firmware.rs` — pure. `parse_devices` takes **no exit
  code**, checks `Error` **before** `Devices`, and reports a document with
  neither as unreadable rather than as an empty machine. `Writable` has three
  states (`Now`/`Blocked(reasons)`/`Never`), `Device::{label,version_text,
  short_id,blockers}` cope with the absent keys, `SECURE_BOOT_PLUGINS` splits
  stores from hardware **by plugin**, an unknown plugin counts as hardware,
  and `Report::attention` reports a blocker **only against an update that is
  actually waiting**.
- `apexd/apex/src/firmware.rs` — `apex firmware status [--json]`,
  `$APEX_FIRMWARE_ROOT` fixture prefix (**no subprocess at all** under it, so
  the suite runs with no fwupd, no bus and no privilege), `FWUPDMGR` absolute,
  `doctor_lines` into `apex doctor`.
- **`fwupd` added to `Containerfile.core`'s dnf line**, with `rpm -q fwupd`
  and `command -v fwupdmgr` in the style of the `scrcpy` assertions already in
  that block. `apex update`'s firmware pass no longer depends on a binary that
  rode in from the base image by luck.
- `tests/test-apex-firmware.sh`, both halves in `pr-validation.yml`.

**Defect 1 in the draft: `apex doctor` reported one unreadable fwupd as a PASS
and a WARN at the same time.** `doctor_lines`' `Unavailable` arm pushed a
`(true, "not measured …")` line and then **fell through** to the attention
loop, whose sentence reads "whether this machine has firmware updates is
unknown: …" — which does not contain the words `"update waiting"` that the
loop's filter excludes on. So the same condition produced a green line and a
red line, and the function's own doc comment promised the opposite. Fixed by
**returning** from that arm, deliberately not by widening the filter: a filter
that has to know the wording of every sentence `attention()` can produce is
one edit away from saying it twice again. It had no test — the shell suite
never drives `apex doctor` — so four went in (`…is_exactly_one_doctor_line…`,
`a_healthy_machine_on_battery_is_one_passing_doctor_line`,
`…blocker_comes_with_it`, `a_fwupd_that_cannot_be_read_at_all_still_only_passes`)
plus a structural check that the arm cannot fall through.

Counts: firmware Rust tests **33** (11 in `apex --bins` + 22 in `apexd-core`);
shell structural **24/0, fifteen consecutive runs**; `--with-binary` **67/0,
four consecutive runs**.

**P2-011 — done.** `1086478`, `84aea0a`, `635b683`, `23b543ad`, all pushed.

- `apexd/apex-agentd/src/budget.rs` — new file, pure above the marked line and
  reading this machine below it. `Reading<T>` (re-declared here rather than
  shared: the daemon must not take a dependency on the CLI's core crate for a
  twenty-line enum), `Resource`, `Budget::parse` off `Config.extra["budget"]`,
  `parse_size`, `ScopeName` (private field, `checked` the only way in),
  `scope_parent`, `Controllers`, `Plan::build`/`apply`/`notes`, `wrap`,
  `transport_at`, `delegated_controllers`.
- `mod budget;` in `main.rs`; an eight-line hook in `session.rs` after the
  `let argv = if req.disposable { … }` block using the `cfg` already in scope,
  plus `BudgetRefused` mapped to `PolicyRefused` in `run_error`.
- `apexd/apex-agentd/tests/budget.rs` — eight tests over a real daemon with its
  own runtime, state, config and scratch directories, killed by pid and never
  by name. Six need no bus, no privilege and no prompt; two are opt-in behind
  `APEX_BUDGET_LIVE=1`.
- Nothing was added to `pr-validation.yml` and nothing needed to be: its
  `Tests` step is a bare `cargo test --locked` under
  `tests/in-login-session.sh`, so the six CI-safe tests ride in with the
  workspace, and CI never sets `APEX_BUDGET_LIVE`.

Counts: `cargo test -p apex-agentd --no-fail-fast` **196/0 → 200/0** across 7
targets (29 budget unit tests in the `apex-agentd` bin target + 8 in
`tests/budget.rs`). Live half, from a login session: **8 passed in 5.18 s**,
with `memory.max` 536870912, `pids.max` 64, `cpu.max` `50000 100000`, no
`io.max` anywhere, the recorded pid's `comm` = `sleep` (so `--scope` really
execs in place), and a 60 s sleep really ended by `RuntimeMaxSec=5`.

**The 5.18 s is half the point.** Before the fix in `635b683` the same command
took 0.04 s and said `ok`, because both live tests `return` when the daemon
does not come up and a `return` in a `#[test]` is a pass. That is now an
assertion failure whenever `APEX_BUDGET_LIVE=1` is set (`23b543ad`).

### The pre-existing apex-agentd failure is confirmed environmental

The card said the `system_grants.rs`
`renewing_a_grant_that_does_not_exist_is_refused_without_asking_anybody`
failure was a cgroup artifact, and explicitly said it had **not** verified the
test passes outside a systemd user service. Now verified: from
`/proc/self/cgroup` = `0::/user.slice/user-1000.slice/session-4.scope` the
test is `ok` and the workspace is **1893 passed, 0 failed**. So the earlier
`1869/1` was the environment, not the code, and nobody should "fix"
apex-agentd for it.

### Measured while wiring the CLI

- **andre is in `wheel` and `kvm`, NOT `disk`**, and `/dev/nvme0n1p3` is
  `brw-rw---- root:disk`, so an unprivileged `wipefs` on a real partition gets
  EACCES. That is a permission accident, not a safety property — `sudo
  ./tests/test-apex-storage.sh --with-binary` is a reasonable thing to type —
  which is why the suite now asserts that no device it expects a *permit* for
  exists under `/dev` on the host, and judges real partitions only with a
  deliberately wrong token.
- **An EXIT trap that mentions one of its function's `local`s leaks the loop
  device.** Written the obvious way, `cleanup` referred to `local mnt`; by the
  time the trap ran the local was gone, `set -u` aborted the trap at its first
  line, and two loop devices were left attached to deleted images — while the
  suite printed `59 passed, 0 failed`. Every name the cleanup touches is now a
  global with a `${x:-}` default. A cleanup that can abort is a cleanup that
  will.
- **`/dev/loopNp1` and `/sys/block/loopN/loopNp1` appear at different
  moments.** A wait loop on the `/dev` node was satisfied while sysfs was not
  ready, so `machine()` did not see the partition and it came back
  `NotAKnownBlockDevice` — still exit 3, so only the assertion on the
  refusal's *wording* caught it. Seen once as a flake; the loop now waits on
  the sysfs directory, which is what the code actually reads. Four clean runs
  after.
- `apex` reads no `XDG_STATE_HOME` at startup, so `sudo`'s env_reset costs the
  destructive half nothing. Exit 3 is `EXIT_NOT_FOUND` in `ops.rs` for `apex
  update`'s firmware pass — a different command's convention, not a collision.

### Mutation battery, destructive half (M9-M16)

Battery extended at `/var/tmp/p2010-mutate.sh`; full log
`/var/tmp/p2010-battery-9c31464.log`. `run()` deliberately never invokes
`--destructive`: several of these remove the guard rails from a command that
erases disks, and splicing one in and then really erasing something would be
the most dangerous thing this repo could do.

**All sixteen rows below are ONE run at `0d752fc`** — log
`/var/tmp/p2010-battery-0d752fc.log`. The earlier `…-9c31464.log` had a
different baseline (135/0) because M10 and M11 were still green then, so its
numbers are not comparable with these and are not quoted. Baseline and
`restored` both **60/0 · 40/0 · 141/0**, and the M1-M8 mid-point checkpoint
matches them exactly, so the eight older mutations still behave as first
recorded.

| mutation | core | structural | binary |
|---|---|---|---|
| green baseline | 60/0 | 40/0 | 141/0 |
| M1 `classify_exit`: any non-zero exit means the read did not happen | 57/3 | 39/1 | 131/8 |
| M2 `space()`: drop the `/dev/` filter, so composefs is a filesystem | 58/2 | 39/1 | 139/2 |
| M3 `encryption()`: no udev record means not encrypted | 9/1 | 40/0 | 139/2 |
| M4 `space_health()`: percentage alone, no byte floor | 59/1 | 40/0 | 140/1 |
| M5 `remedy_for()`: a refused read gets no next step | 59/1 | 40/0 | 139/2 |
| M6 `discards()`: exact word match, missing btrfs `discard=async` | 59/1 | 40/0 | 139/2 |
| M7 notifier: fire for rows nobody could measure too | 60/0 | 40/0 | 140/1 |
| M8 disk size: sectors x 4096 instead of x 512 | 9/1 | 40/0 | 140/1 |
| M9 `machine()`: a disk whose directory will not list has no partitions | 60/0 | 40/0 | 139/2 |
| M10 marker stat: a permission denial means "not a partition" | 60/0 | 40/0 | 140/1 |
| M11 `loop_backing()`: an unreadable `backing_file` means "not a loop device" | 60/0 | 40/0 | 138/3 |
| M12 `holders()`: an unreadable holders directory is an empty one | 60/0 | 39/1 | 138/3 |
| M13 `erase()`: the fixture gate is gone | 60/0 | 38/2 | 134/6 |
| M14 `guard()`: an attached loop device is not in use | 57/3 | 40/0 | 136/5 |
| M15 `machine()`: a partition does not inherit its parent's backing file | 60/0 | 40/0 | 140/1 |
| M16 a refused erase is indistinguishable from a failed one | 60/0 | 40/0 | 123/18 |
| restored | 60/0 | 40/0 | 141/0 |

**Note on M3's and M8's `core` column:** `9/1` is not a typo, and it is not a
smaller failure than `57/3` — it is a bigger one. The battery's `run()` does
not pass `--no-fail-fast`, and the two targets run in a fixed order: `apex
--bins` (10 tests) first, `apexd-core` (50) second. M3 and M8 both mutate
`apex/src/storage.rs`, so the first target fails and **cargo never runs the
other 50 tests at all** — `9/1` means "1 of the first 10 failed and 50 were
skipped". M14 mutates `apexd-core`, so the first target passes and the totals
still add to 60. Same fail-fast trap the card recorded for
`cargo test --workspace`, met again in a different tool. Do not read these
totals across rows as comparable.

**Two were completely green on the first pass, and are why the battery earns
its keep** — both fixed in `0d752fc`, and the M9-M16 counts above are the
re-run with those tests in place:

- **M10 was 60/0 · 40/0 · 135/0.** The `chmod 100` test fails at
  `dir_names(&base)?` before the marker is stat'ed, and the `chmod 000`
  holders test refuses through `CouldNotVerify`; nothing exercised the case
  between them — the disk's directory listing fine and one partition's own
  directory not traversable. Folding that into "not a partition" leaves
  `/dev/nvme0n1` refused for carrying the running system, so the suite still
  went through the motions; it just stopped knowing the disk has an ESP.
- **M11 was 60/0 · 40/0 · 135/0,** and the mutation was also aimed at the
  wrong arm. The `loop/` *directory* stat is nearly unreachable — a
  non-traversable parent fails enumeration first. The arm that matters is the
  `read_to_string` of `backing_file`: `/dev/loop0`'s node is plainly there and
  only its CONTENT says that erasing it destroys a live system extension.

## FOUND (eighth, and NOT this unit's file): apex-agentd has a ~5% flake that CI will meet

Not fixed from here, and deliberately so — `apexd/apex-agentd/tests/
system_grants.rs` is P1-020's/§7's file and editing it from this worktree is
the merge minefield the card already refused to enter for the 33 grep-blind
shell suites. But it now matters more than it did, because the orchestrator has
just put a `push` trigger on `roadmap/v2.2`, so this will start turning CI red
at random.

Measured here, on an otherwise clean tree:

```
whole system_grants target, 20 runs        -> 1 failure     (~5%)
the_ending_is_recorded_once_…, alone, 25 runs -> 0 failures
```

Two tests alternate as the failure and both count lines in an audit file:
`the_ending_is_recorded_once_however_many_daemons_see_it` and
`a_grant_from_another_boot_is_reported_as_ended_on_the_next_start`. Neither
fails alone in 25 runs, so it needs its siblings running beside it — a
load-sensitive timing race between a daemon's audit write and the test's read,
not a logic error. It reproduced on a stashed, unmodified tree, so it is not
caused by anything in this unit.

It also had to be worked around to get a clean mutation table: a 16-row battery
meets a 5% flake about half the time and would record it as a mutation being
caught. `/var/tmp/p2010-mutate-budget.sh` therefore re-runs (up to three times)
any run whose **only** failures are those two names, and records nothing
otherwise. Whoever owns that file: the fix is presumably to wait for the audit
line rather than to read it immediately.

## FOUND (seventh): the daemon read its own cgroup as the machine's

**`635b683`.** P2-011's live half failed the first time it was run from this
round's cgroup, and the refusal it printed is the finding:

```
policy_refused: a resource budget was configured but this machine's delegated
controllers could not be read ... no user@<uid>.service component in the
cgroup path "/user.slice/user-1000.slice/session-4.scope", so this process is
not under a systemd user manager and a transient user scope has no knowable
parent
```

It plainly is under one. Measured from that exact cgroup, moments later:

```
systemd-run --user --scope -q --unit=apex-p2010-probe-$$ --collect -- cat /proc/self/cgroup
  -> 0::/user.slice/user-1000.slice/user@1000.service/app.slice/apex-p2010-probe-276102.scope
```

`scope_parent` found the delegation boundary by walking **the daemon's own**
`/proc/self/cgroup` to a `user@<uid>.service` component. The shipped
`apex-agentd.service` is a systemd user unit and has one, so production was
never affected. A daemon started from a terminal, from ssh, or from
`tests/in-login-session.sh` sits in `…/user-<uid>.slice/session-<n>.scope`,
which has none — and every budgeted session on it was refused. The absence of
`user@` from the **caller's** path is not the absence of a user manager.

Same family as FOUND (fifth) and it fails in the same direction: a reading
that is not the fact it looks like, refusing a budget the machine can enforce.
That is now three times in this unit, and the third one shipped in a commit
whose own message said the live half proved the feature works.

**The test that should have caught it is the one that recorded the
measurement.** `the_boundary_is_the_user_managers_app_slice_whoever_asks` —
the name is "whoever asks" — said in its own comment:

> Measured: a scope asked for from a LOGIN SESSION still lands under
> user@1000.service/app.slice, so the boundary is not the caller's parent.
> **Both of these cgroups must produce the same answer.**

and then asserted, three lines below, that they produce different ones
(`from_login` → `Unavailable`). The comment was right, the assertion under it
was the bug, and the test was green. A green test carrying a correct
measurement it does not check is worse than no test: it is the reason nobody
looked again. Same shape as FOUND (fourth)'s blind greps, in Rust instead of
shell.

**Why the predecessor did not see it.** The card already records that this
worktree's owner has been, on different rounds, a systemd **user service**
(`apex-roadmap-resume.service`) and a **login session** (`session-4.scope`),
and that the split flips `system_grants.rs`'s pre-existing failure. It flips
this too, in the opposite direction: from a user service the live half passes,
from a login session it refuses. The leftover `apex-agent-211248-1.scope`
found still active on the user manager is the predecessor's successful live
run, from the placement where the defect is invisible. **This unit has now
been bitten by the same cgroup-placement split in both directions.**

Fix: the boundary has two namings because a daemon has two placements — a
`user@<uid>.service` component if the path has one (preferred, and a deeper
one still wins over anything inferred above it), otherwise the
`user@<uid>.service` child of the `user-<uid>.slice` component. Neither
present is still `Unavailable` and not a guess; a `user-<uid>.slice` whose uid
is not a number is `Unavailable` too, naming the component, because one that
cannot be made sense of is not one that is not there. An inferred boundary
that does not exist fails its `cgroup.controllers` read and refuses — the
unknown branch, the correct polarity for a path nobody measured.

Live half from a login session: **6 passed / 2 failed → 8 passed / 0 failed**,
5.18 s. Before the fix it was 0.04 s, which is the other half of the lesson —
see the note on skip-as-pass under `## P2-011 verdict`.

### The remainder this fix does not close

`sudo apex-agentd` from a user's login session: the process is uid 0 and its
cgroup still says `user-1000.slice`, so the constructed boundary names uid
1000's manager while `systemd-run --user` would target root's. Both are
`cpu memory pids` on the L16, so the answer is right here and would be wrong on
a machine where root's and the user's delegation differ. Closing it means
threading the effective uid through what is currently a pure function of one
string. Not done, deliberately, and named here rather than left to be
rediscovered: it needs a daemon nothing in APEX starts that way.

## FOUND (fourth): a structural check that was a coin flip, and 96% blind

`producer | grep -q` under `set -o pipefail` **does not reliably report a
match.** `grep -q` exits at its first match and SIGPIPEs whatever is feeding
it, so the PIPELINE's status is 141 rather than grep's 0, and an `if` reads a
match as "no match". Both directions are wrong and **one of them is silent**:

- match means `bad` (a "this must not appear" check) → a SIGPIPE turns a real
  regression into a **pass**, and no mutation can ever be caught by that check
  again.
- match means `ok` → a SIGPIPE is a **spurious red** on an unchanged tree.

Found in `tests/test-apex-firmware.sh`, which had four. Measured under bash:
40 trials of `sed -E 's://.*$::' apexd-core/src/firmware.rs | grep -q
uefi_capsule` returned **0 twenty times and 141 twenty times**, and the suite's
own structural half was **21/0 three times and 20/1 nine times out of twelve
runs over a tree that never changed**. That is the landing lesson from
2026-09-08 in a new form: green in a worktree, red in CI — except here it is
red or green in the *same* worktree.

**Then the same pattern was measured against `tests/test-apex-storage.sh`,
which is this unit's own landed work, and the result is the finding.** Ten of
its checks were this shape and **seven were the silent direction**. A mutation
was spliced into `apex/src/storage.rs` making the CLI look up
`ID_SERIAL_SHORT` — the exact privacy regression the first of those checks
exists to catch, and the reason the read half never spawns `nvme list` — 35
lines into a 1,331-line file:

```
raw pipeline, 40 bash trials      -> 141 in 40 of 40
suite reports the mutation        ->   1 of 25 runs      ** 96% BLIND **
after the fix, same mutation      ->  25 of 25
```

The check guarding the disk serial was 96% blind to the one change it is for,
while printing `40 passed, 0 failed` — fifteen consecutive runs of it on a
clean tree are 40/0, so nothing about the suite's own output ever hinted at
this.

**Why the other three survived, and why that is not a defence.** They use
`awk '/^fn x/,/^}/'`, which emits one small function and finishes writing
before grep can exit. The same class of mutation inside `machine()` was caught
**20/20**. That is luck about a 64 KB pipe buffer, not a property anyone
maintains: a function that grows past it, or a match near its start, flips the
check to blind. The discriminator is **producer size and where in the stream
the match sits** — a whole-file `sed` with an early match is the reliably
broken shape, and it is the shape the two privacy checks had.

Fix, in both suites: two helpers, `absent`/`present`, that take **captured
text** rather than a pipeline — command substitution reads its producer to EOF,
so there is nobody left to signal. `has`/`hasnt` grep a plain file and were
never affected. Also fixed while there: one firmware assertion could
**vanish** — its outer guard was `… | grep -n Error | head -1 | grep -q .`,
and a non-zero pipeline skipped the whole `if`, recording neither a pass nor a
failure — and the `uefi_capsule` check had a **second, independent** bug, in
that it grepped the whole file for a string that legitimately appears eight
times in fixtures and in an assertion message, so it was a false alarm on
correct code whenever it was not a coin flip. It is now scoped to
`SECURE_BOOT_PLUGINS`' own declaration.

**Not fixed, deliberately: `grep -c '| grep -q' tests/*.sh` finds this shape
in 33 more suites under `set -o pipefail`** — 43 in `test-apex-dispatch.sh`,
35 in `test-secret-broker.sh`, 34 in `test-apex-input.sh`, 28 in
`test-apex-task.sh`, 26 in `test-shell-agent.sh`, 25 in `test-apex-ai.sh`, and
so on down. Those are other units' files and touching them from here would be
a merge minefield; the two fixed are the two this unit owns. **Whoever picks
this up: the direction is what decides urgency** — a `found → bad` check is
silently mutation-blind and is the one worth auditing first, and the whole-file
producers among them are the ones that are actually broken today rather than
merely fragile.

## FOUND (second): one unreadable directory turns six refusals into a permit

Measured in the fixture, with `.fixture/euid` 0 and the correct token, against
the CLI enumerator as the predecessor wrote it:

```
chmod 755 <disk sysfs dir>  -> 6 refusals, exit 3   (CarriesTheRunningSystem,
                                IsTheEsp, 4x CouldNotVerify)
chmod 100 <disk sysfs dir>  -> "would erase /dev/nvme0n1", exit 0   ** WRONG **
```

`chmod 100` is readable to nobody and still traversable, so `holders/` and
`dev` keep answering — only the *listing* of the disk's directory fails. The
draft's enumerator had `let Ok(entries) = read_dir(&base) else { continue }`
and `base.join(n).join("partition").exists()`, so all five partitions vanished
from the `Machine`. Then: no mount source is `/dev/nvme0n1` (measured: zero
times), nothing was left carrying `parent == nvme0n1`, and the ESP rule is
only ever asked of partitions. **Only the fixture gate stood between that and
`wipefs --all` on the disk carrying the running system.**

This is a **fifth** way "is the device mounted?" green-lights destroying this
machine, and the first that needs no unusual hardware at all — one directory
with the wrong mode. It is also `Path::exists()` being false on EACCES for the
sixteenth time in this module.

Fix: `machine()` returns `Result<Machine, String>` and **every** enumeration
error is the `Err` arm — a partially-enumerated machine is not a machine to
judge against. Unlike every other reading here it cannot be a `Reading`,
because the guard reasons about a device by looking at its *neighbours*: a
device that was never enumerated is not an unknown the guard can refuse on, it
is a fact the guard never learns. `erase()` reports it as a refusal (exit 3),
not an error — nothing was wrong with the request. `NotFound` and
`NotADirectory` are the only two definite "not a partition" answers, and
`read_dir`'s per-entry `Err` is no longer `.flatten()`ed away.

## FOUND (third): a partition of a loop device has no `loop/` directory

Measured with `sudo -n losetup --find --show --partscan` on a 64 MB GPT image
(on `/dev/loop1`, chosen by `--find`, backing file asserted first — `loop0`
was never touched):

```
/sys/block/loop1/loop1p1/partition  -> exists
/sys/block/loop1/loop1p1/loop/      -> DOES NOT EXIST
```

So read on its own a loop partition answers `Known(None)`, "not an attached
loop device", and the `LoopDeviceInUse` rule lets it through — while `wipefs`
on it writes through the parent into the backing file exactly as it would on
the whole device. `would_destroy` walks downward only, so nothing else relates
a partition back up to its disk: the loop rule was one-directional where the
mount rule is explicitly both, which is the card's own sentence.

Fix: in `machine()`, a partition inherits its parent's `loop_backing` when its
own read is `Known(None)` — including the parent's `Unavailable`, because if
nobody could tell whether the disk is an attached loop device then nobody can
tell whether this partition is on one. A partition's own reading, if it ever
has one, still wins.

## Measured, second pass (P2-014)

- **`blkid -o value -s TYPE /dev/nvme0n1p5` unprivileged exits 0 and prints
  nothing.** Same trap family as the two smartctl ones: exit zero, no data, no
  error. A caller trusting it reports an encrypted disk as unencrypted. The
  filesystem type comes from `/run/udev/data/b<major>:<minor>` instead — udev
  wrote it as root at boot and left it world-readable.
- **smartctl's exit status is a bitmask and bits 3-7 mean the read WORKED.**
  Bit 3 is "DISK FAILING". `if status != 0 { unavailable }` turns the one row
  this feature exists for from red to grey. This is not on the card's trap list
  and is the biggest of the three.
- **`/` is `overlay`/`composefs`, 37.8M, 0 available, 100% used** on every
  bootc machine forever, and `/dev/nvme0n1p5` is mounted at five paths. Free
  space is per filesystem-on-a-block-device, one row, or the report is a
  permanent false alarm about a machine with 1.1 TB free.
- **btrfs writes `discard=async`, not `discard`.** A comparison against the
  bare word reads every btrfs filesystem on this OS as untrimmed.
- Two hwmon layouts both live on the L16: `<device>/hwmon4` (nvme) and
  `<device>/hwmon/hwmon6` (platform). Checking one reports "no sensor" for the
  other's hardware.
- **`cargo test --workspace` has ONE pre-existing failure in this environment**
  and it is not a regression: `apex-agentd` `system_grants.rs`
  `renewing_a_grant_that_does_not_exist_is_refused_without_asking_anybody`.
  `origin::classify` reads the caller's cgroup; an agent running under
  `user@1000.service/app.slice/apex-roadmap-resume.service` is a systemd user
  service, so §7 classifies it `ScheduledJob` and refuses elevation before the
  test's assertion is reached. **Verified at `ee30c3b` in a detached worktree:
  identical failure, exit 101.** What I verified is the cgroup rule and that
  the failure predates these commits; I did NOT verify it passes from a login
  terminal, only that `classify` would return `LocalTerminal` there. Do not
  "fix" apex-agentd for this without reproducing it outside a user service.
- `cargo clippy` does not exist on the L16; `tests/run-clippy.sh` (podman) is
  the only real clippy. It caught `items_after_test_module` that
  `cargo build --all-targets` did not.

### Mutation proofs (P2-014)

Battery at `/var/tmp/p2010-mutate.sh` — snapshots the three files, splices,
runs all three suites, restores. Counts are `apexd-core+apex storage unit
tests` / `structural` / `with-binary`.

| mutation | core | structural | binary |
|---|---|---|---|
| green baseline | 37/0 | 23/0 | 75/0 |
| M1 `classify_exit`: any non-zero exit means the read did not happen | 34/3 | 22/1 | 65/8 |
| M2 `space()`: drop the `/dev/` filter, so composefs is a filesystem | 35/2 | 22/1 | 73/2 |
| M3 `encryption()`: no udev record means not encrypted | 9/1 | 23/0 | 73/2 |
| M4 `space_health()`: percentage alone, no byte floor | 36/1 | 23/0 | 74/1 |
| M5 `remedy_for()`: a refused read gets no next step | 36/1 | 23/0 | 73/2 |
| M6 `discards()`: exact word match, missing btrfs `discard=async` | 36/1 | 23/0 | 74/1 |
| M7 notifier: fire for rows nobody could measure too | 37/0 | 23/0 | 74/1 |
| M8 disk size: sectors x 4096 instead of x 512 | 9/1 | 23/0 | 74/1 |
| restored | 37/0 | 23/0 | 75/0 |

**Two of these were green on the first pass and are the reason the battery was
worth running** — both fixed in `f7ffc0f`:

- **M8 was completely green.** The shell suite asserted every disk's model and
  kind and never once asserted a SIZE, and the 512-byte unit test lives in the
  `apex` BIN target — `cargo test -p apex --lib` matches nothing, so a runner
  using `--lib` silently skips all ten of them. Use `--lib --bins`.
- **M7 was completely green,** because widening the notifier's grep is
  unreachable on every path the suite exercised: the one test with unavailable
  rows has no attention row beside it, so `apex storage warnings` exits 0 and
  the notifier returns before the grep. The claim in its own comment — that an
  unavailable reason moving must not re-announce a standing warning — had no
  test. It has one now.

## Measured, fifth pass (P2-011 surfaces, before writing a line of it)

Three things the third pass had not measured decide whether budgets work at
all. All three are now measured, and **two of them changed the design**.

### The daemon is a systemd USER unit, and there are two of them

```
systemctl --user show apex-agentd.service -p ControlGroup -p Delegate
  ControlGroup=/user.slice/user-1000.slice/user@1000.service/app.slice/apex-agentd.service
  Delegate=no
pgrep apex-agentd -> 1406 (uid 0) and 1472 (uid 1000)
  1406: /user.slice/user-0.slice/user@0.service/app.slice/apex-agentd.service
```

So `--user` from a child targets a real user manager for both, and root's
`/run/user/0/bus` exists (`ls` as andre is EACCES, error 13 not 2 — present,
not absent). `user@0.service/cgroup.controllers` is `cpu memory pids`, the
same as 1000's.

### The child CAN reach the bus — but not for the reason it looks like

`pty::spawn` is called with `clear_env=true`, so the child's whole environment
is `sandbox::resolved_env(&spec)`, and **`ENV_BASE` contains neither
`XDG_RUNTIME_DIR` nor `DBUS_SESSION_BUS_ADDRESS`** (it is TERM, COLORTERM, the
LC_*/LANG set, TZ, TERMINFO, NO_COLOR — nothing else). Reproduced exactly:

```
env -i TERM=… PATH=… systemd-run --user --scope -q -p MemoryMax=64M -- true
  -> rc=1  "Failed to connect to user scope bus via local transport:
            $DBUS_SESSION_BUS_ADDRESS and $XDG_RUNTIME_DIR not defined"
+ XDG_RUNTIME_DIR=/run/user/1000                        -> rc=0
+ DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus -> rc=0   (either alone)
```

What saves it is `session.rs:378`, which already pushes `XDG_RUNTIME_DIR` =
`real_target(&spec.runtime_dir)` into `spec.env_set` for an unrelated reason
(so a session can find its control socket). `env_set` is inside
`resolved_env`, so the wrapper inherits it. **No env injection is needed and
none should be added** — the alternative was putting a bus variable into every
agent's environment, which for an unconfined session is a new leak, and this
feature must not pay for itself that way.

### `--scope` execs in place, so no pid bookkeeping changes

```
systemd-run --user --scope -q -- sleep 4 &   ->  PID 89655 ppid 89653 (the shell)
                                                 pgid 89653, no systemd-run alive
```

One process, not two: systemd-run creates the scope over the bus and then
`execve`s the command in its own process. So the pid `pty::spawn` records is
still the pid of the real program chain, `peer.rs`'s parent-chain session
resolution is unchanged, and `apex agent kill`'s killpg still names the right
group. The wrapper is a pure exec-chain prefix and is invisible to everything
that counts processes. This is the property that makes a one-line hook safe.

`RuntimeMaxSec=5 -- sleep 30` reproduces the card: **rc=143 at exactly 5s.**

### FOUND (fifth): reading `subtree_control` reports `cpu` as unenforceable on a machine where it is enforceable

The third pass read `user@1000.service`, where `cgroup.controllers` and
`cgroup.subtree_control` happened to be the same string. One level down they
are not, and the difference is **transient**:

```
app.slice/cgroup.controllers      -> cpu memory pids
app.slice/cgroup.subtree_control  -> memory pids          ** no cpu **

then, once ONE child scope asked for CPUQuota:
app.slice/cgroup.subtree_control  -> cpu memory pids       ** cpu appeared **
```

systemd enables a controller in `subtree_control` **on demand**, when a child
unit first needs it. So a discovery routine that keys availability on
`subtree_control` answers "cpu budgets are not enforceable here" on a machine
where they are — and answers it differently depending on what else happens to
be running. Measured against the real thing, inside a scope that asked for all
four:

```
scope cgroup.controllers -> cpu memory pids
  cpu.max     50000 100000     (CPUQuota=50%, enforced)
  memory.max  67108864         (MemoryMax=64M, enforced)
  pids.max    64               (TasksMax=64, enforced)
  io.max      No such file or directory
```

So the file to read is the **delegation boundary's `cgroup.controllers`**, and
`subtree_control` must not be consulted as an availability signal at all. Same
family as everything else this unit found: a reading that is not the fact it
looks like. Here it fails in the direction of falsely reporting *absence* —
which under the "never a budget of zero" rule would have silently dropped a
budget the machine could have honoured.

`io` really is absent, and that part holds up: it is in the root cgroup's
`cgroup.controllers` (`cpuset cpu io memory hugetlb pids rdma misc dmem`) but
**not in the root's `subtree_control`**, so it is never delegated to anyone,
and `find /sys/fs/cgroup/user.slice -maxdepth 4 -name io.max` finds **nothing**.
`io` is a `Reading::Unavailable` carrying that reason.

### FOUND (sixth): a transient scope's NAME can widen a request's privilege

`origin::classify` decides §7 origin by substring, over the peer's cgroup path:

```
cgroup.contains("/session-") && cgroup.contains(".scope") -> LocalTerminal / ApexShell
cgroup.contains("/user@")    && cgroup.contains(".service") -> ScheduledJob
```

A transient user scope is placed under the user manager regardless of who asks
for it — measured from a login session at `/user.slice/user-1000.slice/session-4.scope`:

```
systemd-run --user --scope --unit=apex-budget-probe-1 -- cat /proc/self/cgroup
  -> /user.slice/user-1000.slice/user@1000.service/app.slice/apex-budget-probe-1.scope
```

That path still contains `/user@` and `.service` (from the `user@1000.service`
ancestor), so a wrapped agent still classifies **ScheduledJob**, exactly as it
does today inside `apex-agentd.service`. No widening — **as long as the unit
name cannot supply the other rule's substring.** It can:

```
systemd-run --user --scope --unit=session-99-probe -- cat /proc/self/cgroup
  -> …/app.slice/session-99-probe.scope        ** matches "/session-" + ".scope" **
                                               ** classifies LocalTerminal **
```

The scope name is the last path segment, so a name *beginning* `session-`
puts `/session-` into the path and promotes every request from inside that
agent to the origin §7 reserves root and break-glass for. It is not reachable
today (the name is built from a fixed prefix and the daemon's own integer
session id, neither caller-supplied), but it is one refactor — "name the scope
after the session" — away from being reachable, and nothing in `origin.rs`
would notice. So the name is validated at construction, and a test asserts the
produced cgroup path still classifies `ScheduledJob`.

### Consequences for the design

- No `Reading` exists in `apex-agent-core` (grepped: only `*Unavailable`
  error variants in `policy.rs`/`client.rs`), and **no limits/resources stanza
  exists in `adapter.rs` or `config.rs`** — so `budget.rs` declares its own
  two-state reading and owns its own config surface. `apexd-core` is
  deliberately NOT added as a dependency: the daemon must not gain a
  dependency on the CLI's core crate for a 20-line enum, and P1-020's tip
  makes `Cargo.toml` a conflict surface.
- Default is **no budget and therefore no wrapper at all**, so an unbudgeted
  session's argv is byte-identical to today's. That is what keeps the three
  existing integration tests (`hook_bridge.rs`, `request_origin.rs`,
  `system_grants.rs`, which drive a real daemon over a socket) unchanged, and
  it is why `RuntimeMaxSec` defaults to none — a wall-clock kill on a coding
  agent is a regression, not a feature.

## Measured, third pass (verification at `1dd0b13`, and P2-011/P2-015 surfaces)

### The battery reproduces exactly, at HEAD

Re-ran `/var/tmp/p2010-mutate.sh` at `1dd0b13` — two commits *after* the table
above was recorded — and **every row matches to the digit**: baseline and
`restored` both 37/0 · 23/0 · 75/0, all eight mutations red. Log kept at
`/var/tmp/p2010-battery-1dd0b13.log`. The two style commits changed no
behaviour. Tree clean afterwards.

### `cargo test --workspace` stops at the first failing target

- `cargo test --locked --workspace` → **1025 passed, 1 failed, exit 101**, in
  only **10** test-result blocks. The failure is the known pre-existing
  `apex-agentd` `system_grants.rs`
  `renewing_a_grant_that_does_not_exist_is_refused_without_asking_anybody`.
- **That count is truncated, and measuring it proved how badly.** cargo stops
  running further test binaries once a target fails, so the pre-existing
  apex-agentd failure *masks every target ordered after it*. The same command
  with `--no-fail-fast` → **1869 passed, 1 failed, 29 blocks**. Fail-fast was
  therefore hiding **844 passing tests and 19 of the 29 targets**: "1025
  passed" describes a run that quit a third of the way in. **Always
  `--no-fail-fast` on this workspace** while that failure stands. It is also
  why the card's earlier "1801 -> 1833" and the 1025 are not comparable
  numbers. No doc-tests exist in the workspace (0 `Doc-tests` blocks).
- `--locked` did **not** complain about `Cargo.lock` — no lock update is owed.
- `tests/run-clippy.sh 1dd0b13` → **PASS, clippy is clean, exit 0** (run
  against the pushed ref in a detached worktree, not the live tree, so later
  edits cannot contaminate the result).

### P2-011: budgets are enforceable, but only three of the four

- The **user manager's** cgroup is the one that decides, not the root cgroup the
  first pass checked. `user@1000.service/cgroup.controllers` **and** its
  `subtree_control` are both `cpu memory pids` — so cpu/memory/pids are
  delegated all the way down to `app.slice`, and `io` is absent at every level.
  **`io` is not enforceable for a user-service agent on this machine**, and per
  the storage discipline that is a `Reading` carrying the reason, surfaced as
  such — never reported as a budget of zero and never silently dropped.
- `systemd-run --user --scope -p MemoryMax=64M -p CPUQuota=50% -p
  RuntimeMaxSec=5 -- sleep 30` runs with **no polkit prompt** and was killed at
  **exactly 5s (exit 143, SIGTERM)**. So a transient scope per agent buys
  cpu + memory + pids + wall-clock runtime with **no `Delegate=` drop-in
  needed** — which retires the first pass's "sub-cgroups are unsupported until
  a drop-in adds one" as the blocker it looked like. Prefer scope-per-agent to
  patching `apex-agentd.service`.
- Put this in a **new** `apexd/apex-agentd/src/budget.rs` with a one-line hook.
  P1-020 landed ~1,700 lines into `apex-agentd` on the tip that this branch
  does not carry; a new file merges, a refactor of `session.rs`/`pty.rs` does
  not.

### P2-015: fwupd's exit status flips meaning on `--json`

- `fwupdmgr get-updates --json` with nothing to update → **exit 0**, body
  `{"Devices": []}`, empty stderr.
- `fwupdmgr get-updates` **without** `--json`, same machine, same moment →
  **exit 2**, a full device list on stdout and `No updates available` on
  stderr. So the *success* case is a non-zero exit in one form and zero in the
  other. A caller that keeps the exit check but drops `--json` reads a healthy
  machine as a broken firmware subsystem. Same family as the three smartctl and
  blkid traps, and the one direction none of them covered: **exit non-zero
  meaning "nothing to do".** Use `--json` and read `Devices`, not the status.
- `fwupdmgr get-devices --json` → exit 0, 37 KB, **28 devices**, as `andre`,
  no session bus, no prompt.
- **Most of those 28 are not hardware.** `KEK CA` (twice), `UEFI CA`,
  `Windows UEFI CA`, `Option ROM UEFI CA`, `ThinkPad Product CA`, `SBAT` are
  Secure Boot certificate entries, and five separate rows are all named
  `UEFI Device Firmware`. A readout that prints every row is unreadable and has
  duplicate names. Filter on the `Flags` array (`updatable` is present on real
  ones) and expect to disambiguate repeated `Name`s by `DeviceId`.
- **`Name` and `Version` are both optional.** `linux_display` and
  `GPIO controller` rows have no `Name` and no `Version` at all; `Version` is
  sometimes a raw hex string (`0x0006`). A reader that indexes `Name`
  unconditionally breaks on this machine's own output.

### The ESP name rule was wrong, and the disk says so

`ID_PART_ENTRY_TYPE=c12a7328-f81f-11d2-ba4b-00a0c93ec93b` is on `nvme0n1p1`,
and **it is the only partition on the disk with an `ID_PART_ENTRY_NAME` at
all**. p2-p5 carry a type GUID and no name. So:

- Keying the ESP rule on the name `EFI System Partition` (the draft's rule)
  misses any ESP an installer left unnamed — which, on the evidence of this
  disk, is the normal case for a partition here.
- Worse, treating a missing name as unverifiable refuses **four of five**
  ordinary partitions for a reason that is not true, which is exactly the
  "safe refusal that teaches the user the guard is noise" failure.
- Hence `Reading<PartitionEntry>` with `Option` fields inside: the RECORD
  being absent is a check that did not happen; udev writing no NAME is a
  fact. Two different unknowns, and collapsing them breaks one direction or
  the other. `is_esp` answers `Yes` on the type GUID or MBR `0xef` first,
  `Yes` on the name second, `No` only when a partition type was actually
  read, and `Unsure` when there is no type at all — because a `vfat`
  partition with no readable type is the exact shape of an ESP under a table
  the parser failed on.

### `ops::require_root` calls an unreadable `/proc/self/status` "must run as root"

`ops::effective_uid()` returns `Option<u32>` and `require_root` folds `None`
into the root refusal — its own test says so deliberately ("must fail
CLOSED"). Safe, but it tells the user to run `sudo`, which will not help if
the problem is that `/proc/self/status` was unreadable. `storage::guard`
therefore takes `&Reading<u32>` and reports that case as
`CouldNotVerify { what: "which user is running this" }`. **`ops.rs` was not
changed** — this is the storage guard being stricter about its own inputs,
not a fix to `apex update`.

### FOUND: the guard permitted erasing the live Steam extension

Ran the guard against **real** machine state under sudo with a deliberately
wrong `--confirm` token, so `Unconfirmed` guaranteed no permit while every
other rule still reported. Everything fired correctly except one:

```
/dev/nvme0n1    -> CarriesTheRunningSystem (via p5 at /etc,/sysroot,/boot,/var)
                   + Mounted(/sysroot/ostree/deploy/default/var)
                   + IsTheEsp(p1, GPT type c12a7328-...)
/dev/nvme0n1p5  -> CarriesTheRunningSystem + Mounted
/dev/nvme0n1p1  -> IsTheEsp on the type GUID, mounted nowhere
/dev/nvme0n1p3  -> Unconfirmed only  (correct: unmounted, non-ESP)
/dev/loop0      -> Unconfirmed only  ** WRONG **
```

**`/dev/loop0` is attached to `/lib/extensions/apex-user.raw`** — the live,
merged system extension carrying 219 packages including Steam — and:

- `grep loop /proc/self/mountinfo` -> **0**
- `sudo grep loop /proc/1/mountinfo` -> **0**
- `/sys/block/loop0/holders` -> **empty**
- `losetup -a` -> `/dev/loop0: [0037]:9076916 (/lib/extensions/apex-user.raw)`,
  `autoclear=1`

So an attached, in-use loop device appears in **no** mount table anywhere and
has no holders. With the correct token the guard granted a `Permit`, and
`wipefs --all /dev/loop0` writes *through* the loop device into
`/lib/extensions/apex-user.raw` and destroys the extension.

This is a **fourth** independent way "is the device mounted?" green-lights
destroying live state, alongside the composefs `/`, the never-mounted whole
disk, and the unmounted ESP — and it is the one that lands on the exact
device class the destructive test is supposed to be safe on. The card already
warned that a *test* hardcoding loop0 destroys the extension; the finding is
that the *guard itself* permitted it.

Fix: `BlockDevice` gains `loop_backing: Reading<Option<String>>` from
`/sys/block/<dev>/loop/backing_file` (`Known(None)` = not an attached loop
device, which for a device with no `loop/` dir is a definite answer;
`Unavailable` = the node existed and could not be read, which refuses). An
attached loop device refuses with `LoopDeviceInUse` **unless** the caller
asserts which file it backs and is right — `apex storage erase
--expect-backing-file <path>`, wrong path being its own refusal
`BackingFileMismatch`. That promotes the assertion the card demanded of the
*test* into the product, where it protects everyone and not just the suite.
Paths are canonicalized at the CLI edge on both sides, so `guard` stays a
pure string comparison.

### P2-014 destructive half: two facts that decide the design

- **`/dev/loop0` is already in use on the L16**, backing
  `/lib/extensions/apex-user.raw` — a live, merged system extension. A test
  that hardcodes a loop device number, or takes one from argv, destroys a
  mounted extension. `losetup --find --show` plus an assertion that
  `/sys/block/<dev>/loop/backing_file` is the file the test just created is not
  belt-and-braces here; it is the only thing standing between the suite and
  live machine state. `sudo -n losetup` works (util-linux 2.41.5).
- **`wipefs --backup[=<dir>]` defaults to `$HOME`, and under `sudo` `$HOME` is
  `/root`.** So the signature backup a user is told to keep lands in root's home
  where they will not look, not in `/var/home/andre`. Recovery guidance has to
  name the actual path, and the destructive path should pass an explicit
  `--backup=<dir>` rather than inherit it.
- Not `udisksctl` for any of this — it goes through polkit.

### "Is the device mounted?" green-lights destroying this machine, three ways

Measured from `/proc/self/mountinfo`, `/sys/block` and `/run/udev/data` on the
L16. The obvious guard — refuse if the device is a mount source, and protect
`/`, `/var`, `/boot` — **fails on all three of these independently**:

1. **`/`'s source is `overlay`, fstype `composefs`.** It is not a block device
   and never will be on a bootc machine. The device that actually carries this
   operating system, `/dev/nvme0n1p5` (btrfs, label `apex-root`), is the source
   of `/etc`, `/sysroot`, `/boot`, `/var` and
   `/sysroot/ostree/deploy/default/var` — and of `/` **never**. A
   critical-target list that contains `/` protects nothing at all here. This is
   the same bootc trap as the free-space one wearing a knife.
2. **The whole disk is a mount source zero times.** `grep ' /dev/nvme0n1 '
   /proc/self/mountinfo` → 0. Only `p5` appears. So `wipefs /dev/nvme0n1` —
   which takes the partition table and therefore everything, including the
   running OS — passes an "is this device mounted?" test cleanly. The guard has
   to relate a partition to its parent disk **in both directions**: refuse the
   target if it is a critical source, if it is the parent disk of one, or if a
   critical source is a partition of it. Parent is `/sys/block/<disk>/<part>`,
   so the relation is available without parsing names.
3. **The EFI System Partition is mounted nowhere.** `nvme0n1p1` is vfat,
   partname `EFI System Partition`, and this machine has **0** vfat mounts —
   bootc mounts the ESP only when it needs it. Wiping it leaves an unbootable
   machine and passes a mount check. Its type is legible unprivileged from
   `/run/udev/data/b<maj>:<min>` (`ID_PART_ENTRY_NAME`, `ID_FS_TYPE`), the same
   surface the read half already uses for encryption.

Also measured: the disk holds two other installations —
`p3` btrfs `fl_fedora`, `p4` ext4 `apex-newboot`, `p2` ext4 — so "unmounted"
here means "someone else's operating system", not "spare". And **every
`holders` directory on this machine is empty** (no LUKS anywhere), so the
holder rule cannot be exercised live and must be fixtured. Which is the
argument for the refusal logic being pure and fixture-driven in the first
place.

**The inversion that matters most:** for the read half, a read it could not
perform is a grey row. For the destructive half, **a check it could not perform
must refuse**. An unreadable `holders` directory is not an absence of holders;
an unreadable `mountinfo` is not an absence of mounts. Same sentence this whole
unit keeps re-learning, except here getting it backwards destroys data instead
of mislabelling a row. So the guard takes `Reading`s, and
`Refusal::CouldNotVerify` is a refusal and not a pass.

## Log

- **2026-09-07 05:30** — Worktree created, card written, branch pushed empty.
- **2026-09-07 06:05** — P2-010 landed on the branch and pushed (`ee30c3b`).
- **2026-09-07 10:2x** — Fresh owner. Predecessor's 10 uncommitted files were a
  complete P2-014 read half; committed as `21005dc` after moving the §48 block
  in `Containerfile.base` (it had been spliced between the boot-v2 comment and
  the code that comment describes) and fixing a clippy lint. `eeb6c1f` adds the
  EACCES remedy to the smartctl row, which reached only the human-mode footer.
- **2026-09-07 ~12:0x** — Fresh owner (predecessor died on a usage limit at
  11:42). Its one dirty file was a real, non-compiling P2-014 guard draft;
  repaired, ESP rule re-keyed on the type GUID after measuring the udev
  records, landed as `46d4ac7` and pushed.
