# p1-020
items: P1-020, P1-021, P1-022
repo: apex-shell + apex-os
worktree: /var/tmp/apex-work/wt-p1-020        branch task/p1-020-agent-graph
worktree: /var/tmp/apex-work/wt-p1-020-os     branch task/p1-020-agent-graph-daemon

## NEXT
P1-021's shell half, in wt-p1-020. The daemon is done and pushed, so
`apex agent list --json` now carries `telemetry` on a session whose status
line has run:
  { model, context_pct, five_hour_pct, five_hour_reset, seven_day_pct,
    seven_day_reset, branch, observed_at }   (absent until one has)
Render it: model/context/branch PER ROW, and the 5h/7d windows ONCE in a
header — they are ACCOUNT-wide, so six rows showing the same 62% is the
P1-022 dedup lesson on a second surface. `observed_at` is there so a stale
observation can say so rather than being drawn as current; a status line runs
on a timer, so a quiet session's numbers can be an hour old.
Pattern to follow: a pure src/services/agenttelemetry.js + node test, the way
agentgraph.js and notifybus.js are done.

## PLAN (decided, do not re-derive)
Option (a) from the previous card: children live in the daemon, fed by the
hook events it already receives. `apex agent list --json` serialises
SessionInfo verbatim (apex/src/agent.rs:982), so a new field on that struct
reaches the shell with no CLI change.

1. P1-020 daemon (wt-p1-020-os) — DONE, 2050767, pushed.
   - Request::Event gains agent_id/agent_type, both #[serde(default)] —
     additive, same no-bump reasoning the `event`/`native` keys carry.
     publish_hook and hook::observe carry them out of Payload.
   - SessionInfo.children: Vec<ChildInfo>, #[serde(default)].
     ChildInfo { id, kind, started, ended: Option<u64>, ended_by }.
     NO state string on a child — liveness is derived (ended.is_none() and
     the parent still live), never asserted.
   - Close open children on Stop and on session exit.
     subagent_stop for an unknown id records a child that started and ended
     at the same instant rather than dropping the fact.
   - Five struct-literal sites need the new field (the same five that carry
     native_observed): protocol.rs x2, registry.rs:800, session.rs:474,
     apex/src/task.rs:1064.
   - Tests in apex-agentd/tests/hook_bridge.rs.
1b. P1-020 daemon, /proc half — DONE, 51529a1, pushed.
2. P1-020 shell — DONE, c1a9896, pushed.
   - src/services/agentgraph.js, pure, node-testable like agentstate.js.
     RULE: an ABSENT children field is not an empty one. The live image runs
     the old daemon; absent means "this runtime predates the graph" and must
     draw nothing, not "no subagents".
   - A child row under the session row. No new Qt.rgba(1,1,1,a) — the
     EXPECT_WHITE_FG ratchet in tests/check-color-tokens.sh is 211.
   - tests/agentgraph-test.js, mutation-proven.
3. P1-021 daemon+CLI — DONE, 85e3e2a, pushed. Shell half is NEXT.
   VERIFIED against code.claude.com/docs (statusline.md, settings.md), do not
   re-derive:
   - `--settings` is MERGED, not a replacement, and sits SECOND in precedence:
     managed > --settings > .claude/settings.local.json > .claude/settings.json
     > ~/.claude/settings.json. List keys (hooks, permissions.allow) combine
     across sources; for a scalar/object key like statusLine the docs are
     SILENT, and the precedence chain implies the highest source wins outright.
     So APEX's settings file DOES override Andre's statusLine. Chaining is
     therefore mandatory, not optional, and it works.
   - statusLine keys are type, command, refreshInterval (min 1; unset means
     event-driven only, no timer), padding (default 0), hideVimModeIndicator.
     Carry ALL of the user's across, not just refreshInterval.
   - a non-zero exit is treated as an error; stderr goes to `claude --debug`
     logs only, never to the status line. stdout is the line.
   - payload always has: cwd, version, session_id, transcript_path,
     model.{id,display_name}, workspace.{current_dir,project_dir,added_dirs},
     cost.*, context_window.{total_input_tokens,total_output_tokens,
     context_window_size,used_percentage,remaining_percentage,current_usage},
     exceeds_200k_tokens, fast_mode, output_style.name.
     Conditional: session_name, prompt_id, workspace.git_worktree,
     workspace.repo, effort.level, vim.mode, agent.name, pr.*, worktree.*,
     rate_limits (Pro/Max only, after the first API response), prompt_cache.
   - THE GIT BRANCH IS NOT IN THE PAYLOAD. Derive it from workspace.current_dir.
   - `apex agent statusline`: read bounded stdin, publish telemetry, then
     CHAIN to the user's own statusLine command with the same bytes and copy
     its stdout. Exit 0 always, like hook(). That is the "without breaking
     the terminal statusline" criterion.
   - settings_json emits statusLine pointing at it, carrying the user's own
     refreshInterval across.
   - Payload shape, measured from Andre's own ~/.claude/statusline.sh:
     model.display_name, workspace.current_dir,
     context_window.used_percentage,
     rate_limits.{five_hour,seven_day}.{used_percentage,resets_at}.
     Branch is NOT in the payload; read .git/HEAD from cwd.
   - SessionInfo.telemetry: Option<Telemetry> with observed_at, so the shell
     can say when an observation went stale.
   - 5h/7d are ACCOUNT-wide, not per-session: show them once in a header,
     model/context/branch per row. Repeating one 73% on six rows is the
     P1-022 dedup lesson on a second surface.

## DONE
- P1-021 daemon+CLI, pushed as 85e3e2a on task/p1-020-agent-graph-daemon.
  apex-agent-core/src/statusline.rs (Telemetry, parse, git_branch from
  .git/HEAD incl. worktree pointer files, settings_sources/user_status_line,
  overlay, chain), SessionInfo.telemetry, Request::Telemetry (its own request
  so last_activity does NOT move), hook::settings_json now emits statusLine,
  `apex agent statusline` runs the user's command FIRST and copies its stdout.
  apex-agent-core 462/0, hook_bridge 12/0, policy_invariants 33/0,
  six mutants killed.
- P1-020 shell, pushed as c1a9896 on task/p1-020-agent-graph.
  src/services/agentgraph.js (pure; three-valued `supported`, derived
  liveness, three end words, accounting mirroring graph::account, the
  agent-lifting process rollup), src/services/agents/SubagentRow.qml,
  SessionRow gains a meta-line summary + an expander + a child list below the
  header, SubagentRow registered in src/services/qmldir, both new suites wired
  into ci.yml.
  tests/agentgraph-test.js 67/0 with 8 mutants incl. an inverse;
  tests/run-agent-graph-render-test.sh 13/0 with 3 mutants.
- P1-020 daemon, /proc half, pushed as 51529a1. ChildInfo gains `parent`;
  graph::{ProcEntry, read_processes, process_tree, fill_rss}; the daemon adds
  a live session's descendants at READ time (main.rs add_process_children,
  called from collect_sessions and live_info) and never persists them.
  apex-agent-core 442/0, hook_bridge 9/0, three more mutants killed.
- P1-020 daemon side, pushed as 2050767 on task/p1-020-agent-graph-daemon.
  apex-agent-core/src/graph.rs (ChildInfo/ChildKind/ChildEnd, pure
  bookkeeping + accounting), SessionInfo.children, Request::Event gains
  agent_id/agent_type, publish_hook now takes the whole Observation,
  Session::apply_graph_event + sweeps in set_exited and the orphan-record
  retire pass. apex-agent-core 435/0, hook_bridge 8/0, four mutants killed
  (see the commit message for the numbers).
- P1-022 notification bridge and deduplication, pushed as 46c6ef2.
  src/services/notifybus.js (pure, dedup/refractory/retraction),
  AgentService spawns detached instead of through one shared Process,
  NotificationService gains extraHints + byKey/idForKey/retract.
  tests/notifybus-test.js 41/0 with 9 mutants + an inverse mutant;
  tests/run-notify-spawn-test.sh 10/0 under headless labwc.
- tests/run-agent-center-smoke.sh wired into CI, pushed as 6046b71.

## IN PROGRESS
- nothing half-written

## FOUND
- tests/run-agent-center-smoke.sh FAILS ON THE TIP, before this branch: the
  restarted shell logs `ERROR quickshell.service.pipewire.loop: Failed to
  connect pipewire context. Errno: 112` and the runner counts any ERROR as a
  failure. Confirmed pre-existing by stashing every change and re-running.
  Environmental (private XDG_RUNTIME_DIR has no pipewire socket), not a
  regression. Run it as
  `APEX_OS_ROOT=<apex-os checkout> env -u WAYLAND_DISPLAY -u DISPLAY
   ./tests/run-agent-center-smoke.sh`; without APEX_OS_ROOT it SKIPS and
  reports success, which is worth knowing.
- DEFECT found and fixed on the daemon branch: session ids are allocated
  against a daemon's OWN store, and `/tmp/apex-agent/<id>` is a fixed path, so
  two daemons of the same user both hand out id 1 and the second one's
  teardown deletes the first one's scratch under a running session. Surfaces
  as `bwrap: Can't open source /tmp/apex-agent/1` and reads as a flaky test.
  `$APEX_AGENT_SCRATCH` now moves the root (apex-agent-core/src/paths.rs).
  Also affects a container or a second user session, not just tests.
- LIVE DEFECT, now fixed: a Quickshell Process holds ONE pending command and a
  new assignment overwrites it. AgentService funnelled every notification
  through one, running `notify-send --wait` (which blocks until dismissed), so
  two notifications in one poll ran only the second and everything raised while
  one sat unread was destroyed except the newest. Six agents needing attention
  produced one notification. Measured, not assumed:
  tests/run-notify-spawn-test.sh. Live evidence: pid 1241444 blocked 4h50m.
- SessionInfo (apex-os apexd/apex-agent-core/src/protocol.rs:277) has NO parent
  or children field. P0-011 bridges subagent_start/stop into the daemon but
  hook::observe() flattens them to Working + a detail string, and
  client::publish_hook only carries {id, event, state, detail, native} —
  agent_id and agent_type are dropped at the CLI.
- Claude subagents are in-process, so /proc can never show them. Their only
  on-disk trace is /tmp/claude-<uid>/<project>/<session>/tasks/<id>.output,
  symlinks into ~/.claude/projects/.../subagents/agent-<id>.jsonl. Real (78 and
  65 of them live) but harness-internal and unstable; the hook is the supported
  channel.
- Sandboxed sessions run under `bwrap --unshare-pid` but the host /proc still
  sees every descendant, so a process tree from SessionInfo.pid works for MCP
  servers and tool processes across every adapter.
- Claude Code sends NO desktop notification for "input needed" on this machine:
  its OS notification channel resolves to no_method_available outside
  kitty/ghostty/iTerm, and inputNeededNotifEnabled/agentPushNotifEnabled are
  MOBILE push. The only notify-send in the binary is the re-auth one. So the
  three converging sources are apexd's state, apexd's pending-request feed, and
  the PTY idle rule — all inside APEX.
- CI IS ALREADY RED ON THE TIP, unrelated to this branch: the "Agent Center
  invariants" step greps for `\bsudo\b` and `control\.sock` across
  src/services/agents/*.qml, and P0-022's AgentHelpContent.qml quotes
  `sudo apex request approve` and documents $XDG_RUNTIME_DIR/apex-agentd/
  control.sock as help text. The check cannot tell a string literal in a help
  document from code.
- tests/run-agent-center-smoke.sh NESTS in the running session when
  WAYLAND_DISPLAY is set, and shell.qml has PanelWindows — so it draws a second
  bar on Andre's desktop. Run it as
  `env -u WAYLAND_DISPLAY -u DISPLAY ./tests/run-agent-center-smoke.sh`.
  (p1-048 owns fixing the runners.)

## BLOCKED ON
- nothing
