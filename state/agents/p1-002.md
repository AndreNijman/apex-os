# p1-002
items: P1-002, P1-003, P1-004
repo: apex-os
worktree: /var/tmp/apex-work/wt-p1-002
branch: task/p1-002-cloudflare

## NEXT
Write apexd/apex-secret-core/src/project.rs — the generic project-config
seam (open <project>/apex.toml with O_NOFOLLOW, fstat regular + owner uid,
size cap, parse to toml::Table). Then providers/cloudflare/.

## DONE
- Baseline confirmed on a141cee: 1477 passed, 0 failed, after
  `cargo build --locked` then `cargo test --locked --no-fail-fast`.

## IN PROGRESS
- nothing committed yet

## FOUND
- `cargo test --locked` ALONE on a cold CARGO_TARGET_DIR fails
  `a_hook_inside_a_session_reaches_the_daemon_that_started_it`
  (apex-agentd/tests/hook_bridge.rs:183) because the `apex` bin is not built
  yet and APEX_REQUIRE_APEX_CLI=1 makes that fatal. `cargo build` must run
  first. The program's own verification gate is order-dependent.
- P1-001 leak: apex-secretd/src/service.rs:322 `refuse()` writes `reason`
  into BOTH the reply and the audit line UNSCRUBBED. `scrub_all` (:479) only
  covers `Ok(Performed).output`. So a provider returning
  `Err(ProviderError::Failed(<child output>))` from `mint` (:469) or
  `perform` (:475) — i.e. after the value has been read at :460 — leaks the
  credential to the caller and to the trail.
- P1-001 gap: `Bound` carries only `endpoint` + `detail`, so `bind` cannot
  hand what it resolved to `perform`; a provider must resolve twice. git does
  exactly this — `bind` runs `git remote get-url`, `perform` runs
  `git push <remote>` which resolves the remote AGAIN inside the child — so
  the URL the host pin checked is not provably the URL contacted.

## BLOCKED ON
- nothing
