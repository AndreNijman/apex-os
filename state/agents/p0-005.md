# p0-005

items: P0-005, P0-006, P0-007
repo: apex-os (+ apex-shell)
worktree: /var/tmp/apex-work/wt-p0-005 (shell: /var/tmp/apex-work/wt-p0-005-shell)
branch: task/p0-005-permission-modes (shell: task/p0-005-agent-center-modes)

## NEXT
commit + push the audit-attribution fix, then write the final report.

## DONE
- P0-005/006/007 implementation, tests, docs, shell half — all pushed.
  os: 85df34f 0fa5cf1 ed9030c e2bbc9f 0f7d63d 3dd9c09 02204d6 6399ba2
  shell: d9cdb37
- cargo test --locked APEX_REQUIRE_APEX_CLI=1: 1507, exit 0 (baseline 1436)
- clippy -D warnings --all-targets clean on katana from a wiped target
- shell: 81 checks / 0 failed (baseline 65), self-test 17/17, 16 mutants
- escalation mutation proof: intact 20/0; SIGKILL->SIGTERM 19/1; escalation
  removed 19/1

## IN PROGRESS
Audit attribution fix (uncommitted, builds, core tests green):
- request.rs: PrivilegeRequest.system_grant + audit key
- privilege.rs: decided_by() — session grant -> allow_once +
  "requested-and-covered"; project grant -> allow_for_project +
  "requested-and-granted"
- grants.rs: covering_grant()
- client.rs/protocol.rs: Request::waits_on_a_human() clears the 120s read
  deadline while polkit waits on the person
- 4 new tests

## FOUND
- A request covered by a §4.4 session grant was recorded as
  allow_for_project — a standing grant no human made. Fixed above.
- P0-007 crit 3 is done in the type but partial in the CLI: issue() fills
  capabilities from Verb::names(), so every session grant covers all eight
  verbs. Remainder = one --capabilities flag.
- P0-015 crit 2 now has a subject: short-lived grants exist and
  GrantAuthority::revoke is the unauthenticated hook a lock handler calls.
  Still missing on P0-015's own side: nothing reads the lock.

## BLOCKED ON
nothing
