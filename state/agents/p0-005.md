# p0-005
items: P0-005, P0-006, P0-007
repo: apex-os (+ apex-shell)
worktree: /var/tmp/apex-work/wt-p0-005
branch: task/p0-005-permission-modes
shell worktree: /var/tmp/apex-work/wt-p0-005-shell
shell branch: task/p0-005-agent-center-modes (pushed, d9cdb37)

## NEXT
Nothing. Everything is committed and pushed on both branches; the final
verification has run. The only remaining action is writing the report to the
coordinator (numbers below).

## DONE
- 85df34f grant.rs + journal.rs: the grant model, TTL bounds, boot stamp, and
  the journald mirror that the granted session cannot edit
- 0fa5cf1 auth.rs + files/system/polkit-1/actions/org.apexos.agent.policy:
  the pure "does this need a human" decision, the pkcheck argv, two auth_admin
  actions, xmllint + no-_keep assertions in Containerfile.base
- ed9030c the daemon and CLI: the grant gate in session::start, the three
  protocol verbs, the reaper, `apex agent grants|revoke-grant|renew-grant`,
  PROTOCOL_VERSION 5
- e2bbc9f P0-005: native_observed from Claude's own hook payloads, plus 11
  acceptance tests in policy_invariants.rs and 12 in tests/system_grants.rs
- 0f7d63d the SIGKILL escalation — automatic expiry was a polite request a
  break-glass session could decline. Mutation-checked both ways (19/1 red).
- 3dd9c09 + follow-up: clippy fixes, constant assertions moved to `const _`
- apex-shell d9cdb37: SessionRow shows the agent's own mode and goes red with
  a countdown + revoke button while break-glass is live. 81 checks (was 65),
  self-test 17/17.
- 6399ba2 docs/agent-runtime.md: the System-access grants section. 39 doc
  verbs valid against the built CLI.

## VERIFIED
- cargo test --locked, APEX_REQUIRE_APEX_CLI=1: 1507 tests, exit 0, on this
  machine AND on katana (baseline 1436)
- clippy --locked --all-targets -D warnings: clean, from a wiped target dir,
  in docker.io/library/rust:1 on katana
- tests/check-no-conflict-markers.sh: PASS, both repos
- tests/test-agent-profile.sh 39/39, test-apex-verbs.sh 44/44,
  test-privilege-requests.sh 38/38, check-doc-verbs 39 valid
- no polkit prompt was raised at any point: every daemon test drives a path
  refused before the daemon asks polkit, and the one test that would have
  reached it skips itself when the connection classifies as local

## IN PROGRESS
- nothing. Both worktrees are clean.

## FOUND
- registry::terminate never escalated past SIGTERM, so §3.4's automatic expiry
  was a request a session could trap and ignore while keeping root. Fixed.
- P0-011's `--settings` outranks ~/.claude/settings.json, so a `permissions`
  key in the document APEX injects would silently override the user's own
  bypassPermissions on every managed launch. It has none; now tested.
- `policy.native` is the wrong field for P0-005 crit 3: it reads `inherit` for
  the case that matters, which is APEX having passed no flag.
- Dimension 3 could be written into agent.json as a stored default, which is
  exactly §3.4's forbidden "remember forever". Now reset on load.
- `--unsafe-everything --sandbox project` was expressible and incoherent:
  bwrap sets no_new_privs unconditionally. Now refused.
- The audit trail wrote `expired` before the session was terminated, so it
  claimed a window was over while the session still had root. Split into four
  events, the last only after the process is observed gone.

## BLOCKED ON
- nothing.

## NEEDS A HUMAN ONCE (cannot be tested without a prompt)
- the polkit dialog appears on the desktop, not in the agent PTY
- cancel and a wrong password both refuse the run
- pkcheck exits 0/1 and not 127 once the image ships the action file
- a break-glass session really does end at its TTL on a live machine
