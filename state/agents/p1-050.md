# p1-050 — APEX Remote protocol, pairing, transports

Tasks: **P1-050** (protocol + desktop service), **P1-051** (device pairing),
**P1-052** (LAN + encrypted relay).

Repo: apex-os. Branch `task/p1-050-remote-protocol`, forked from
`origin/roadmap/v2.2` @ `bc4f06e`. Worktree `/var/tmp/apex-work/wt-p1-050`.

## The finding that shapes everything

`apex-agentd` establishes request origin from the *connection's* cgroup
(`apex-agentd/src/origin.rs::classify`). A proxy is not a managed session, so
`Request::DeclareOrigin` refuses it outright ("only a managed session can narrow
its own origin"). Consequence: whatever origin a remote proxy is observed to
have is the origin every request it forwards is recorded under — and if the
proxy is started from a login session rather than a user unit, that origin is
`local-terminal`, which `privilege::decide` accepts as a human at the keyboard
and will let approve a root operation.

That is exactly "laundering a remote request into a local one", and it happens
by default if you start the service the obvious way. So commit 1 is a
**per-connection origin latch** in agentd: a non-session connection may declare
a narrower origin once, monotonically (`origin::may_declare` already refuses any
move towards local), and it sticks for the life of the connection.

## Decisions taken

- Reuse `RequestOrigin::RemoteControl` (`claude-remote-control` on the wire)
  rather than adding an 8th §7 origin. An 8th is a `PROTOCOL_VERSION` bump, a
  change to `Capability::ruling` and the 7×7 declaration test, and a conflict
  with the open P1-020 branch. Device identity travels in a separate `actor`
  field, which answers "which device" better than an origin name would.
- The remote protocol is the **existing agentd control protocol carried inside
  an encrypted channel**, not a second protocol. Provider neutrality then comes
  from the adapter layer that already exists.
- No inbound port for the relay; LAN listener needs one firewall exception.

## NEXT

Everything owned by P1-050 and P1-051 is done and pushed. What is left is
P1-052's remainder, in the order it should be picked up:

1. **Relay.** `apex_remote_core::rendezvous` has the id derivation and the
   disclosure text; nothing dials one. Needs: a Cloudflare Worker + Durable
   Object that pairs two outbound WebSockets on a rendezvous id and copies
   bytes, a client in `apex-remoted` that keeps one outbound connection open,
   and the same in the device. The Noise channel runs end to end through it
   unchanged — that is the point of the framing already being transport
   agnostic.
2. **Reconnect across network changes.** The daemon holds the PTY, so a
   reconnect is a fresh `Noise_IK` handshake plus re-`Open` of the channels
   the device wants. No session state is lost; nothing writes the resume logic.
3. **LAN discovery.** `_apex-remote._tcp` over mDNS. The firewall already
   opens 5353. Deliberately NOT done as a static Avahi service file, which
   would advertise the machine whether or not the service is running.
4. **Connection quality.** `Frame::Ping`/`Pong` carry a token; nothing
   measures or renders the round trip.

And two things outside this repo: the QR renderer and the paired-device page
belong in apex-shell (P1-051 criteria 1 and 3).

## Log

- 2026-09-07 — card created, worktree made, orientation done.
- 2026-09-07 `7041b74` — the origin latch. apexd 1760/0 (baseline at the fork
  point bc4f06e is **1750/0**, measured on a clean worktree; the 1614 in the
  brief is stale). Two mutations: latch never applied 11/0 -> 8/3; `may_declare`
  removed 11/0 -> 9/2.
- 2026-09-07 `05c73da` — `apex-remote-core`: wire framing, device store,
  identity, Noise (NK for pairing, IK for sessions), rendezvous. 53 tests.
  Four mutations, all red: token not consumed 2 fail, revocation ignored 1,
  rendezvous id = the key 2, identity file 0644 1.
- 2026-09-07 `6f6246e` — `apex-remoted` + `apex remote` + the unit + the
  firewall catalogue entry + Containerfile wiring. 26 tests in the daemon.
- 2026-09-07 `71f8a90` — **a mutation survived and was fixed.** "any origin
  may pair" passed every test, because `cargo test` runs in a login session so
  the refusal branch was unreachable. The rule moved into `may_pair`, tested
  over all seven §7 origins; the same mutant is now 25/1.
- 2026-09-07 — clippy in the container: **`roadmap/v2.2` was not clippy-clean
  at `bc4f06e`.** Two pre-existing lints, both fixed here and both in other
  people's files (drop the hunks at integration if that conflicts):
  `apex-secretd/src/providers/cloudflare/tests.rs:335` type_complexity, and
  `apex/src/cloudflare/tests.rs:55` useless format!. With those and my two,
  `tests/run-clippy.sh` is exit 0.
- 2026-09-07 — design note at `ROADMAP/design/P1-050-remote-protocol.md`.
- 2026-09-07 `17b18c4` — end-to-end suite: real `apex-agentd` + real
  `apex-remoted` + a device that generates a keypair and speaks the protocol.
  With the declaration removed from the proxy the run reads
  `PROBE origin="apex-shell" source="observed"` and then
  `a phone approved root: {"decision":"allow_once"}` — the hole, demonstrated.
- 2026-09-07 `322fa45` — three defects the tests could not see, all found by
  reading the code against its own comments: the handshake deadline was
  cleared before it applied (a silent LAN peer held a thread forever),
  `unregister` removed nothing, and `PtyChannel::open` opened a channel even
  when the daemon had not attached. Plus PTY streaming end to end and the
  production declaration pair pinned.

**Final state: workspace 1860 passed / 0 failed (baseline 1750 at `bc4f06e`),
`tests/run-clippy.sh` exit 0, branch pushed at `322fa45`.**
