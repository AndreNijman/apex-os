# p1-048 — headless runners, Blueprint redesign, Gaming redesign

Worktree: `/var/tmp/apex-work/wt-p1-048` on `task/p1-048-guided-settings`
(branched from `origin/roadmap/v2.2` @ `d2d1f33`).

## Job 1 — test runners that map windows on the live session

The brief named four. A survey of `tests/*.sh` found **twelve** scripts that can
reach the ambient display. Three buckets:

1. **Stage a QML and run `quickshell -p` on the inherited display** —
   `run-service-tier-test.sh`, `run-compositor-facade-test.sh`,
   `run-scaling-test.sh`, `run-niri-keybinds-test.sh`,
   `run-plugin-host-test.sh`, `run-nested-labwc.sh`.
2. **Start the whole shell and drive it over IPC** — `run-popup-smoke.sh`,
   `run-nexus-smoke.sh`, `run-remote-agent-smoke.sh`.
3. **Cannot go headless today** — `run-hypr-configerrors-test.sh`
   (Hyprland 0.56.2 will not start headless on a GPU box with no DRM master;
   measured on katana), `measure-idle-inhibit.sh`, `measure-idle-cost.sh`.
   These get a hard refusal, not a comment in the header.

Plan: extract `run-nav-geometry-test.sh`'s shape into `tests/lib/headless.sh`
once instead of nine times; convert buckets 1 and 2 to it; gate bucket 3 behind
`APEX_TEST_ALLOW_NESTED_ON_DESK=1`. `run-nav-geometry-test.sh` itself is left
alone — it is already correct, and rewriting a working suite buys nothing.

Guard: `tests/check-headless-runners.sh`, checking the invariant rather than
"did you source my library", so nav-geometry passes on its own merits.

## NEXT

Write `tests/lib/headless.sh`, then convert `run-service-tier-test.sh` first
and prove it green before touching the other eight.
