# p1-048 — headless runners, Blueprint redesign, Gaming redesign

Worktree: `/var/tmp/apex-work/wt-p1-048` on `task/p1-048-guided-settings`
(branched from `origin/roadmap/v2.2` @ `d2d1f33`).

## Job 1 — test runners that map windows on the live session

The brief named four. A survey of `tests/*.sh` found **twelve**. All twelve are
converted and committed at `0b36897`, along with `tests/lib/headless.sh`
(run-nav-geometry's shape written once: headless wlroots compositor, private
runtime dir + HOME, stubs ahead of PATH, and `headless_assert_private` refusing
the run if the socket is not one this run created).

Established and worth keeping: **Hyprland does come up nested inside a headless
labwc**, and `configerrors` / `reload` / `version` all answer there. The host
labwc must NOT be on the pixman renderer — aquamarine asks it for a dmabuf and
a software-rendered host has none, and the only symptom is CBackend::create()
failing silently. `run-nav-geometry-test.sh` is left alone; it was already
correct.

Still open: `tests/check-headless-runners.sh` — the guard that fails if any
script under `tests/` can reach the ambient display, plus its mutation proof.
To land as its own commit.

## NEXT

Write `tests/check-headless-runners.sh`. Check the invariant (no runner reaches
the ambient display) rather than "did you source my library", so nav-geometry
passes on its own merits. Mutation-prove by reintroducing the ambient
`WAYLAND_DISPLAY` into one runner. Commit alone. Then read P1-048 and P1-049 in
`ROADMAP/roadmap.yaml` and survey the two live pages.
