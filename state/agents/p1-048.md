# p1-048 — headless runners, Blueprint redesign, Gaming page

Worktree: `/var/tmp/apex-work/wt-p1-048` on `task/p1-048-guided-settings`
(branched from `origin/roadmap/v2.2` @ `d2d1f33`). Eight commits, none pushed,
tree clean. NOT rebased — the integrator does that.

## Job 1 — DONE (`0b36897`, `56240d8`, `abb248f`, `5d4acc9`)

`tests/lib/headless.sh` + twelve runners converted;
`tests/check-headless-runners.sh` (four rules, 25 assertions, four inline
mutants + a control); wired into `ci.yml`, which lists every check by name and
would never have run it.

Two bugs found by RUNNING rather than parsing:
- `headless_cleanup` aborted on its own `kill -9` under `set -e` — `set -e`
  exempts only the LEFT operand of `&&`. Green suite exited 1 and leaked its
  sandbox; five were in /tmp.
- The guard read `command -p X` as a lookup. `-p` RUNS X.

Keep: **Hyprland comes up nested in a HEADLESS labwc** (configerrors, reload
and version all answer). The host labwc must NOT be pixman — aquamarine wants a
dmabuf and the only symptom is a silent `CBackend::create()` failure.
Nineteen scripts launch a client, not twelve; eight were already headless
inline and predate the library.

## P1-048 — DONE (`6213f05`, `fd154bc`, `d54e6d1`)

`changeKind`/`ofKind`/`removals` in blueprint.js; the service splits the
user-domain plan; the page reordered (what this is → nothing here yet → editor
→ not saved → what Apply would do → added/changed/**removed** → needs root →
apply → advanced).

Not done, not faked: raw TOML editing (the only write verb takes JSON), Import,
Export, "use this device as Blueprint" (no CLI verbs), transactional apply with
rollback. `EXPECT_WHITE_FG` unchanged at 211 — the page had no colour literals.

## P1-049 — DONE as a first page (`4cd2890`, `7266f50`)

New `gaming.js` + `GamingService.qml` + `GamingPage.qml`, registered in
`src/qmldir`, `src/services/qmldir` and `PageRegistry.qml`. The smoke suites
derive their page list from the registry, so no test-list edits were needed.

Reads `apex gaming --json` (readiness, per-tool presence, blockers, warnings,
`install_hint`) and `apex mode status` (text — no `--json` on that subcommand).
Writes only `apex mode set`, then re-reads status.

**The design trap, worth keeping:** `apex mode set --auto` is documented
one-shot — "APEX ships nothing that re-evaluates this on a timer". So the
master "optimise games automatically" control CANNOT be a switch; it is the
choice plus a read-back. `check-gaming-settings.sh` enforces no Timer anywhere.

Not built, no verb exists: VRR, background-update suppression. Not reached yet:
per-game profiles (`apex game profile` DOES exist — that is the obvious next
increment).

## NEXT

1. `apex game profile` → per-game profiles section on the Gaming page.
2. Ask apex-os for `apex mode status --json`; it would delete `readModeStatus`,
   the weakest thing on the page.
3. P1-048 remainder needs CLI verbs first (blueprint import/export/adopt).
