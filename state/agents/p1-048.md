# p1-048 — headless runners, Blueprint redesign, Gaming redesign

Worktree: `/var/tmp/apex-work/wt-p1-048` on `task/p1-048-guided-settings`
(branched from `origin/roadmap/v2.2` @ `d2d1f33`).

## Job 1 — DONE, three commits

- `0b36897` — `tests/lib/headless.sh` + twelve runners converted.
- `56240d8` — fix: `headless_cleanup` aborted on its own `kill -9` under
  `set -e` (only the LEFT operand of `&&` is exempt). A green suite exited 1
  and leaked its sandbox; five were in /tmp.
- `abb248f` — `tests/check-headless-runners.sh`, four rules, 24 assertions.

Verified by RUNNING, not just parsing: service-tier 88/0, compositor-facade
48/0, scaling 25/0, plugin-host 94/0, niri-keybinds 5/0, popup-smoke clean,
nexus-smoke 0 errors, hypr-configerrors 15/0. The two `measure-*` refuse
without `APEX_TEST_ALLOW_NESTED_ON_DESK=1`. agent-center and remote-agent skip
(no apex-os checkout).

Facts worth keeping:
- **Hyprland comes up nested in a HEADLESS labwc**, and configerrors/reload/
  version all answer. The host labwc must NOT be pixman — aquamarine wants a
  dmabuf and the only symptom is a silent `CBackend::create()` failure.
- The predecessor's survey said twelve. There are nineteen launching scripts;
  the other seven were already headless inline and predate the library.

## NEXT — P1-048 and P1-049

Read both in `ROADMAP/roadmap.yaml`. Nothing done yet. Start with the honest
survey the brief asks for: what each page asks a person to understand, and
which of those questions a person can answer.

Binding constraints: `src/components/config/settings-semantics.js` (four
states live/staged/applied/saved, four verbs Apply/Save/Revert/Reset;
Discard/Undo/Forget/Restore/Commit/Write are banned). Gaming page must not
assume steam/gamescope/mangohud/gamemode exist — they are on-demand
`apex install` packages. `EXPECT_WHITE_FG` in `tests/check-color-tokens.sh`
is 211 and a redesign is the moment to lower it.
