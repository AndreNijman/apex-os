# p1-035
items: P1-035 (screenshot/file injection into TUI agents), P1-036 (worktree
test/conflict/PR status), P1-037 (agent disposable capsule integration)
repo: apex-os
worktree: /var/tmp/apex-work/wt-p1-035
branch: task/p1-035-agent-tooling

## NEXT
Orientation done, nothing written yet. Next action: implement P1-035 —
`Request::Inject { id, source }` in apexd/apex-agent-core/src/protocol.rs,
handled in apexd/apex-agentd/src/main.rs's `dispatch` (NOT in handle_attach),
copying the source file into a path the session's SandboxSpec already binds rw
and then writing the daemon-composed destination path (no trailing newline) to
`s.master` with pty::write_all. Refuse when the connection's peer credentials
resolve to a managed session, the way `Decide` does.

## DONE
- Nothing committed yet.

## IN PROGRESS
- P1-035 design + implementation.

## FOUND (orientation)
- The daemon owns `s.master` for every session; `Request::Resize` already
  reaches `TIOCSWINSZ` through `pty::resize` from an ordinary control
  connection. Injection is the same shape with `pty::write_all`.
- `apex project worktrees` prints name/path/branch only. No test, conflict,
  diff or PR state anywhere.
- `Project.capsule` is a LABEL. Nothing runs an agent inside a capsule today,
  so P1-037 is genuinely new work rather than wiring.
- `files/system/libexec/apex-disposable` says in its own header that it is NOT
  a security boundary (distrobox mounts the host root at /run/host).

## BLOCKED ON
- Nothing.
