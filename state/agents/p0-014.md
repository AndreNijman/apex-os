# p0-014 / p0-015 — Remote Control privilege policy, lock-state policy

Tasks: **P0-014** (remote privilege policy), **P0-015** (lock-state policy).
The last two P0 items with work left in them.

Repos and branches:

- apex-os `task/p0-014-remote-lock`, from `origin/roadmap/v2.2` @ `b2d7905`,
  worktree `/var/tmp/apex-work/wt-p0-014`.
- apex-shell `task/p0-015-lock-tests`, from `origin/roadmap/v2.2` @ `8d081ff`,
  worktree `/var/tmp/apex-work/wt-p0-015-shell`.

## What was already done, and must not be redone

- §7's table is `Capability::ruling`; project edit / tests / push run remotely;
  `may_use_broker` takes no origin. Root and unsafe-everything need a human
  (`privilege::decide`).
- `apex-agentd/src/origin.rs` classifies from the connection's own provenance.
- `lock.rs` has `LockState`, `LockPolicy` (§7 verbatim) and `decide`, checked
  over 8 policies x 7 origins x 4 states. **Nothing calls it.**
- apex-shell `8d081ff` landed `LockedHintService.qml` + `Lockscreen.qml`
  wiring. No test of any kind, and no initial sync.
- P0-006/007 landed real time-bounded root grants;
  `grants.rs GrantAuthority::revoke` is the hook, deliberately unauthenticated.

## The three remainders

1. P0-014 — `--origin-policy remote` parses, stores, and is refused by
   `validate`: no FIDO/WebAuthn path behind it.
2. P0-014 — the root approval path is argued, not tested (needs sudo).
3. P0-015 — no shell test, no initial sync, criterion 1 unwired.

## Plan, ordered by certainty

1. This card.
2. P0-015 OS side: a `LockObserver` trait + `loginctl` reader, transitions
   driven from the existing expiry thread, `LockPolicy` in `config.rs`,
   revoke-on-lock going through the *same* end-the-session path the expiry
   loop uses (break-glass cannot be revoked by dropping a map entry —
   `no_new_privs` is already off).
3. P0-015 shell side: initial sync + headless tests of the shipped singleton.
4. P0-014 remainder 2: end-to-end approval under `unshare -r` (real euid 0,
   no sudo prompt). Write the one-minute human procedure for what is left.
5. P0-014 remainder 1: CTAP2-level assertion verification via openssl (no new
   crates, so no conflict with p1-050's workspace change), owner-imported
   credential from `fido2-cred`, challenge bound to session+kind+ttl.

## Facts established

- This session's cgroup is `session-3.scope`, so `origin::classify` answers
  local-terminal here. `unshare -r` works and gives a real euid 0.
- `state_home()`, `runtime_dir()`, `home()` are all env-overridable, so a
  daemon can be stood up entirely inside a temp tree.
- `openssl` 3.5.7 is in the image; `libfido2` is (the library, not the tools).
- `tests/lib/headless.sh` is NOT on apex-shell `roadmap/v2.2` — it exists only
  on `task/p1-048-guided-settings`.
- apexd baseline: measure on this worktree, do not trust a number from another
  fork point (p1-050 measured 1750 at `bc4f06e`).

## NEXT

Measure the apexd test baseline on `/var/tmp/apex-work/wt-p0-014`, then read
`registry.rs`/`session.rs` for the existing pause/SIGSTOP path and
`config.rs` for where `LockPolicy` should be persisted.

## Log

- 2026-09-07 — card created, both worktrees made, orientation done.
