# p1-023
items: gate repair (Agent Center invariants, agent-center smoke, the grep|pipefail flake), P1-023, P1-024, P1-029
repo: apex-shell AND apex-os (the labwc arm and the daemon verb live there)
worktree: /var/tmp/apex-work/wt-p1-023   branch task/p1-023-handoff-lifecycle
base: origin/roadmap/v2.2 @ 8d081ff — NOT rebased, the integrator merges
pushed: yes. origin/task/p1-023-handoff-lifecycle @ 7544834 (was branch-less
until 2026-09-07; only refs/wip/wt-p1-023- covered it. Push after every commit.)

second worktree: /var/tmp/apex-work/wt-p1-023-os  branch
task/p1-023-push-to-talk-daemon, apex-os, base 95d068c.
pushed: yes. origin/task/p1-023-push-to-talk-daemon @ 8c3958d (was
branch-less until 2026-09-07; only refs/wip/wt-p1-023-os- covered it).
Ignore /var/tmp/apex-work/wt-p1-023-head — a detached scratchpad.

## NEXT
The wiring is DONE and pushed (6b469ec) — do not redo it, and do not re-run its
8-mutant proof, it is written out in the commit message.

**THE VISIBLE INDICATOR IS ALSO DONE** — f5b494b, pushed. Checked, not assumed:
`git show f5b494b` carries src/modules/Center/CenterContent.qml (+102), the
`voice` carousel item keyed on `indicatorLabel !== ""` (so a REFUSAL shows too),
the delegate rendering the label verbatim, the micOpen force-scroll override,
`dismiss` in the reducer + a 6s timer in the service, and the suite extension
(check-push-to-talk.sh 34 -> 42/0, node 51 -> 57 assertions, 13 -> 14 mutants
all caught). The headless probe was re-run in that commit and built the delegate
for the first time. Do NOT build the indicator again and do NOT re-run its
mutants. The old item-1 text that used to live here was written at ~15:20,
BEFORE that commit, and cost this program one wasted start; it is deleted.

**THE HELP PROSE IS ALSO DONE** — 7544834, pushed. The "Speak to it" section
plus BOTH todo blocks (transcript stops here; starting a session for a project
is out of v1), the new check-agent-help.sh section 2c that asserts the pair,
and its 8th self-test mutant. 22 -> 24 passed / 0 failed, 7 -> 8 mutants, all
17 static suites 467/0, slopcheck TOTAL: 0 with NO new allowlist entry. Also
folded in: the one `producer | grep -q` f5b494b had left inside a `bash -c`
string in check-push-to-talk.sh. Zero non-comment `| grep -q` under tests/
again. Do not redo any of this.

**THE DAEMON VERB IS ALSO DONE** — apex-os 8c3958d, pushed. `Request::Input
{ id, data }`, `session::write_input`, `privilege::refuse_input`, the
`apex agent input <id> <text> [--submit]` CLI, completion + doc parity, and
tests/session_input.rs. cargo test 1805 -> 1820 passed / 0 failed / exit 0 (30
binaries), clippy-local exit 0, 11/11 mutants caught. P1-023 is therefore
COMPLETE in substance across both repos, pending the image ordering below.
Testing tips for whoever works apex-os next: cargo clippy is NOT installed on
this machine (no rustup on an atomic image) — use `./files/scripts/clippy-local`,
which runs it in podman; docker.io/library/rust:1.97 is already pulled, so it
costs ~30 s and not a download.

Remaining, in order:
1. P1-024 and P1-029 on a NEW branch from origin/roadmap/v2.2 in a THIRD
   worktree. P1-020 HAS landed — see the corrected decision rule in ## FOUND.

Everything already decided is in ## DESIGN — do NOT re-derive the
hold-vs-toggle finding or the daemon finding.

## MEASURED, DO NOT RE-DERIVE
Four facts about `producer | grep -q` established by running the shell, not by
reading about it (/tmp/sigp/t2.sh, t3.sh):

- It is NOT a rare flake. It is size-dependent and then certain. Against a
  25 KB producer: 0 spurious failures in 200 runs. Against 183 KB and 915 KB:
  200/200. The pipe buffer is 64 KB — under it the producer finishes writing
  and exits before `grep -q` is even scheduled; over it the producer is still
  writing when grep exits at the first match, takes SIGPIPE, and `pipefail`
  hands 141 to the caller. Test files only grow.
- `bash -c '… | grep -q …'` is SAFE from a pipefail parent. A child bash does
  not inherit pipefail (SHELLOPTS is not exported): measured 0/200 on the
  915 KB file, and 200/200 with `set -o pipefail` added inside the string.
- `grep -q PAT <<<"$var"` — 0/200. `grep -q PAT < <(producer)` — 0/200.
- `producer | grep PAT >/dev/null` — also 0/200 (grep reads to EOF), but the
  repo's established fix is process substitution; that is what is used here.

Added 2026-09-07, also measured, also do not re-derive: the run-*.sh half
inverts rather than flakes. `if echo "$out" | grep -q "FAIL"` on a 1.6 MB log
returns 141, the `if` reads that as "no FAIL found", and a suite that FAILED
reports that it passed. Single deterministic shot above the buffer:
old form MISSED a real FAIL line, new form DETECTED it, and on a clean log
neither invents one. Reproducer kept at /var/tmp/p1023-inversion-proof.sh.

## DONE
- 1788443  test(agents): ci.yml "Agent Center invariants" extracted into
  tests/check-agent-center-invariants.sh (296 lines) with the mention-vs-use
  rule. 6 mutants: 4 caught, 2 correctly not-failures (prose mentions). Step
  went from 6+1 findings red to 14 passed / 0 failed. Do not redo this
  mutation proof; it is written out in the commit message.
- da50162  test(smoke): run-agent-center-smoke.sh — the restarted shell's error
  count was reading the log the first shell had already filtered. Before exit 1
  "ERROR count: 1", after exit 0, both counts 0, 5 sessions and 1 request drawn.
- 89cb520  test: the remaining 85 `grep -q` sites across 24 files moved to
  `<<<"$var"` / `< <(producer)`. Behaviour-preserving and measured to be:
  all eleven static suites give the identical count either side of the change
  (agent-help 22, agent-settings 17, blueprint-editor 75, compositor-backends
  36, display-transaction 42, idle-inhibit 17, keybind-lua 16, plugin-platform
  98, remote-agents 13, scale-tokens 5, wheel-value 16 = 357 passed 0 failed).
  Four rewritten sites mutation-proved on a copy, all four caught; harness kept
  at /var/tmp/p1023-mutate.sh. tests/check-no-conflict-markers.sh passes.

- af33aba  feat(voice): src/services/pushtotalk.js + tests/push-to-talk-test.js.
  The P1-023 decision layer as a pure reducer, house pattern (agentstate.js /
  remoteagents.js), driven by node against the file the shell loads.
  51 assertions passed, 0 failed. 13 mutants, all applied to a verified-changed
  copy, all 13 caught (cap never fires / cap fires early / refused target
  records anyway / unconfigured STT records anyway / pinned-exited degrades to
  focused / focused outranks pinned / dead session is a valid target / press
  during transcription restarts / target re-resolved at stop / indicator stops
  naming the target / mic reads open while transcribing / empty transcript
  delivered / reduce mutates its argument). Do not redo this proof; the mutant
  list is in the commit message and the harness is the test file itself.
  NOT in this commit: the keybind entry, the indicator, delivery.

- acd985d  ci(voice): `node tests/push-to-talk-test.js` added to ci.yml as
  "Push-to-talk decision layer", beside the other node suites. 69 steps, yaml
  parses. Without this the suite was a file, not a gate.

- 6b469ec  feat(voice): THE WIRING. tests/check-push-to-talk.sh (324 lines) +
  the `voice-ptt` `_defaults` entry (SUPER+ALT+V, no `type:`) + the
  `IpcHandler { target: "voice-ptt" }` with toggle() and state() +
  src/services/PushToTalkService.qml (237 lines) + the src/qmldir singleton +
  AgentService.lastFocusedId + the "Push-to-talk wiring" ci.yml step.
  Suite 34 passed / 0 failed; the SAME file against the pre-wiring tree acd985d
  is 16 passed / 18 failed, so it fails on absence, not only on mutation.
  8/8 mutants caught (see the section above).
  LOADED FOR REAL, which the static suite cannot do: labwc headless, isolated
  XDG_RUNTIME_DIR, actual shell.qml, never nested in Andre's session.
  `ipc show` lists `target voice-ptt` with `toggle(): string`/`state(): string`;
  `state` -> `idle` (so the singleton resolves through src/qmldir AND
  AgentService resolves from inside it); `toggle` -> "Push-to-talk: no
  speech-to-text command is configured", phase -> `error`, no parecord process
  afterwards, zero "is not a type" in the log. Probe kept, see ## FOUND.
  Green either side: check-keybind-lua 16, check-agent-help 22,
  check-agent-settings 81+17, check-no-conflict-markers. ci.yml 70 steps.

- 7544834  docs(voice): the guide says where push-to-talk stops. The
  AgentHelpContent "Speak to it" section (5 blocks) + the SUPER+ALT+V row in
  the Keyboard section + BOTH `{ k: "todo" }` blocks, check-agent-help.sh
  section 2c (asserts the section and the todo TOGETHER, so neither drifts
  alone) and its 8th self-test mutant, plus the `bash -c` grep -q removal in
  check-push-to-talk.sh (now a `timer_block` function read `< <(...)`).
  22 -> 24 passed / 0 failed, self-test 7 -> 8 applied / 0 failed-to-apply,
  check-push-to-talk unchanged at 42/0, all 17 static suites 467/0, node suite
  14/14 mutants, slopcheck TOTAL: 0, ci.yml 70 steps.
  FAILS ON ABSENCE, not only on mutation: the new suite against HEAD's
  AgentHelpContent.qml is 22 passed / 2 failed. Restored file checksummed back
  (4bc9275…) and re-confirmed 24/0. Do not redo this proof.
  Two slopcheck hits found and fixed before commit, worth knowing because both
  read as ordinary English: "which session it is aimed at" is flagged
  `passive`, and "belongs to the shell and never to a session" is flagged
  `lazy extreme: never`. Do not reach for "never" in guide prose.

- apex-os 8c3958d  feat(voice): a session can be spoken to, and not by another
  agent. `Request::Input { id, data }` (protocol.rs, beside Resize) +
  `session::write_input` returning Written/Exited/Failed + `privilege::
  refuse_input` + the dispatch arm + `apex agent input <id> <text> [--submit]`
  + `input` added to bash/zsh/fish completions, both session-id case arms, and
  docs/agent-runtime.md + apex-agentd/tests/session_input.rs (4 tests, real
  daemon, own XDG dirs, killed by pid).
  cargo test --locked 1805 -> 1820 passed / 0 failed / exit 0, 30 binaries.
  clippy-local exit 0. Mutants 11 applied / 11 caught / 0 survived / 0
  unapplied, control green either side, harness at
  /var/tmp/p1023-input-mutate.sh. Do not re-run; the table is in the message.
  THREE DESIGN POINTS, settled, do not re-derive:
   * the master fd is copied out and the lock RELEASED before pty::write_all,
     which blocks (it waits for writability) when the agent is not draining
     input. Holding the lock would freeze Signal for that session at exactly
     the moment a user needs it.
   * a session may not write into another session, NOR into itself, AND a
     connection that could not be classified at all is refused too —
     `session: None` on an unreadable origin means the ancestry walk never
     happened, which is not evidence of a human.
   * `--submit` appends CR, the daemon adds nothing. The shell does NOT pass
     --submit, so a transcript lands in the prompt and waits for a person.
     MEASURED: in cooked mode CR and LF BOTH end a line (ICRNL), so the PTY
     test does NOT discriminate between them; CR is for raw-mode TUIs and
     nothing tests that case.
  KNOWN GAP: no test drives the dispatch arm's USE of refuse_input from a
  caller that really is a session. The decision is proved as a function, the
  wiring is one line, and proving the wiring needs a process inside a session
  connecting to the socket.

- apex-os 9c38a2e  feat(voice): the labwc arm. `ShellCmd::Voice` + its
  `shell_ipc("voice-ptt","toggle")` arm + a `shell_targets()` row + the
  `"voice-ptt": "voice"` row in apex-labwc-keybinds VERBS. This is step 1 of
  the three-step cross-repo ordering; see ## LANDING ORDER. Measured safe on
  its own: `check-labwc-keybinds <shell-checkout> files/desktop/labwc/rc.xml`
  against the shell tree WITHOUT voice-ptt prints "68 defaults, 4 without a
  labwc equivalent" and PASS, exit 0 — voice-ptt is absent from the shell's
  defaults so it is absent from both the generated block and the seed.

## MUTANTS — RUN, all caught, do NOT re-run
tests/check-push-to-talk.sh, 8 mutants, each on a verified-changed copy
(checksummed before and after), control re-confirmed green after every restore.
Harness kept at /var/tmp/p1023-ptt-mutate.sh. Table is in 6b469ec's message.
  1 entry deleted (6 red) / 2 typed $qsIpc exec added (2) / 3 combo to SUPER+V
  (2) / 4 target misspelled (4) / 5 toggle() renamed (1) / 6 qmldir line
  deleted (2) / 7 parecord in a COMMENT under agents/ (1) / 8 clipboard-toggle
  moved ONTO SUPER+ALT+V (1).
Mutant 8 was ADDED to the card's list of 7 and is the one worth knowing about:
mutant 3 is caught by the hardcoded combo assertion whether or not the
duplicate-combo scan works, so it could not prove that scan. 8 leaves voice-ptt
correct and collides clipboard-toggle onto it, so only the dupe scan can fire.

## LANDING ORDER. Measured safe on
  its own: `check-labwc-keybinds <shell-checkout> files/desktop/labwc/rc.xml`
  against the shell tree WITHOUT voice-ptt prints "68 defaults, 4 without a
  labwc equivalent" and PASS, exit 0 — voice-ptt is absent from the shell's
  defaults so it is absent from both the generated block and the seed.

## MUTANTS TO RUN on tests/check-push-to-talk.sh (from the advisor, not yet run)
Each on a copy of the tree, restore and re-confirm green after every one:
1. delete the `voice-ptt` `_defaults` entry
2. add `type: "exec", command: "$qsIpc voice-ptt toggle"` to it
3. change the combo to SUPER + V (taken by Popups/clipboard-toggle)
4. misspell the IpcHandler `target:` string
5. rename the handler's `toggle()` function
6. delete the qmldir singleton line
7. drop the word `parecord` into a COMMENT in src/services/agents/SessionRow.qml
   — this one must FAIL. Unlike check-agent-center-invariants' mention-vs-use
   rule, a mention of a recorder under agents/ is exactly the thing being
   forbidden, so the test is deliberately stricter there.

## LANDING ORDER — cross-repo, for the integrator, do not re-derive
apex-os `files/scripts/check-labwc-keybinds` compares the SEEDED
files/desktop/labwc/rc.xml against what apex-labwc-keybinds generates from the
shell's `_defaults`, and Containerfile.base clones apex-shell at
`APEX_SHELL_REF` (default `main`; Containerfile.base:593, and
Containerfile.release:17 asserts the vendored commit equals the ref). So the
seed cannot be regenerated in the same commit as the verb:
  1. apex-os 9c38a2e — safe against every shell ref, old or new. DONE.
  2. apex-shell's `voice-ptt` `_defaults` entry reaches the ref the image
     build pins.
  3. THEN, in apex-os, regenerate the seed and commit the one-hunk diff:
       files/system/libexec/apex-labwc-keybinds apply \
         --shell-dir <apex-shell checkout> --shell-path /usr/share/apex-shell \
         --rc files/desktop/labwc/rc.xml --no-reload
     Needs BOTH checkouts present, which is why it is not in step 1.
  4. WHEN THE `input` VERB REACHES AN IMAGE (added 2026-09-08), four edits in
     apex-shell, together or not at all:
       a. add `input` to the transcribed `AGENT_VERBS` at
          tests/check-agent-help.sh:52
       b. add it to the guide's "The full vocabulary" cmd block
       c. delete the `{ k: "todo", t: "This build reaches the transcript and
          stops. …" }` block
       d. flip section 2c to the two-way form 2b uses for Always Unrestricted,
          so a STALE todo fails too
       e. and one more, easy to miss: the shell does NOT pass `--submit`, so
          once the verb ships a transcript lands in the agent's prompt and
          WAITS for the person to press Enter. The guide's new prose must then
          say that, or a user will report that push-to-talk does nothing. The
          default is deliberate (see 8c3958d) — do not "fix" it by adding
          --submit to `_deliver()` without deciding that on purpose.
     Doing (c) without (a) and (b) leaves the guide silent about a shipped
     verb; doing (b) without (a) turns section 1 red in CI, which has no
     apex-os checkout. The comment above 2c in the suite says all of this.
Regenerating early goes red until the shell lands; never regenerating goes red
the moment it does.

## FOUND
- **A SKIPPED INTEGRATION TEST REPORTED `ok`, AND ONLY THE CLOCK SAID SO.**
  apex-agentd's `Response::Session` is an internally-tagged NEWTYPE variant, so
  SessionInfo's fields are at the TOP LEVEL of the reply: the id is
  `reply["id"]`, NOT `reply["session"]["id"]`. The first draft of
  tests/session_input.rs read the nested path, got None, and returned it
  through the same `else { return }` the deliberate skip path uses. All four
  tests printed `ok` in 0.13 s having executed none of their assertions, and
  printed NO skip line, because the skip message is on the other branch. The
  only symptom was the duration: four tests that each sleep 300 ms cannot
  finish in 0.13 s. Now 1.88 s, and an unreadable id PANICS.
  Same family as the pipefail inversion in ## MEASURED: a fixture that cannot
  tell "skipped" from "passed" asserts nothing. When adding to
  tests/request_origin.rs, tests/hook_bridge.rs or tests/system_grants.rs,
  CHECK THE WALL CLOCK, not just the verdict.
- **PushToTalkService.pinnedTarget HAS NO WRITER.** Grepped the whole of src/:
  PushToTalkService.qml:76 declares it, :100 reads it into the reducer's env,
  and nothing anywhere assigns it. There is no pin UI in the Agents tab and no
  `pin` method on the `voice-ptt` IpcHandler. So the reducer's pinned arm —
  and the two node mutants that cover it — are CORRECT and UNREACHABLE at
  runtime, and the only target resolution a user can reach is the focused
  session. NOT fixed here, deliberately: the "explicit target" criterion is met
  by the indicator naming the target before recording (see ## DESIGN), so this
  is a gap and not a failure. The fix is a `pin(id)`/`unpin()` pair on the
  existing handler, ~6 lines, plus a suite assertion, a mutant and a probe
  re-run. The guide says focus-only because that is what the build does.
- **ci.yml's 50 `grep -q` sites are NOT the measured hazard. Do not sweep
  them.** Checked, not assumed: .github/workflows/ci.yml has no `shell:` key,
  no `defaults:` block and no `pipefail` anywhere, so its `run:` steps get
  GitHub's default `bash -e {0}`. Under `-e` WITHOUT pipefail a pipeline's
  status is the LAST command's, i.e. grep's, so a producer taking SIGPIPE is
  discarded and the 141 never reaches the step. Only 6 of the 50 are piped at
  all, and 4 of those are `head -1 file | grep -q`, three orders of magnitude
  under the 64 KB buffer. A naive grep of ci.yml looks alarming and is not.
- **check-agent-help.sh is a CROSS-REPO gate, which constrains what the guide
  may name.** It reads the real `AgentCmd` enum out of an apex-os checkout
  (`APEX_OS_ROOT`, else `../apex-os`) when one exists, else falls back to the
  vocabulary transcribed at check-agent-help.sh:52, then scrapes
  `apex agent <verb>` out of the WHOLE file — todo blocks included — and fails
  on any verb the list lacks. Verified both ways: `APEX_OS_ROOT=` the os
  worktree prints "verb source: read from …/apex/src"; with nothing there it
  prints "transcribed vocabulary", which is what CI gets, since
  /var/tmp/apex-work/apex-os does not exist. => the guide MUST NOT contain the
  literal string `apex agent input` until the verb ships. See ## LANDING ORDER
  step 4.
- check-push-to-talk.sh's `mic_names` pattern forbids
  parecord|arecord|pw-record|pw-cat|wpctl |Quickshell.Services.Pipewire|
  [Pp]ipe[Ww]ire|defaultAudioSource|AudioSource anywhere under
  src/services/agents/, and a MENTION fails there by design.
  AgentHelpContent.qml IS in that directory, so guide prose about the
  microphone may not name what the shell records with. The word "microphone"
  itself is fine and is already at line 159.
- ScreenRecService is declared in BOTH src/qmldir:12 AND src/services/qmldir:20.
  By the repo's OWN documented rule (src/qmldir:24-29, above AgentHelp) that
  means it is instantiated twice, once per module, and several files import both
  modules (DataPage imports "../../../" and "../../"; QuickSettings imports
  "../../" and "../"). So the card's "mirror ScreenRecService, both if it is in
  both" would have shipped two recorders. PushToTalkService mirrors AgentHelp
  instead — src/qmldir only. The ScreenRecService duplication is a latent bug in
  the repo, NOT this unit's to fix, and not something to copy.
- The headless load probe is the only thing that proves any of this wiring
  resolves, and it is NOT in the repo (it needs a compositor, so it cannot be a
  CI step). Copy kept at /var/tmp/p1023-ptt-headless-probe.sh. It forces the
  headless labwc path explicitly: this machine HAS a WAYLAND_DISPLAY, and
  run-agent-center-smoke.sh's nested arm would map a bar onto Andre's desktop.
- Scope was 25 files, not 26. check-unified-search.sh needed NO change: it is
  the suite that discovered this bug and was already written the safe way. The
  card's earlier "unified-search 3" counted its comment lines documenting the
  bug, not live sites.
- Zero non-comment `| grep -q` sites remain anywhere under tests/. The 7
  matches a naive grep still reports are all comment lines in
  check-agent-center-invariants / check-compositor-backends / check-recovery-ui
  / check-remote-agents / check-unified-search that explain the hazard.
- All 24 files are `#!/usr/bin/env bash`; no ci.yml step invokes any of them
  through `sh`, so the `< <(...)` process substitutions are safe there.
- The whole diff is mechanical: every removed line contained `grep -q`, every
  added line contains `< <(` or `<<<`. Nothing else rode along. Checked, not
  assumed.
- P1-023 spec (roadmap.yaml:1465): "Global push-to-talk routing", area voice,
  depends_on nothing, acceptance = works in Hyprland/niri/Floating +
  microphone indicator visible + target session explicit. Gates: tests,
  stop_slop. Nothing in the repo implements any of it yet.
- BOTH P1-024 and P1-029 depend_on P1-020, not just P1-024. P1-029's acceptance
  (distinguish PTY / Remote Control / recurring / cloud job / remote-host /
  subagent / MCP sidecar) is a classification layered on p1-020's
  src/services/agentgraph.js ChildKind, which is NOT on this base. P1-020 is
  status:done but lives on task/p1-020-agent-graph, the branch the queue lists
  as conflicting with this one. So P1-023 is the ONLY unblocked item here.
  RESOLVED 2026-09-07: P1-020 HAS LANDED. The card's own decision rule gave a
  FALSE NEGATIVE and must not be trusted again —
    git branch -r --contains origin/task/p1-020-agent-graph | grep roadmap/v2.2
  prints nothing, because integrate-4 SQUASH-merged, so the branch commit is
  not an ancestor of roadmap/v2.2. Test the SUBSTANCE instead:
    git ls-tree origin/roadmap/v2.2 src/services/ | grep agentgraph
  src/services/agentgraph.js is present on origin/roadmap/v2.2 @ 690014a, 342
  lines. So P1-024/P1-029 go on a NEW branch cut from the new roadmap/v2.2 in a
  SEPARATE worktree. Never merge p1-020 into this branch by hand.
  Note for P1-029: the landed agentgraph.js has NO `ChildKind` enum. A child
  carries a `kind` string and only two values are ever produced or filtered on,
  "subagent" and "process" (agentgraph.js:137, 141, 174). P1-029 needs seven
  (PTY / Remote Control / recurring / cloud job / remote-host / subagent / MCP
  sidecar), so most of them have to be REPORTED by apex-agentd before the
  Agent Center can distinguish them. Expect P1-029 to be cross-repo and
  partial; do not fake the seven by inferring them in QML.

## DESIGN — P1-023 global push-to-talk (measured, not guessed)

THE REAL SPEC is ROADMAP.md §8.2 (line 457), not the 3-line yaml. It adds a
fourth requirement the yaml omits: "no permanent microphone access to all
agents". Full list: compositor-neutral global PTT route to the active project
or focused agent session; clear microphone indicator; explicit routing target;
no permanent mic access to all agents; works in Hyprland, niri and
Floating/labwc. P1-023 is the ONLY `area: voice` item in roadmap.yaml, so it
owns the whole route; nothing else will supply STT later.

### Hold vs toggle — decided: TOGGLE everywhere. Do not re-derive.
Read off the shipped compositors on this machine, not from memory:
- Hyprland 0.56.2 — `bindr` exists, release binds available.
- labwc 0.9.6    — `<keybind key="..." onRelease="yes">`, confirmed in
                   `man 5 labwc-config`. Release binds available.
- niri 26.04     — NO release bind. The complete property set on a bind, from
                   the shipped wiki (/usr/share/doc/niri/wiki/
                   Configuration:-Key-Bindings.md), is exactly: repeat,
                   cooldown-ms, hotkey-overlay-title, allow-when-locked,
                   allow-inhibiting. Nothing fires on release.
ROADMAP.md:122 records "Voice / hold-to-talk is used" as a PROFILE ASSUMPTION,
but the acceptance criterion is "works in Hyprland/niri/Floating". Hold would
work on two of three. So: one semantic, toggle, on all three backends. The
hazard of toggle is a hot mic you forgot about; that is what makes the
indicator load-bearing rather than decorative, and it wants a hard cap too.
NiriBackend.qml:69 independently confirms runtime keybind capture is off on
niri, so PTT must ride the compositor's own config-file binds, not a
shell-side grab.

### Where the bind comes from
KeybindService.qml already generates all three artifacts — `_genLua()` →
~/.config/hypr/apex/shell-keybinds.lua, `_genKdl()` →
~/.config/apex-shell/ApexShellKeybinds.kdl, `_applyLabwc()` splices into a
marked region of ~/.config/labwc/rc.xml (README.md:332). PTT is a new entry in
`_defaults` of type exec dispatching `$qsIpc` (`qs -p <shellDir> ipc call`),
which is how every existing shell-side action already reaches the running
shell. So the "compositor-neutral routing" is a new IpcHandler in
src/state/IpcManager.qml plus one `_defaults` entry — NOT a new input path.

### The privacy contract is already shipped and already asserted
AgentHelpContent.qml:159 tells the user a sandboxed session has "No camera and
no microphone", and check-agent-help.sh asserts on that document (proved by the
todo-block mutant in 89cb520). So the mic MUST stay shell-side: the shell opens
the source only while recording, and only TEXT ever crosses to a session. Any
design that hands an agent the mic contradicts shipped prose and breaks a gate.
Pipewire is already available in-process (Quickshell.Services.Pipewire;
AudioControl.qml:10 holds `Pipewire.defaultAudioSource`), so the indicator and
the capture gate have a real source object to bind to.

### Target resolution — the rule, settled
§8.2 routes "to the active project or focused agent session", so the focused
session is a NAMED target, not a sloppy fallback. Resolve: pinned target →
focused agent session → REFUSE. What "explicit" forbids is the user not knowing
where their words went, so the indicator names the resolved session BEFORE
recording starts, and a refusal says which arm failed ("no session pinned, none
focused"). Refuse-on-no-target stays; refusing merely because the target came
from focus does NOT. "Active project" meaning spawn-a-new-session is OUT OF
SCOPE for v1 — say so in the help prose rather than half-building it.

### THE GAP THAT DECIDES SCOPE — read before planning any commit
There is no way to put text INTO a session today. AgentService.qml's whole
action surface is `apex agent {pause,resume,kill}` + focusTerminal +
reviewRequest; there is no send/prompt/stdin verb. apex-os has none either
(`grep send_input|write_stdin|send_text|"prompt"` over *.rs: one unrelated hit,
main.rs:1289). ROADMAP/design/P1-050-remote-protocol.md confirms the shape:
apex-agentd owns the PTY and every input goes through the daemon's own
protocol; "send prompt" is listed under the REMOTE client's Input and media
(ROADMAP.md:1868), i.e. P1-05x, unbuilt. Screen-scraping/typing into the
focused window is explicitly what that design refuses.
=> Delivering a transcript needs a new apex-agentd verb, but it is NOT a long
   pole — measured, do not re-derive. The full Request enum
   (apex-agent-core/src/protocol.rs:222) is Hello, Run, List, Info, Attach,
   Resize, Signal, Event, Logs, Remove, Prune, PrivilegeRequest, Requests,
   Decide, RequestExecuted, Grants, Revoke, SecretUse, SecretGrant,
   SecretGrants. No Input/Send. BUT `Attach {id, cols, rows, replay}` is
   already a bidirectional PTY byte stream: client.rs:249 forwards stdin
   straight to the socket and the daemon writes those bytes into the PTY
   master. So the write path into a live session EXISTS and is proven; a
   `Request::Input { id, data }` variant reuses it and is on the order of 100
   lines, not a rewrite.
   Do NOT deliver by piping into `apex agent attach`: its reader loop keeps
   running after stdin EOF until the session itself closes, and `replay`
   defaults non-zero so it would dump scrollback. It hangs and it is dirty.
   Session control today is by signal only (apex-agent-core/src/session.rs:386
   maps pause/stop→SIGSTOP), which is why no text path surfaced in the first
   grep.

### STT ownership
No roadmap item anywhere owns speech-to-text and §8.2 says "route". So STT is a
user-configured command hook (audio on stdin, text on stdout), never a bundled
engine. When it is unset, PTT must say it is unconfigured rather than pretend.

### Proposed commit order (tests first, each mutation-proved)
1. tests/check-push-to-talk.sh — static: the bind exists in all three
   generators; the target must be explicit (refuse, never fall back to the
   focused session); the indicator names the target; nothing under
   src/services/agents/ gains a mic. Mutation-prove before any src/ change.
2. The shell half: PTT service + IpcManager handler + `_defaults` entry +
   topbar indicator. Target selection explicit.
3. `Request::Input { id, data }` in a SEPARATE apex-os worktree (never the
   main checkout at /var/home/andre/Projects/apex/apex-os). If it does not get
   built, P1-023 stays in-progress rather than done, and AgentHelpContent.qml
   gains a `{ k: "todo" }` block saying transcript delivery is unbuilt —
   check-agent-help.sh asserts on those blocks, so that is how the honesty is
   enforced rather than merely intended.

### Environment facts checked, do not re-check
- Recorder: `parecord` and `arecord` and `wpctl` are on this machine;
  `pw-record` is NOT. Use parecord (or wpctl) — pw-record would be a
  Containerfile change and a blocker for nothing.
- Node suites are run by ci.yml as plain `node tests/<name>-test.js`
  (lines 128, 154, 163, 465, 895, 984). Match that; no framework.
- The stop_slop gate is `$HOME/.claude/skills/stop-slop/scripts/slopcheck.py`
  run with `--allow-file .slopcheck-allow` (check-agent-help.sh:223). The
  allowlist file already exists at the repo root. Any PTT help prose must pass
  it, and check-agent-help.sh is where that is asserted.
- Do NOT implement "no permanent mic access" by muting
  Pipewire.defaultAudioSource when idle — that mutes the user's other apps
  too. The contract is structural (sessions are sandboxed and
  AgentHelpContent.qml:159 already promises "No camera and no microphone");
  the indicator binds to the shell's OWN recorder Process.running.

- The stop_slop gate is NOT repo-wide. slopcheck.py is run only over
  user-facing prose: check-agent-help.sh:225 over AgentHelpContent.qml and
  check-agent-settings.sh:416 over a settings copy, each asserting `TOTAL: 0`.
  Source-file comments are out of scope — pushtotalk.js scores 51 and that is
  irrelevant. But any PTT prose ADDED to AgentHelpContent.qml must reach
  TOTAL: 0, and .slopcheck-allow entries each need a comment explaining them
  (check-agent-help.sh:234 asserts that too).

### THE LABWC ARM IS NOT IN THIS REPO — measured, do not re-derive
"Works in Hyprland/niri/Floating" is NOT satisfied by adding an entry to
KeybindService `_defaults`. Traced end to end:
- Hyprland and niri are generated in-repo (`_genLua`, `_genKdl`). An entry with
  NO `type` becomes `qs -p <shell> ipc call <action-id> toggle` automatically
  (KeybindService.qml:976-978), so those two arms are free.
- labwc is NOT generated in-repo. `_applyLabwc()` (KeybindService.qml:595) only
  execs `/usr/libexec/apex-labwc-keybinds apply`, which lives in apex-os at
  `files/system/libexec/apex-labwc-keybinds`. It reads the same
  keybinds.json the shell writes, so the data arrives — but the TRANSLATION is
  an allowlist:
    * `VERBS` maps ~20 known action ids to `apex shell <verb>`. An id not in
      that dict and with no `type` returns None and is SKIPPED. Corrected: the
      skip is REPORTED, not silent — apex-labwc-keybinds prints "N without a
      labwc equivalent" and a `skipped <action>` line per action (lines
      426-430). It is not fatal: `do_check` compares the generated block to the
      seeded rc.xml region, and a skipped action is absent from both, so the
      gate stays green while printing the gap. Loud enough to find, quiet
      enough to ship past.
    * The `exec` arm substitutes only `APPS` = {$terminal, $browser,
      $fileManager}. `$qsIpc` is NOT in it, and any command still starting
      with `$` returns None. So `command: "$qsIpc voice-ptt toggle"` is
      silently dropped on labwc too.
    * `session-lock` works on labwc ONLY because "session-lock" is in VERBS
      (→ `apex shell lock`), not because its $qsIpc command translates.
- `apex shell <verb>` is a hand-written clap enum, not a passthrough:
  `ShellCmd` at apexd/apex/src/main.rs:277 with one arm per verb calling
  `shell_ipc(target, method, &[])` (main.rs:1277-1284), plus a verb table at
  main.rs:1061.
=> The labwc arm costs three small apex-os edits: a `ShellCmd::Voice` variant
   and arm, a row in the main.rs:1061 table, and `"voice-ptt": "voice"` in
   VERBS. Small, but apex-os, so a SEPARATE worktree — never the main checkout
   at /var/home/andre/Projects/apex/apex-os.
=> ORDERING, and it is a chicken-and-egg. apex-os `files/scripts/
   check-labwc-keybinds` asserts the SEEDED rc.xml is exactly what the
   generator produces FROM THE SHELL'S `_defaults` (it loads them out of a
   shell checkout). So:
     1. apex-os first: `ShellCmd::Voice` + arm, the main.rs:1061 table row, and
        the `"voice-ptt": "voice"` VERBS row. Harmless on its own — an unused
        verb — and it cannot regenerate rc.xml yet because the shell default
        does not exist.
     2. apex-shell: the `voice-ptt` `_defaults` entry.
     3. apex-os again: regenerate the seeded rc.xml against the updated shell
        tree, which needs BOTH checkouts present.
   Shell-first would ship a bind that works on two compositors and prints a
   skip line nobody reads on the third.
- Free combo: SUPER+ALT+V. Taken already are SUPER+{T,Q,W,E,L,D,Z,C,ESCAPE,N,
  V,A,M,B,F,P,J,arrows,PRINT,1-9}, SUPER+SHIFT+{W,M,SPACE,arrows},
  SUPER+ALT+{W,B,G,H,I}, CTRL+SUPER+{SPACE,arrows,EQUAL,MINUS,0},
  CTRL+SHIFT+ESCAPE, ALT+SPACE, ALT+F9. SUPER+V is NOT free (Popups).

## OVERLAP WITH OTHER BRANCHES (checked, not assumed)
- task/p1-048-guided-settings rewrites tests/run-agent-center-smoke.sh (118
  lines) onto tests/lib/headless.sh, and already fixes the `errors2` line the
  same way. It does NOT tighten the `pipewire` noise pattern. It touches
  check-agent-settings.sh (a pure 84-line deletion) and NOT check-remote-agents.sh.
- task/p1-020-agent-graph carries the pipefail fix for check-agent-settings.sh
  and check-remote-agents.sh. Neither was on this base, so both are fixed here
  with p1-020's exact hunks, which merge as an identical change.
- Neither p1-020 nor p1-048 modifies the "Agent Center invariants" ci.yml step;
  both only append steps. The extraction conflicts with nothing.
- tests/lib/headless.sh is on p1-048 only. Not on this base. Not ported.

## BLOCKED ON
- nothing
