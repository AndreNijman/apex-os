# Handoff — 2026-09-22 (second pause, ~12:58 AWST)

Everything is pushed. `main` has NOT been merged — deliberately, see §4.

## 1. The history rewrite is DONE. Do not redo it.

Both repos re-authored, stripped of every Claude attribution, re-signed,
force-pushed. GitHub answers `VERIFIED: true | valid`, including on a March 2026
commit by an upstream contributor.

| | apex-os | apex-shell |
|---|---|---|
| commits | 2,654 | 1,361 |
| signatures | all good | all good |
| attribution trailers | 0 | 0 |
| trees vs pre-rewrite | identical | identical |

- Key **7A218EF3FE753FA3**, `Andre Nijman <andre.perth.2010@gmail.com>`, ed25519,
  no passphrase, registered on GitHub by Andre. Public copy:
  `/var/home/andre/apex-gpg-public-key.txt`.
- **apex-shell is a fork** — Brainitech (240) and Rahul (18) keep their
  authorship by Andre's explicit choice; only the committer became him, which is
  what makes the signature verify. Do not "finish the job" on those.
- Rollback: `/var/home/andre/apex-git-backup-20260922/` (mirrors + bundles +
  pre-rewrite ref shas). `git -C <backup>/apex-os.git push --mirror origin`.
- **NO AI ATTRIBUTION in any future commit.** Three dispatched agents added the
  trailers back by default and had to be amended. Tell every agent explicitly.

## 2. Landed since the last handoff

- **Colours** — fastfetch/terminal accent, top-left icon, window border. The
  border was a startup race: `Theme.active` arrives from a file watcher, so
  `Component.onCompleted` set it once from an unloaded palette and never again.
- **Launcher flashing** — same bug as the notch flash, eight lines away in
  `HyprlandBackend.qml`. Every Hyprland event killed the in-flight
  `hyprctl clients`; the killed read's empty output was then *assigned*.
  Measured at 66 spurious empty lists per 20 idle seconds. `clients.js` +
  `tests/clients-test.js` (12/0; 4-pass/8-fail against the old code), CI-wired.
- **Installer's three keymap reds** — all in `installer/test-installer-luks.sh`,
  not the files originally suspected. The suite measured its own host, so it had
  **never once passed in CI** since landing on 09-20. Now fixture-driven via
  `APEX_KBD_KEYMAPS`/`APEX_KBD_MODEL_MAP`; 62/0 → 67/0, mutation-verified.
  apex-os `e2e548d2`.

## 3. The USB is flashed and verified

`/dev/sda` carries `apex-os-netinstall-x86_64.iso` (v1.0.0). Read back
byte-for-byte: on-device sha256 == published sha256
(`0b1fa062…9236c`). Layout: `sda1` 1.8G iso9660 APEX-INSTALL, `sda2` 20M vfat
APEXEFI (EFI System), `sda3` 300K. GPT, bootable.

Its previous contents — 176 files of student project PDFs — were copied to
`/var/home/andre/usb-contents-backup-20260922/` first. Only a macOS Spotlight
index artefact failed to copy.

## 4. Why main was NOT merged

Andre: *"when absolutely everything is done you can finally merge."* Two agents
were still running when he paused, so it is not all done. Their branches exist
and are **0 commits ahead** — they had committed nothing yet; their
work-in-progress is in `refs/wip` on the remote:

- `docs/readme-accuracy-os` / `docs/readme-accuracy-shell` — README accuracy
  pass (one dirty file in wt-readme-os, snapshotted).
- `fix/installer-scratch-fallback` — see §5.

**Merging main is high value and should happen as soon as those land.**
`build-image.yml` publishes on `branches: [main]` only, `main` is frozen at
`57f593ad` (2026-09-05), and `roadmap/v2.2` is 1,240 commits ahead with nothing
missing. The live registry image's `org.opencontainers.image.revision` is
literally `57f593ad`. So the fleet's image is three weeks stale, and main is why.

## 5. The netinstall ISO — audited, and one real regression found

- The shipped v1.0.0 ISO's installer **exists in no commit in either repo**. It
  was built from an uncommitted working tree; the ISO was its only copy.
  Preserved at `apex-git-backup-20260922/shipped-installer-v1.0.0/`.
- **Do not rebuild the ISO from the tip as-is.** The tip's `pick_scratch`
  (`installer/apex-install` ~104-137, ~1842-1868) needs ~22 GB of non-tmpfs
  scratch; on a single-internal-disk laptop booted from a plain USB nothing
  qualifies, so the install **refuses** and points at a full offline ISO that was
  never published. Its own commit (`63857891`) says "NOT YET TESTED ON HARDWARE".
  The shipped installer stages inside the target root instead, which is why it
  survived real hardware. An agent (`fix/installer-scratch-fallback`) was
  reconciling the two designs when work paused.
- The shipped ISO is **stale, not dangerous** — not one install-blocking fix on
  the tip addresses a defect it actually has.
- When it IS rebuilt: use `EDITION=apex`, not `daily` (the ISO currently stamps
  `daily`, which resolves only while the registry aliases keep being re-pointed).

## 6. Still outstanding

- Land the two in-flight agent branches, then **merge roadmap/v2.2 → main**.
- Rebuild the netinstall ISO after §5's fix.
- Andre should back up `/var/home/andre/apex-android-signing`.
- `claude-memory` MCP was returning 502 — vault notes for today were not written.
