{
  "//": "APEX-OS Firefox defaults (apex-logs 28-dark-mode.md). Sets dark UI + dark",
  "//2": "content by default; Status 'default' means users can still change it.",
  "policies": {
    "Preferences": {
      "ui.systemUsesDarkTheme": {
        "Value": 1,
        "Status": "default"
      },
      "layout.css.prefers-color-scheme.content-override": {
        "Value": 0,
        "Status": "default"
      },
      "browser.startup.homepage": {
        "Value": "about:home",
        "Status": "default"
      },
      "browser.newtabpage.pinned": {
        "Value": "[]",
        "Status": "default"
      }
    }
  },
  "//3": "Homepage/newtab: Fedora's firefox package sets browser.startup.homepage to the literal data: URL 'data:text/plain,browser.startup.homepage=https://start.fedoraproject.org/' and pins a Fedora Start Page tile. Enterprise policy is applied AFTER default prefs, so this wins regardless of what the package ships; the matching zz-apex-default-prefs.js is the belt to this braces. Status 'default' keeps both user-changeable."
}
